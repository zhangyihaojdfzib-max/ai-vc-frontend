---
title: OlmoEarth v1.1：效率提升3倍的遥感模型系列
title_original: 'OlmoEarth v1.1: A more efficient family of models'
date: '2026-05-19'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/allenai/olmoearth-v1-1
author: ''
summary: OlmoEarth v1.1是Allen AI发布的新一代遥感基础模型系列，在保持v1版本性能的同时，通过优化Token序列长度和合并分辨率Token，将计算成本降低高达3倍。该模型基于Transformer架构，专为处理Sentinel-2等多分辨率卫星影像设计，支持从红树林追踪到作物类型地图等大规模地理空间任务。v1.1通过改进预训练方案，在不牺牲跨波段关系建模能力的前提下，显著提升了推理效率，使更频繁的全球尺度地图更新成为可能。
categories:
- AI产品
tags:
- 遥感基础模型
- Transformer优化
- 计算效率
- 地理空间AI
- OlmoEarth
draft: false
translated_at: '2026-05-20T06:09:56.230533'
---

# OlmoEarth v1.1：一个更高效的模型系列

🧠 模型：https://huggingface.co/collections/allenai/olmoearth | 📄 技术报告：https://allenai.org/papers/olmoearth_v1_1 | 💻 代码：https://github.com/allenai/olmoearth_pretrain

我们于2025年11月发布了OlmoEarth（v1）。自那以来，合作伙伴已将其广泛应用于各类任务，从追踪红树林变化、分类森林损失驱动因素，到在数天内生成国家尺度的作物类型地图，并将部署规模扩展至国家、大陆和全球区域。每一次发布都让我们更接近使命：将最先进的AI带给致力于保护人类和地球的组织与社区。

当OlmoEarth处理卫星影像以在数万至数十万平方公里范围内进行预测时，效率决定了可能性边界。在运行OlmoEarth的完整生命周期中——数据导出、预处理、推理和后处理——计算成本是最高的开销。更高效的模型意味着我们可以在OlmoEarth平台上支持更多合作伙伴，也意味着任何自行运行OlmoEarth的用户都能以更低成本更快地利用这项技术。

正因如此，我们构建了OlmoEarth v1.1：一个新的模型系列，在保持OlmoEarth v1在研究基准测试及与合作伙伴共同构建的任务上性能的同时，将计算成本降低高达3倍。

### 通过缩短序列长度提升效率

OlmoEarth模型基于Transformer架构，这是当今机器学习的主流架构之一。为了处理遥感数据，我们首先将其转换为模型可摄入的Token序列。

控制Transformer模型效率的两个重要杠杆是：模型大小（这也是我们发布模型系列的原因，用户可根据计算预算选择合适的大小）和Token序列长度。计算成本随Token序列长度呈二次方增长，因此即使小幅缩减也能显著降低运行模型的成本。

![bench-capture-2026-05-18T14-40-39](/images/posts/2eb2067436d7.png)

MACs（乘累加操作数）用于估算单次模型前向传播所需的计算量；MACs越低通常意味着推理更便宜、更快。纵轴为反向显示，因为平均排名越低越好。标签显示模型系列和大小。所有绘制点均使用粘贴的MAC/排名值。

### 设计Token

这引出了基于Transformer的遥感模型的一个重要问题：Token应该代表什么？

以我们常处理的Sentinel-2影像为例。一个Sentinel-2输入是一个具有高度和宽度（H, W代表纬度和经度像素）、时间维度T以及12个Sentinel-2通道（[H, W, T, D=12]）的张量。

![OlmoEarth v11 blog copy - Google Docs-image-3](/images/posts/ad248c5424dd.png)

目前，我们将数据划分为基于分辨率的图块。具体来说，我们会选择某个空间图块大小p，并将整个Sentinel-2图像分割为p x p大小的图块：

对于每个图块，我们为每个时间步和每个分辨率创建一个Token。因此，一个具有2个时间步的Sentinel-2输入，每个图块会产生6个Token（2个时间步 x 3个分辨率：10米、20米和60米）。

总计，一个[H, W, T, D=12]的Sentinel-2输入将产生 H/p x W/p x T x 3 个Token。

在处理Sentinel-2数据时，为每个分辨率使用独立的Token是一种常见技术——Galileo和SatMAE均采用此方法，且SatMAE在这样做时显示出显著更好的结果。然而，这并非通用做法：CROMA是一个仅对所有波段使用单一Token的模型，无论分辨率如何。由于Token数量呈乘法叠加，将分辨率合并为单一Token可减少三分之二的Token数量，并在预训练、微调和推理中带来实质性节省。

以这种方式简单合并Token会导致性能显著下降，包括在m-eurosat kNN（遥感模型常用基准任务）上下降10个百分点。我们假设，将Sentinel-2波段分离到不同Token中，能使OlmoEarth更容易建模重要的跨波段关系。

在不影响性能的前提下合并Token，要求我们修改预训练方案。我们在论文中详细描述了这些变更。

### 面向开发者

结果是一个用更少资源做更多事情的模型系列。在每种规模下，OlmoEarth v1.1的运行成本比OlmoEarth v1低至多三倍，使每个运行OlmoEarth的团队都能更经济地进行频繁的星球尺度地图更新。如果你正在使用原始OlmoEarth模型系列中的模型，请尝试OlmoEarth v1.1。它在提供与OlmoEarth v1相似性能的同时，仅需三分之一的计算量，不过我们也观察到一些性能回退（详见技术报告）。如果它适用于你的任务，你将在微调和推理期间看到显著的加速。

### 面向研究人员

预训练遥感模型具有许多自由度，这使得它们难以研究。当性能发生变化时，是架构、数据集还是预训练算法导致的？

我们在与OlmoEarth v1相同的数据集上训练OlmoEarth v1.1，因此两者之间的任何差异都隔离了方法论变更的影响。我们希望这能推动对遥感模型预训练中科学原理的理解。

### 开始使用

查看OlmoEarth v1.1的权重和训练代码，包括我们的Base、Tiny和Nano模型的权重。

---

> 本文由AI自动翻译，原文链接：[OlmoEarth v1.1: A more efficient family of models](https://huggingface.co/blog/allenai/olmoearth-v1-1)
> 
> 翻译时间：2026-05-20 06:09
