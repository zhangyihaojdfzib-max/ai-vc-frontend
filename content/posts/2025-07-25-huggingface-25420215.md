---
title: Parquet内容定义分块：提升Hugging Face数据集存储与传输效率
title_original: Parquet Content-Defined Chunking
date: '2025-07-25'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/parquet-cdc
author: ''
summary: 本文介绍了Apache Parquet内容定义分块（CDC）功能及其在Hugging Face Xet存储层中的应用。CDC通过智能分块和去重技术，仅上传或下载已更改的数据块，显著减少了Parquet文件在微小修改（如增删列、更改类型、插入行等）时产生的重复数据传输和存储开销。文章通过多个具体用例展示了CDC在PyArrow和Pandas中的使用方法与性能优势，为大规模数据集的高效管理提供了解决方案。
categories:
- AI基础设施
tags:
- Parquet
- 数据存储
- Hugging Face
- 内容定义分块
- 数据去重
draft: false
translated_at: '2026-02-27T04:39:39.490935'
---

# Parquet 内容定义分块

通过利用新的 Xet 存储层和 Apache Arrow 的 Parquet 内容定义分块功能，减少 Hugging Face Hub 上 Parquet 文件的上传和下载时间，从而实现更高效、可扩展的数据工作流。

**太长不看版**：Parquet 内容定义分块现已可在 PyArrow 和 Pandas 中使用，它能在 Hugging Face 的 Xet 存储层等内容寻址存储系统上高效地对 Parquet 文件进行去重。CDC 通过仅上传或下载已更改的数据块，显著降低了数据传输和存储成本。通过传递 `use_content_defined_chunking` 参数来启用 CDC：

```python
import pandas as pd
import pyarrow.parquet as pq

df.to_parquet("hf://datasets/{user}/{repo}/path.parquet", use_content_defined_chunking=True)
pq.write_table(table, "hf://datasets/{user}/{repo}/path.parquet", use_content_defined_chunking=True)

```

- 引言
- 数据准备
- Parquet 去重的不同用例
    1. 重新上传表的精确副本
    2. 在表中添加和删除列
    3. 更改表中的列类型
    4. 追加新行和连接表
    5. 在表中插入/删除行
    6. 使用不同的行组大小
    7. 使用不同的文件级分割
- 在 Pandas 中使用 Parquet CDC 功能
- 参考文献
- 结论

- 1. 重新上传表的精确副本
- 2. 在表中添加和删除列
- 3. 更改表中的列类型
- 4. 追加新行和连接表
- 5. 在表中插入/删除行
- 6. 使用不同的行组大小
- 7. 使用不同的文件级分割

## 引言

Apache Parquet 是一种列式存储格式，在数据工程社区中被广泛使用。

截至目前，Hugging Face 托管着近 21 PB 的数据集，其中仅 Parquet 文件就占用了超过 4 PB 的存储空间。因此，优化 Parquet 存储是重中之重。
Hugging Face 引入了一个名为 `Xet` 的新存储层，它利用内容定义分块来高效地对数据块进行去重，从而降低存储成本并提高下载/上传速度。

虽然 Xet 与格式无关，但 Parquet 的布局和基于列块（数据页）的压缩可能会对仅有微小更改的数据产生完全不同的字节级表示，从而导致去重性能不佳。为了解决这个问题，应以一种能最小化相似数据之间字节级差异的方式写入 Parquet 文件，这正是内容定义分块发挥作用的地方。

让我们来探讨新的 Parquet CDC 功能与 Hugging Face 的 Xet 存储层结合使用所带来的性能优势。

## 数据准备

出于演示目的，我们将使用 `OpenOrca` 数据集中一个大小适中的子集。

```python
import numpy as np
import pyarrow as pa
import pyarrow.compute as pc
import pyarrow.parquet as pq
from huggingface_hub import hf_hub_download


def shuffle_table(table, seed=40):
    rng = np.random.default_rng(seed)
    indices = rng.permutation(len(table))
    return table.take(indices)



path = hf_hub_download(
    repo_id="Open-Orca/OpenOrca", 
    filename="3_5M-GPT3_5-Augmented.parquet", 
    repo_type="dataset"
)


orca = pq.read_table(path, schema=pa.schema([
    pa.field("id", pa.string()),
    pa.field("system_prompt", pa.string()),
    pa.field("question", pa.large_string()),
    pa.field("response", pa.large_string()),
]))


orca = orca.add_column(
    orca.schema.get_field_index("question"),
    "question_length",
    pc.utf8_length(orca["question"])
)
orca = orca.add_column(
    orca.schema.get_field_index("response"),
    "response_length",
    pc.utf8_length(orca["response"])
)


orca = shuffle_table(orca)


table = orca[:100_000]


table[:3].to_pandas()

```

### 将表作为 Parquet 文件上传到 Hugging Face Hub

自 `pyarrow>=21.0.0` 起，我们可以在 `pyarrow` 函数中使用 Hugging Face URI，通过 `hf://` URI 方案直接向 Hub 读取和写入 parquet（及其他文件格式）文件。

```python
>>> pq.write_table(table, "hf://datasets/kszucs/pq/orca.parquet")
New Data Upload: 100%|███████████████████████████████████████████████| 96.1MB / 96.1MB, 48.0kB/s  
Total Bytes:  96.1M
Total Transfer:  96.1M

```

我们可以看到，该表已作为新数据被完整上传（总字节数 == 总传输量），因为 Xet 存储层尚不知道它。现在将其作为 `pyarrow` 表读回：

```python
downloaded_table = pq.read_table("hf://datasets/kszucs/pq/orca.parquet")
assert downloaded_table.equals(table)

```

请注意，所有接受文件路径的 `pyarrow` 函数也接受 Hugging Face URI，例如 `pyarrow datasets`、`CSV functions`、`incremental Parquet writer` 或仅读取 parquet 元数据：

```python
pq.read_metadata("hf://datasets/kszucs/pq/orca.parquet")

```

```
<pyarrow._parquet.FileMetaData object at 0x16ebfa980>
  created_by: parquet-cpp-arrow version 21.0.0-SNAPSHOT
  num_columns: 6
  num_rows: 100000
  num_row_groups: 1
  format_version: 2.6
  serialized_size: 4143

```

## Parquet 去重的不同用例

为了演示内容定义分块功能的有效性，我们将尝试在以下情况下测试其性能：

1.  重新上传表的精确副本
2.  在表中添加/删除列
3.  更改表中的列类型
4.  追加新行和连接表
5.  在表中插入/删除行
6.  更改表的行组大小
7.  使用不同的文件级分割

### 1. 重新上传表的精确副本

虽然这个用例听起来很简单，但传统的文件系统不会对文件进行去重，导致数据被完全重新上传和重新下载。相比之下，利用内容定义分块的系统可以识别出文件内容相同，从而避免不必要的数据传输。

```python
>>> pq.write_table(table, "hf://datasets/kszucs/pq/orca-copy.parquet")
New Data Upload: |                                                   |  0.00B /  0.00B,  0.00B/s  
Total Bytes:  96.1M
Total Transfer:  0.00

```

我们可以看到没有上传任何新数据，并且操作是即时完成的。现在让我们看看，如果我们将同一个文件再次上传到不同的仓库会发生什么：

```python
>>> pq.write_table(table, "hf://datasets/kszucs/pq-copy/orca-copy-again.parquet")
New Data Upload: |                                                   |  0.00B /  0.00B,  0.00B/s  
Total Bytes:  96.1M
Total Transfer:  0.00

```

由于去重功能在跨仓库时也有效，上传再次是即时完成的。这是 Xet 存储层的一个关键特性，允许高效的数据共享和协作。您可以在《从块到块：加速 Hub 上的上传和下载》博客文章中阅读更多关于细节和扩展挑战的信息。

### 2. 在表中添加和删除列

首先将原始表和更改后的表写入本地 parquet 文件，以查看它们的大小：

```python
table_with_new_columns = table.add_column(
    table.schema.get_field_index("response"),
    "response_short",
    pc.utf8_slice_codeunits(table["response"], 0, 10)
)
table_with_removed_columns = table.drop(["response"])
    
pq.write_table(table, "/tmp/original.parquet")
pq.write_table(table_with_new_columns, "/tmp/with-new-columns.parquet")
pq.write_table(table_with_removed_columns, "/tmp/with-removed-columns.parquet")

```

```python
!ls -lah /tmp/*.parquet

```

```
-rw-r--r--  1 kszucs  wheel    92M Jul 22 14:47 /tmp/original.parquet
-rw-r--r--  1 kszucs  wheel    92M Jul 22 14:47 /tmp/with-new-columns.parquet
-rw-r--r--  1 kszucs  wheel    67M Jul 22 14:47 /tmp/with-removed-columns.parquet

```

现在将它们上传到 Hugging Face，看看实际传输了多少数据：

```python
>>> pq.write_table(table_with_new_columns, "hf://datasets/kszucs/pq/orca-added-columns.parquet")
新数据上传: 100%|███████████████████████████████████████████████|  575kB /  575kB,  288kB/s
总字节数:  96.6M
总传输量:  575k

```

我们可以看到，只有新列以及放置在文件页脚的新 Parquet 元数据被上传了，而原始数据并未再次传输。这是 Xet 存储层的一个巨大优势，因为它允许我们高效地添加新列，而无需再次传输整个数据集。

删除列的情况也同样适用，如下所示：

```python
>>> pq.write_table(table_with_removed_columns, "hf://datasets/kszucs/pq/orca-removed-columns.parquet")
新数据上传: 100%|███████████████████████████████████████████████| 37.7kB / 37.7kB, 27.0kB/s
总字节数:  70.6M
总传输量:  37.7k

```

为了更好地理解上传了什么内容，我们可以使用去重估算工具来可视化两个 Parquet 文件之间的差异：

```python
from de import visualize

visualize(table, table_with_new_columns, title="With New Columns", prefix="orca")

```

#### 添加新列

![Parquet none nocdc](/images/posts/10528c5b6e7b.png)

![Parquet snappy nocdc](/images/posts/fc7c13d95ee8.png)

添加两个新列意味着我们有未出现过的数据页必须传输（以红色高亮显示），但其余数据保持不变（以绿色高亮显示），因此不会被再次传输。请注意页脚元数据中的小块红色区域，它几乎总是在我们修改 Parquet 文件时发生变化。去重统计显示 `<去重后大小> / <总大小> = <去重率>`，其中比率越小意味着去重性能越高。

同样地，可视化删除列后的差异：

```python
visualize(table, table_with_removed_columns, title="With Removed Columns", prefix="orca")

```

#### 删除列

![Parquet none nocdc](/images/posts/39983cf951e6.png)

![Parquet snappy nocdc](/images/posts/c1cbedb02ddf.png)

由于我们删除了整个列，我们只能看到页脚元数据中的变化，所有其他列保持不变且已存在于存储层中，因此它们不会被再次传输。

### 3. 更改表中的列类型

另一个常见用例是更改表中的列类型，例如为了减少存储大小或针对特定查询优化数据。让我们将 `question_length` 列的数据类型从 `int64` 更改为 `int32`，看看传输了多少数据：

```python


table_without_text = table_with_new_columns.drop(["question", "response"])


table_with_casted_column = table_without_text.set_column(
    table_without_text.schema.get_field_index("question_length"),
    "question_length",
    table_without_text["question_length"].cast("int32")
)

```

```python
>>> pq.write_table(table_with_casted_column, "hf://datasets/kszucs/pq/orca-casted-column.parquet")
新数据上传: 100%|███████████████████████████████████████████████|  181kB /  181kB,  113kB/s
总字节数:  1.80M
总传输量:  181k

```

再次，我们看到只有新列和更新后的 Parquet 元数据被上传了。现在可视化去重热图：

```python
visualize(table_without_text, table_with_casted_column, title="With Casted Column", prefix="orca")

```

#### 转换列类型

![Parquet none nocdc](/images/posts/0921291f4d6f.png)

![Parquet snappy nocdc](/images/posts/74b00f722c27.png)

第一个红色区域表示添加的新列，而第二个红色区域表示页脚中更新后的元数据。其余数据保持不变，不会被再次传输。

### 4. 追加新行和连接表

我们将通过将原始数据集的另一个切片连接到表来追加新行。

```python
table = orca[:100_000]
next_10k_rows = orca[100_000:110_000]
table_with_appended_rows = pa.concat_tables([table, next_10k_rows])

assert len(table_with_appended_rows) == 110_000

```

现在检查是否只有新行被上传，因为原始数据 Xet 存储层已经知晓：

```python
>>> pq.write_table(table_with_appended_rows, "hf://datasets/kszucs/pq/orca-appended-rows.parquet")
新数据上传: 100%|███████████████████████████████████████████████| 10.3MB / 10.3MB, 1.36MB/s
总字节数:  106M
总传输量:  10.3M

```

```python
visualize(table, table_with_appended_rows, title="With Appended Rows", prefix="orca")

```

#### 追加行

![Parquet none nocdc](/images/posts/8c3d63ac1145.png)

![Parquet snappy nocdc](/images/posts/a8db7550f17c.png)

由于每列都获得了新数据，我们可以看到多个红色区域。这是由于实际的 Parquet 文件规范，其中整个列是按顺序排列的（在每个行组内）。

### 5. 在表中插入/删除行

这里来到了困难的部分，因为插入和删除会移动现有行，从而导致 Parquet 术语中不同的列块或数据页。由于每个数据页都是单独压缩的，即使是单行的插入或删除，也可能导致从被编辑的行开始到 Parquet 文件末尾的字节级表示完全不同。

这个 Parquet 特有的问题不能仅靠 Xet 存储层解决，Parquet 文件本身需要以一种即使在有插入或删除行的情况下也能最小化数据页差异的方式来写入。

让我们尝试使用现有机制，看看它的表现如何。

```python
table = orca[:100_000]


table_with_deleted_rows = pa.concat_tables([
    orca[:15_000],
    orca[18_000:60_000],
    orca[61_000:100_000]
])


table_with_inserted_rows = pa.concat_tables([
    orca[:10_000],
    orca[100_000:101_000],
    orca[10_000:50_000],
    orca[101_000:103_000],
    orca[50_000:100_000],
])

assert len(table) == 100_000
assert len(table_with_deleted_rows) == 96_000
assert len(table_with_inserted_rows) == 103_000

```

```python
>>> pq.write_table(table_with_inserted_rows, "hf://datasets/kszucs/pq/orca-inserted-rows.parquet")
新数据上传: 100%|███████████████████████████████████████████████| 89.8MB / 89.8MB, 42.7kB/s
总字节数:  99.1M
总传输量:  89.8M

```

```python
>>> pq.write_table(table_with_deleted_rows, "hf://datasets/kszucs/pq/orca-deleted-rows.parquet")
新数据上传: 100%|███████████████████████████████████████████████| 78.2MB / 78.2MB, 46.5kB/s
总字节数:  92.2M
总传输量:  78.2M

```

同样地，可视化两种情况以查看差异：

```python
visualize(table, table_with_deleted_rows, title="Deleted Rows", prefix="orca")
visualize(table, table_with_inserted_rows, title="Inserted Rows", prefix="orca")

```

#### 删除行

![Parquet none nocdc](/images/posts/17ebb0b27f63.png)

![Parquet snappy nocdc](/images/posts/6b62baf05cf4.png)

#### 插入行

![Parquet none nocdc](/images/posts/96fb605cae4e.png)

![Parquet snappy nocdc](/images/posts/6409288a80a2.png)

我们可以看到去重性能显著下降（比率更高），并且去重热图显示压缩后的 Parquet 文件彼此差异很大。这是因为插入和删除的行移动了现有行，导致从被编辑的行开始到 Parquet 文件末尾的数据页不同。

我们可以通过使用 pyarrow 的一项名为内容定义分块（CDC）的新功能来写入 Parquet 文件来解决这个问题。该功能确保列根据其内容被一致地分块成数据页，类似于 Xet 存储层对数据进行去重的方式，但这是在序列化或压缩发生之前，应用于列的逻辑值。

可以通过向 `write_parquet` 函数传递 `use_content_defined_chunking=True` 来启用此功能：

```python
import pyarrow.parquet as pq
```

pq.write_table(table, "hf://user/repo/filename.parquet", use_content_defined_chunking=True)

```

Pandas 也支持此新功能：

```python
df.to_parquet("hf://user/repo/filename.parquet", use_content_defined_chunking=True)

```

让我们可视化使用 Parquet CDC 功能前后的去重差异：

```python
visualize(table, table_with_deleted_rows, title="With Deleted Rows", prefix="orca", with_cdc=True)
visualize(table, table_with_inserted_rows, title="With Inserted Rows", prefix="orca", with_cdc=True)

```

![Parquet none cdc](/images/posts/6289facea4f9.png)

![Parquet snappy cdc](/images/posts/bf588d05ed79.png)

![Parquet none cdc](/images/posts/746f0b31ea8a.png)

![Parquet snappy cdc](/images/posts/01d74c5f4da4.png)

看起来好多了！实践出真知，让我们实际使用内容定义分块 Parquet 功能上传表格，看看传输了多少数据。

注意，我们需要先启用内容定义分块上传原始表格：

```python
>>> pq.write_table(table, "hf://datasets/kszucs/pq/orca-cdc.parquet", use_content_defined_chunking=True)
New Data Upload: 100%|███████████████████████████████████████████████| 94.5MB / 94.5MB, 46.5kB/s  
Total Bytes:  96.4M
Total Transfer:  94.5M

```

```python
>>> pq.write_table(
...     table_with_inserted_rows, 
...     "hf://datasets/kszucs/pq/orca-inserted-rows-cdc.parquet", 
...     use_content_defined_chunking=True
... )
New Data Upload: 100%|███████████████████████████████████████████████| 6.00MB / 6.00MB, 1.00MB/s  
Total Bytes:  99.3M
Total Transfer:  6.00M

```

```python
>>> pq.write_table(
...     table_with_deleted_rows, 
...     "hf://datasets/kszucs/pq/orca-deleted-rows-cdc.parquet", 
...     use_content_defined_chunking=True
... )
New Data Upload: 100%|███████████████████████████████████████████████| 7.57MB / 7.57MB, 1.35MB/s  
Total Bytes:  92.4M
Total Transfer:  7.57M

```

上传的数据量比之前显著减少，如上文热力图所示，去重性能得到了极大改善。

需要注意的是，同样的性能优势也适用于使用 `huggingface_hub.hf_hub_download()` 和 `datasets.load_dataset()` 函数的下载操作。

### 6. 使用不同的行组大小

根据读写器的限制，有时使用更大或更小的行组大小可能更有益。Parquet 写入器实现默认使用固定大小的行组，例如 pyarrow 的默认值是 100 万行。数据集写入器可能会减小行组大小，以提高随机访问性能或减少读取器应用程序的内存占用。

更改行组大小会导致行在不同行组之间移动，值在不同数据页之间移动，因此我们遇到了与插入或删除行类似的问题。让我们使用 Parquet CDC 功能比较不同行组大小之间的去重性能：

```python
from de import visualize


table = orca[2_000_000:3_000_000]

visualize(table, (table, {"row_group_size": 128 * 1024}), title="Small Row Groups", with_cdc=True, prefix="orca")
visualize(table, (table, {"row_group_size": 256 * 1024}), title="Medium Row Groups", with_cdc=True, prefix="orca")

```

#### 小行组

![Parquet none nocdc](/images/posts/238320ebcde7.png)

![Parquet none cdc](/images/posts/c04f57963442.png)

![Parquet snappy nocdc](/images/posts/493d3361cc35.png)

![Parquet snappy cdc](/images/posts/f6b5ec4837c4.png)

#### 中等行组

![Parquet none nocdc](/images/posts/0071b3578fa9.png)

![Parquet none cdc](/images/posts/9515d252305a.png)

![Parquet snappy nocdc](/images/posts/124004de076d.png)

![Parquet snappy cdc](/images/posts/c4309b051194.png)

### 7. 使用不同的文件级分片

数据集通常被分割成多个文件以提高并行性和随机访问能力。Parquet CDC 与 Xet 存储层结合，即使数据在不同边界分割，也能高效地对多个文件间的数据进行去重。

让我们用三种不同的文件级分片方式写出数据集，然后比较去重性能：

```python
from pathlib import Path
from de import estimate


def write_dataset(table, base_dir, num_shards, **kwargs):
    """简单的工具函数，将 pyarrow 表写入多个 Parquet 文件。"""
    
    base_dir = Path(base_dir)
    base_dir.mkdir(parents=True, exist_ok=True)
    
    rows_per_file = len(table) / num_shards
    for i in range(num_shards):
        start = i * rows_per_file
        end = min((i + 1) * rows_per_file, len(table))
        shard = table.slice(start, end - start)
        path = base_dir / f"part-{i}.parquet"
        pq.write_table(shard, path, **kwargs)


write_dataset(orca, "orca5-cdc", num_shards=5, use_content_defined_chunking=True)
write_dataset(orca, "orca10-cdc", num_shards=10, use_content_defined_chunking=True)
write_dataset(orca, "orca20-cdc", num_shards=20, use_content_defined_chunking=True)

estimate("orca5-cdc/*.parquet", "orca10-cdc/*.parquet", "orca20-cdc/*.parquet")

```

```
Total size: 9.3 GB
Chunk size: 3.2 GB

```

尽管我们以三种不同的分片配置上传了数据集，但总体上传大小几乎不会超过原始数据集的大小。

### 在 Pandas 中使用 Parquet CDC 功能

到目前为止我们使用了 PyArrow，现在让我们探索在 Pandas 中使用相同的 CDC 功能，通过下载、筛选然后启用内容定义分块功能上传数据集：

```python
import pandas as pd

src = "hf://datasets/teknium/OpenHermes-2.5/openhermes2_5.json"
df = pd.read_json(src)

```

```python
>>> dst = "hf://datasets/kszucs/pq/hermes-2.5-cdc.parquet"
>>> df.to_parquet(dst, use_content_defined_chunking=True)
New Data Upload: 100%|███████████████████████████████████████████████|  799MB /  799MB,  197kB/s  
Total Bytes:  799M
Total Transfer:  799M

```

```python
>>> short_df = df[[len(c) < 10 for c in df.conversations]]
>>> short_dst = "hf://datasets/kszucs/pq/hermes-2.5-cdc-short.parquet"
>>> short_df.to_parquet(short_dst, use_content_defined_chunking=True)
New Data Upload: 100%|███████████████████████████████████████████████| 21.9MB / 21.9MB, 45.4kB/s  
Total Bytes:  801M
Total Transfer:  21.9M

```

```python
import pyarrow as pa
from de import visualize

visualize(
    pa.Table.from_pandas(df), 
    pa.Table.from_pandas(short_df),
    title="Hermes 2.5 Short Conversations",
    with_cdc=True,
    prefix="hermes"
)

```

#### Hermes 2.5 简短对话

![Parquet none nocdc](/images/posts/c3a3127d7b00.png)

![Parquet none cdc](/images/posts/c71c5205e9bc.png)

![Parquet snappy nocdc](/images/posts/c9455c4104ec.png)

![Parquet snappy cdc](/images/posts/b45c440add4a.png)

由于 Parquet CDC 应用于 Parquet 数据页级别（列块级别），去重性能取决于筛选器的选择性，或者更准确地说，是数据集中变更的分布情况。如果大多数数据页都受到影响，那么去重率将显著下降。

## 参考资料

有关此功能的更多详细信息，请参阅：

- Hugging Face 的 Xet 公告
- parquet-dedupe-estimator 的自述文件
- PyArrow 的文档页面
- 实现 Parquet CDC 的拉取请求

我们探讨了新的 Parquet 内容定义分块功能与 Hugging Face 的 Xet 存储层结合使用的性能优势。我们展示了它如何在各种场景下高效地去重数据，使 Parquet 操作更快、存储效率更高。与传统的云存储解决方案相比，结合 Parquet CDC 的 Xet 存储层可以显著减少数据传输时间和成本。

将您的 Hugging Face 存储库从 Git LFS 迁移到 Xet 以享受此优势：https://huggingface.co/join/xet

---

> 本文由AI自动翻译，原文链接：[Parquet Content-Defined Chunking](https://huggingface.co/blog/parquet-cdc)
> 
> 翻译时间：2026-02-27 04:39
