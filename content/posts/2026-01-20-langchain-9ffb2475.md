---
title: 从海量追踪到智能洞察：规模化理解智能体行为
title_original: 'From Traces to Insights: Understanding Agent Behavior at Scale'
date: '2026-01-20'
source: LangChain Blog
source_url: https://blog.langchain.com/from-traces-to-insights-understanding-agent-behavior-at-scale/
author: ''
summary: 本文探讨了智能体（Agent）开发与传统软件开发的本质差异，指出其非确定性、提示词敏感性和无界输入空间等特性使得生产前测试难以覆盖实际行为。作者强调，传统产品分析工具虽能提供指标，却无法解释原因，更难以发现未知模式。为此，LangSmith推出了Insights
  Agent，利用聚类技术自动分析海量非结构化对话记录，帮助开发者发现潜在的使用模式、错误类型，实现基于生产数据的有效迭代。
categories:
- AI基础设施
tags:
- 智能体开发
- 生产环境分析
- LangSmith
- 行为聚类
- LLM运维
draft: false
translated_at: '2026-02-04T04:19:53.920142'
---

- Dev Shah

追踪对于Agent（智能体）开发至关重要——它为评估、调试和标注提供动力。但当你的生产环境中部署的Agent每天生成数千条追踪记录时，人工审查无法扩展。

![](/images/posts/5c42cb0816d9.jpg)

在构建传统软件时，我们有产品分析工具来解决这个问题。你也可以追踪Agent的指标——延迟、错误率、反馈。但这些指标只能暴露问题。要理解其背后的模式，你需要分析非结构化的对话——而非预定义的事件流。传统的产品分析工具并非为此而设计。这正是我们构建LangSmith Insights Agent的原因。

## 为何无法预测Agent行为

构建Agent与构建软件不同。与传统软件相比，存在三个关键差异：

**非确定性。** 当你多次运行软件时，会得到相同的结果。但Agent并非如此。每次调用LLM（大语言模型）都可能产生不同的结果。当你让Agent运行一段时间并连续进行数百次调用时，相同的输入极有可能产生截然不同的路径。

**提示词敏感性。** 软件不会因输入的微小变化而发生巨大改变。它对用户输入的微小变化具有鲁棒性。而LLM具有一种称为“提示词敏感性”的特性。这意味着提示词空间的微小变化可能导致输出结果的巨大差异。

**无界输入空间。** 软件通过用户界面来结构化用户输入。输入空间自然受到界面内容的限制和界定。而Agent接受自然语言，这是无界的。用户可以输入任何内容。

💡 由于Agent具有非确定性且接受无界输入，因此在投入生产之前，你无法预测它们会做什么或用户将如何使用它们。

## 你需要生产环境分析

软件是确定性的，对用户输入的微小变化具有鲁棒性，并且输入空间有限。因此，在发布到生产环境之前，你可以相当确信软件会按预期运行，并有一套测试来验证这一点。你可以通过观察生产环境事件来发现用户行为，但这受到用户在用户界面中可执行操作的限制（这是传统的产品分析）。

Agent具有非确定性，并且对用户输入的微小变化敏感。因此——在投入生产之前，你并不知道Agent实际会做什么。Agent通常还拥有自然语言界面，这赋予了它们完全无界的输入空间。这意味着你可以捕获和分析的用户意图要多得多。

💡 在构建Agent时，你比构建传统软件更需要基于生产数据进行迭代。

**你需要迭代Agent的失败方式。** 对于软件，你可以通过测试在生产前发现正确性问题。也许会有一些边缘情况遗漏。而对于Agent，情况恰恰相反——大多数故障都出现在生产环境中。

**你需要迭代用户实际使用Agent的方式。** 对于软件，用户只能按照你允许的方式使用产品。而对于Agent，自然语言意味着他们可以使用更多样化的方式。

## 传统产品分析对Agent迭代是必要的，但还不够

传统软件生成大量事件（点击、页面浏览、会话）。产品分析工具（如Mixpanel、Amplitude等）通过以下方式解决“数据过多”的问题：

*   将离散事件聚合成指标
*   构建漏斗和群组分析
*   A/B测试

对于Agent，你也可以（并且应该）这样做。我们发现，大多数在生产环境中部署Agent的公司会追踪最终用户反馈、延迟和工具调用情况。

这些指标有助于你了解发生了什么，但无法解释原因。为了理解这些指标变动的原因，你需要分析追踪记录本身。

**在线评估有助于解决已知问题。** 你可以对追踪记录运行评估器，对特定方面进行评分——例如用户挫败感、主题标签、成功标准。但在线评估要求你事先知道要寻找什么。

那么探索性问题呢？例如“用户实际上是如何使用我的Agent的？”或“存在哪些故障模式？”。你无法为尚未发现的模式编写评估器。

那么，Agent分析究竟应该是什么样的？你需要一个能够：

*   分析非结构化对话，而非离散事件
*   发现你未曾想到要寻找的模式
*   大规模地呈现聚类结果

的工具。

这就是**LangSmith Insights Agent**。

## LangSmith Insights Agent

**LangSmith Insights Agent** 使用聚类技术自动发现你追踪记录中的模式。它无需预先定义要寻找什么，而是分析成千上万的对话，并呈现重要的聚类结果——无论是使用模式、错误模式，还是你指定的任何维度。它处理那些原本需要人工审查数百条追踪记录才能回答的探索性问题。

Insights Agent 会寻找模式并生成报告。该报告将基于对运行记录的聚类分析。报告将包含多个不同层级的聚类。会有顶级聚类，然后是更详细的分组作为第二级，其下是单个的运行记录。你可以点击这些层级，以你认为最合适的任何级别探索数据。这让你可以放大查看具体问题，或缩小以观察高级别模式。

![](/images/posts/cb0c3471034e.png)

你可以配置 Insights Agent 来寻找任何你想要的模式。我们最常见用户寻找的两种模式是：

*   用户如何使用我的Agent？
*   我的Agent可能如何失败？

我们为这两种用例提供了预构建的提示词，但如果你想配置它以寻找其他内容，你可以指定任意的提示词。因为每个Agent都不同——你可能关心合规性、语气、准确性或特定领域的故障。

![](/images/posts/e17539504d23.png)

有时，你可能只想对特定的运行记录子集运行 Insights Agent。这让你可以将 Insights Agent 与在线评估或传统产品分析结合使用。例如，你可能只想调查那些获得最终用户负面反馈（例如点踩）的运行记录。你可以通过指定一个经过筛选的运行记录集来运行 Insights Agent。将定量信号（点踩）与定性分析（这些点踩中存在哪些模式）结合起来。

![](/images/posts/e91f7e9abcc0.png)

你还可以根据尚不存在的属性进行筛选。例如：“用户为何感到沮丧？” LangSmith Insights Agent 可以动态计算“用户是否沮丧”，然后基于此进行筛选和聚类。

![](/images/posts/c5de86db0a54.png)

当聚类生成后，它会聚合与这些组中追踪记录相关的属性，以便你快速识别异常聚类。你还可以指定要动态计算的属性（这些属性随后也会包含在聚合统计中）。从而发现你未曾想到要追踪的模式。

![](/images/posts/21b6dceb19ff.png)

## 试用 LangSmith Insights Agent

Agent每天生成数千条非结构化的追踪记录。传统指标告诉你**某些东西**发生了变化，但无法告诉你**是什么**或**为什么**。人工审查无法扩展。

**LangSmith Insights Agent** 使模式发现变得切实可行——自动呈现使用模式和故障模式。它使得基于生产环境Agent数据进行迭代成为可能。

立即在 LangSmith 中试用。

### 加入我们的通讯

获取来自 LangChain 团队和社区的更新

正在处理您的申请...

成功！请检查您的收件箱并点击链接确认订阅。

抱歉，出错了。请重试。

---

> 本文由AI自动翻译，原文链接：[From Traces to Insights: Understanding Agent Behavior at Scale](https://blog.langchain.com/from-traces-to-insights-understanding-agent-behavior-at-scale/)
> 
> 翻译时间：2026-02-04 04:19
