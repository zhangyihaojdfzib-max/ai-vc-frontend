---
title: 利用Llama 2语法规则实现精准信息提取
title_original: Jet-setting with Llama 2 + Grammars – Replicate blog
date: '2023-10-09'
source: Replicate Blog
source_url: https://replicate.com/blog/llama-2-grammars
author: ''
summary: 本文介绍了如何利用Replicate平台上支持语法规则的Llama 2模型，解决大语言模型输出格式不可控的问题。作者以从航班确认邮件中自动提取结构化信息为例，展示了通过定义JSON
  Schema语法规则，强制模型输出符合特定格式的JSON数据，从而可靠地完成信息提取任务。这种方法避免了提示工程或微调的不足，确保了输出结果的句法精确性。
categories:
- AI产品
tags:
- Llama 2
- 语法规则
- 信息提取
- Replicate
- JSON Schema
draft: false
translated_at: '2026-04-16T04:58:44.197839'
---

- Replicate
- 博客

# 与 Llama 2 + 语法规则一同翱翔

- mattt

![](/images/posts/291f023ef6c7.webp)

羊驼天性温顺，但也有固执的一面。如果逼得太紧，它们很可能会吐出些难闻又令人不快的东西。

与现实中的同类一样，要让 Meta 的 Llama 2 完全按照你的意愿行事，可能颇具挑战。这对于某些生成任务来说还好，但对于任何需要句法完美的事情来说就成了问题。提示词工程、少样本示例和微调都有助于将输出塑造成期望的形状。但语法规则是唯一能确保每次都能精确获得所需结果的万全之策。

在这篇文章中，我们将探索一系列内置支持语法规则的 Llama 2 模型，并展示如何将其用于信息提取任务。

上个月，Replicate 在旧金山举办了其首次黑客马拉松。活动很精彩。我与参会者愉快地交流，也和飞过来参加活动的同事们共度了美好时光。看着演示项目，我心想：“我们真的生活在未来。”

但转瞬间，这个美好的幻象就被打破了。你知道是什么把我拉回现实吗？是确认我返回波特兰航班的邮件——你猜怎么着——它没有附带日历事件。真是岂有此理！都 2023 年了，难道还要我……自己读邮件？（或者更糟，接受 Siri 的帮助来自动创建事件？）

```
It's time to check in for your flight.
Use the [REDACTED] app for smooth sailing and we'll see you soon!
Confirmation code:
ABCDEF
CHECK IN
Your trip details

420
Seat  10D
5:00 PM            6:30 PM
SFO                PDX
San Francisco      Portland, OR

Departure
9/20/2023
Arrival
9/20/2023
```

我可不吃这一套。于是我拿起 MacBook，打开偷偷塞进行李的 Stumptown 冷萃咖啡，开始工作。

## 飞行计划

我的灵感来源于 Replicate 联合创始人 Andreas Jansson 在黑客马拉松上演示的项目。他拿了一堆 8 位合成器音乐作品，并使用 Llama.cpp 的语法解码器将输出限制为有效的单行代码。

采用类似的方法，我可以指定一个语法规则，将输出约束为符合给定 JSON 模式的 JSON 文档。

以下是一个用于提取航班详情的简单模式：

```
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "origin": {
      "type": "string",
      "description": "Three-letter ICAO airport code for the departure airport."
    },
    "destination": {
      "type": "string",
      "description": "Three-letter ICAO airport code for the departure airport."
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "departure_time": {
      "type": "string",
      "format": "time"
    },
    "arrival_time": {
      "type": "string",
      "format": "time"
    }
  },
  "required": ["origin", "destination", "date", "departure_time", "arrival_time"],
  "additionalProperties": false
}
```

使用 Replicate 的 JavaScript 客户端库，我运行了 Andreas 的模型，传入了 JSON 模式和我航班确认邮件中的原始文本。

```
import Replicate from "replicate";

const replicate = new Replicate();

const model = "andreasjansson/codellama-34b-instruct-gguf:f1091fa795c142a018268b193c9eea729e0a3f4d55d723df0b69f17b863bf5ea"
const input = {
  prompt: `
    Extract flight information from the following email.
    Use RFC 3339 date time formatting.

    [...]
  `,
  jsonschema: `{...}`,
  max_tokens: 256
};
const output = await replicate.run(model, { input });
```

效果如何？它表现得非常出色。

```
{
  "origin": "SFO"
  "destination": "PDX",
  "date": "2023-09-20",
  "arrival_time": "17:30",
  "departure_time": "15:00",
}
```

为了让模型以 RFC 3339 格式生成日期和时间，确实需要一些引导，但除此之外，整个过程一帆风顺。

我对自己的工作成果感到满意（同时也意识到黑客马拉松几小时前就结束了），于是合上笔记本电脑，上床睡觉，并立刻后悔在几段文字前喝了那么多咖啡 🫨

## 获准起飞

几年前，我迷上了 TripIt 的行程规划工作流。将航班和酒店确认邮件转发到 plans@tripit.com，它们就会自动变成你个人 iCalendar 订阅源中的事件。在 00 年代，这简直堪称魔法。但在 AI 时代，你只需几分钟就能拼凑出一个原型。（说真的，如果我想到了，CodeLlama 本可以替我写那段代码！）

别误会我的意思：LLM 绝非要取代传统的 NLP 工具，比如 SpaCy 或 NLTK（甚至正则表达式！）。懂行的人完全可以轻松超越一个通用 AI 模型。但另一方面，解决问题可能需要比你拥有的更多的时间和专业知识。就我而言，我梦想自己构建 TripIt 导入器已经好几年了，却从未真正让它可靠地工作过。

现在你知道了 Llama 2 和语法规则能实现什么，或许你会受到启发，重新审视一些早已遗忘、在当时看来难以逾越的想法。

## 现在您可以在机舱内自由活动

这篇文章只是带你初步了解能用 Llama 2 做什么。如果你想提升自己的理解，这些资源堪称头等舱级别：

- 探索我们支持语法规则的 Llama 2 模型系列 🧑‍🏫🦙
- 通读将基于语法的采样添加到 llama.cpp 的 PR，以了解实现细节 🤓🦙
- 通过这份 Llama 2 提示词指南成为羊驼语者 🤫🦙
- 通过从数据集微调 Llama 2 来教你的羊驼新技巧 🏋️🦙

---

> 本文由AI自动翻译，原文链接：[Jet-setting with Llama 2 + Grammars – Replicate blog](https://replicate.com/blog/llama-2-grammars)
> 
> 翻译时间：2026-04-16 04:58
