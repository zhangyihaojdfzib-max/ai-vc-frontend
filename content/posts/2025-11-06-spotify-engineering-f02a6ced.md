---
title: Spotify后台AI编码Agent：1500+PR背后的自动化革新
title_original: '1,500+ PRs Later: Spotify’s Journey with Our Background Coding Agent
  (Part 1) | Spotify Engineering'
date: '2025-11-06'
source: Spotify Engineering
source_url: https://engineering.atspotify.com/2025/11/spotifys-background-coding-agent-part-1/
author: Max Charas; Marc Bruggmann
summary: 本文是Spotify工程团队关于后台编码Agent实践系列的第一部分。文章介绍了Spotify如何在其现有的Fleet Management系统（一个用于自动化代码转换的框架）中引入AI编码Agent，以处理更复杂的代码维护任务。传统自动化方法虽能处理简单、重复性工作（如依赖升级），但复杂代码变更门槛高、实现困难。通过构建内部CLI工具，集成AI
  Agent接收自然语言指令，Spotify成功将AI应用于语言现代化、破坏性升级等复杂迁移，已生成并合并超过1500个拉取请求，显著提升了大规模软件维护的自动化水平与效率。
categories:
- AI产品
tags:
- AI编码助手
- 软件工程自动化
- 代码重构
- 开发者工具
- Spotify
draft: false
translated_at: '2026-01-05T16:35:07.386Z'
---

1,500+次PR之后：Spotify与我们的后台编码Agent之旅（第一部分）

这是我们关于Spotify后台编码Agent实践与大规模软件维护未来系列文章的第一部分。另请参阅第二部分和第三部分。

多年来，开发者生产力通过更好的工具得以提升。我们拥有更智能的IDE、更快的构建、更好的测试和更可靠的部署。但即便如此，维护代码库、保持依赖项最新以及确保代码遵循最佳实践，仍然需要大量的人工工作。在Spotify，我们的Fleet Management系统自动化了大部分这类繁琐工作，然而任何中等复杂度的迁移仍然需要大量人力参与。

如果我们能填补这最后的空白呢？

本文探讨了我们如何通过后台编码Agent演进Fleet Management平台，以及超过1,500个已合并的AI生成拉取请求，为我们揭示的大规模软件维护的未来。

**使用Fleet Management自动化繁琐工作**

Fleet Management是一个强大的框架，用于在Spotify所有代码库中应用代码转换。其理念很简单：我们编写能修改源代码的小段代码，并将这些转换应用于数千个软件组件。通过这种方式，我们可以处理整个代码库的维护任务。

总体而言，Fleet Management取得了巨大成功。它使我们自动化了无数小时的繁琐工作，大大缩短了在全代码库范围内实施变更所需的时间。该系统通过在容器化环境中运行这些源码到源码的转换作业来工作，然后自动针对目标代码库创建拉取请求。

这种方法在多个领域表现出色，包括：
*   升级构建文件中的依赖项，例如Maven项目对象模型（POM）文件
*   更新配置文件，例如部署清单
*   执行简单的代码重构，例如移除或替换已弃用的方法调用

其影响是显著的，每天都有稳定的自动化拉取请求被合并，使我们的代码库保持一致、最新和安全。自2024年年中以来，Spotify约一半的拉取请求是由该系统自动生成的。

但我们从一开始就知道这种方法有其局限性。虽然它非常适合简单、可重复的任务，但进行复杂的代码更改是我们从未完全解决的挑战。通过操作程序的抽象语法树（AST）或使用正则表达式，以编程方式定义源代码转换，需要高度的专业知识和技能。我们的自动化Maven依赖项更新器就是一个例子。虽然其核心功能——识别pom.xml文件并更新Java依赖项——是直接的，但处理所有边界情况导致转换脚本增长到超过20,000行代码。由于这种复杂性，我们能实现的大多数自动化变更都是简单的。只有少数团队具备所需的专业知识和时间，能在整个代码库上实现更复杂的Fleetshifts。

与此同时，AI工具正变得越来越擅长进行复杂的代码更改。这带来了一个清晰而充满希望的机遇：
👉 我们能否应用AI来降低门槛，释放Fleet Management在更复杂变更方面的潜力？

**引入AI编码Agent**

去年二月，我们开始研究在我们的Fleet Management系统中使用AI Agent。目标是让工程师能够使用自然语言定义和运行全代码库范围的变更。

我们从最需要帮助的环节开始：代码转换本身的定义。我们用了一个从提示词接收指令的Agent，取代了确定性的迁移脚本。所有周围的Fleet Management基础设施——定位代码库、创建拉取请求、获取评审以及合并到生产环境——都保持完全不变。

我们没有直接采用现成的编码Agent，而是决定构建一个小的内部CLI。这个CLI可以将提示词执行委托给Agent，使用本地模型上下文协议（MCP）运行自定义格式化和代码检查任务，使用LLM作为评判者评估差异，将日志上传到Google Cloud Platform（GCP），并在MLflow中捕获跟踪信息。至关重要的是，拥有这个CLI使我们能够在不同的Agent和LLM之间无缝切换。在快速发展的GenAI环境中，这种灵活和可插拔的方式已经让我们多次更换组件，为用户提供了一个预先配置且集成良好的开箱即用工具，而无需他们了解底层细节。

我们在内部立即看到了对此类产品的需求。我们与早期采用者共同开发工具，他们将其应用于正在进行的迁移中。迄今为止，我们的Agent已生成了超过1,500个拉取请求，这些请求已被Spotify各团队合并到我们的生产代码库中。而且这些并非微不足道的变更——我们现在开始处理诸如以下类型的变更：
*   语言现代化，例如用记录（records）替换Java值类型
*   包含破坏性变更的升级，例如将数据管道迁移到Scio的最新版本
*   UI组件之间的迁移，例如迁移到Backstage中的新前端系统
*   配置更改，例如更新YAML或JSON文件中的参数，同时仍遵循模式和格式

对于这些迁移，与传统的纯手工编写代码相比，我们看到了总计60-90%的时间节省。随着我们将其扩展到越来越多的代码库，自动化变更的投资回报率也在持续增加。我们认为这是一个非常有希望的开端，并且我们坚信，在迁移领域利用AI的可能性方面，我们仅仅触及了表面。

**超越迁移**

如果你也能从IDE或聊天工具触发同一个Agent，给它一个任务，然后去吃午饭呢？随着时间的推移，我们清楚地认识到，不仅迁移需要后台编码Agent，临时性任务也同样需要。

通过MCP公开我们的后台编码Agent，Spotify员工现在可以从Slack和GitHub Enterprise启动编码Agent任务。他们首先与一个交互式Agent对话，该Agent帮助收集有关当前任务的信息。这次交互会产生一个提示词，然后传递给编码Agent，由它生成一个拉取请求。

我们很快为这个功能找到了热情的用户，例如，工程师可以将其作为一种轻量级方式，从Slack的讨论线程中捕获架构决策记录（ADRs），或者产品经理现在可以提出简单的变更建议，而无需在笔记本电脑上克隆和构建代码库。

我们看到迁移用例和后台Agent用例之间产生了良好的协同效应。例如，对Agent配置和工具的改进现在适用于所有生成的PR，无论任务来源如何。我们也从标准化中受益，比如在标记提交、管理LLM配额或收集跟踪信息方面。

**后台Agent带来的新挑战**

将AI Agent引入我们的迁移和开发者工作流，对我们来说是一个全新且令人兴奋的领域。我们从第一轮工具迭代中看到了巨大的影响和势头：现在有数百名开发者与我们的Agent互动，并且我们已经合并了超过1500个拉取请求。

但编码Agent带来了一系列有趣的权衡。性能是一个关键的考虑因素，因为Agent可能需要很长时间才能产生结果，并且其输出可能不可预测。这就需要新的验证和质量控制机制。除了性能和可预测性，我们还必须考虑安全性和成本。我们需要强大的防护栏和沙箱来确保Agent按预期运行，同时还要管理大规模运行LLM所带来的巨大计算开销。

我们还没有所有的答案，但解决这些挑战正是我们团队努力的方向。

我们很高兴能分享在探索这一领域过程中的更多发现。敬请关注我们关于高效上下文工程以及利用反馈循环实现更可预测结果的后续文章。

> 本文由AI自动翻译，原文链接：[1,500+ PRs Later: Spotify’s Journey with Our Background Coding Agent (Part 1) | Spotify Engineering](https://engineering.atspotify.com/2025/11/spotifys-background-coding-agent-part-1/)
> 
> 翻译时间：2026-01-05 13:34
