---
title: 通过API运行Snowflake Arctic大语言模型
title_original: Run Snowflake Arctic with an API – Replicate blog
date: '2024-04-23'
source: Replicate Blog
source_url: https://replicate.com/blog/run-arctic-with-an-api
author: ''
summary: 本文介绍了如何通过Replicate平台，使用API便捷地运行Snowflake最新开源的大语言模型Arctic。文章指出，Arctic拥有4800亿参数，在多项指标上媲美或优于Llama
  3 8B和Llama 2 70B，且擅长SQL与编码任务。内容详细演示了在Replicate的API游乐场体验模型，并提供了使用JavaScript、Python及cURL三种方式调用该模型API的具体代码示例和步骤，帮助开发者快速上手。
categories:
- AI产品
tags:
- Snowflake Arctic
- 大语言模型
- API调用
- Replicate
- 开源模型
draft: false
translated_at: '2026-04-09T04:36:07.808926'
---

-   Replicate
-   博客

# 通过 API 运行 Snowflake Arctic

-   cbh123

Snowflake Arctic 是 Snowflake 推出的一个新型开源大语言模型。在所有指标上，Arctic 的表现与 Llama 3 8B 和 Llama 2 70B 相当或更优，同时使用的训练计算资源不到后者的一半。

它的规模非常庞大。拥有 4800 亿参数的 Arctic 是迄今为止最大的开源模型。正如对 Snowflake 模型的预期，它擅长 SQL 和其他编码任务，并且采用了宽松的 Apache 2.0 许可证。

通过 Replicate，您只需一行代码即可在云端运行 Arctic。

## 在我们的 API 游乐场中尝试 Arctic

在深入使用之前，您可以在我们的 API 游乐场中尝试 Arctic。

尝试调整提示词，看看 Arctic 如何回应。Replicate 上的大多数模型都有这样一个交互式 API 游乐场，可在模型页面上找到：https://replicate.com/snowflake/snowflake-arctic-instruct

API 游乐场是了解模型功能的好方法，并提供多种语言的可复制代码片段，帮助您快速上手。

## 使用 JavaScript 运行 Arctic

您可以使用我们的官方 JavaScript 客户端运行 Arctic：

安装 Replicate 的 Node.js 客户端库

```
npm install replicate
```

设置 REPLICATE_API_TOKEN 环境变量

```
export REPLICATE_API_TOKEN=r8_9wm**********************************
```

（您可以在账户中生成一个 API 令牌。请妥善保管。）

导入并设置客户端

```
import Replicate from "replicate";

const replicate = new Replicate({
  auth: process.env.REPLICATE_API_TOKEN,
});
```

使用 Replicate 的 API 运行 snowflake/snowflake-arctic-instruct。查看模型架构以了解输入和输出的概览。

```
const input = {};

for await (const event of replicate.stream("snowflake/snowflake-arctic-instruct", { input })) {
  process.stdout.write(event.toString());
};
```

要了解更多信息，请查看 Node.js 入门指南。

## 使用 Python 运行 Arctic

您可以使用我们的官方 Python 客户端运行 Arctic：

安装 Replicate 的 Python 客户端库

```
pip install replicate
```

```
export REPLICATE_API_TOKEN=r8_9wm**********************************
```

导入客户端

```
import replicate
```

```
# snowflake/snowflake-arctic-instruct 模型可以在运行时流式输出。
for event in replicate.stream(
    "snowflake/snowflake-arctic-instruct",
    input={},
):
    print(str(event), end="")
```

要了解更多信息，请查看 Python 入门指南。

## 使用 cURL 运行 Arctic

您可以直接使用 cURL 等工具调用 HTTP API：

```
export REPLICATE_API_TOKEN=r8_9wm**********************************
```

```
curl -s -X POST\
  -H "Authorization: Bearer $REPLICATE_API_TOKEN"\
  -H "Content-Type: application/json"\
  -H "Prefer: wait"\
  -d $'{
    "input": {}
  }'\
  https://api.replicate.com/v1/models/snowflake/snowflake-arctic-instruct/predictions
```

要了解更多信息，请查看 Replicate 的 HTTP API 参考文档。

您也可以使用 Replicate 为 Go、Swift 等语言提供的其他客户端库来运行 Arctic。

## Snowflake Arctic 聊天机器人

如果您想找一个起点，Streamlit 已经使用 Replicate 构建了一个演示应用：

![Streamlit 演示](/images/posts/cda6750cf5d6.webp)

在此处试用或在 GitHub 上复刻它。

## 保持同步

-   与 Arctic 聊天。
-   在 Twitter/X 上关注我们，获取 Arctic 的最新动态。
-   加入我们的 Discord 讨论 Arctic。

祝您探索愉快！🦙

---

> 本文由AI自动翻译，原文链接：[Run Snowflake Arctic with an API – Replicate blog](https://replicate.com/blog/run-arctic-with-an-api)
> 
> 翻译时间：2026-04-09 04:36
