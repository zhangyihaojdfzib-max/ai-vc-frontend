---
title: AnyLanguageModel：统一苹果平台本地与远程LLM的API
title_original: 'Introducing AnyLanguageModel: One API for Local and Remote LLMs on
  Apple Platforms'
date: '2025-11-20'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/anylanguagemodel
author: null
summary: 本文介绍了专为苹果平台开发者推出的Swift包AnyLanguageModel，旨在解决集成不同语言模型时面临的复杂性问题。该包提供了一个统一的API，支持包括苹果基础模型、Core
  ML、MLX、llama.cpp、Ollama以及OpenAI、Anthropic等云服务在内的多种模型提供商。通过采用与苹果基础模型框架一致的API设计，开发者只需更换导入语句即可在不同模型间轻松切换，显著降低了实验和集成成本，并鼓励更多地采用本地运行的开源模型。
categories:
- AI基础设施
tags:
- 苹果开发
- 大语言模型
- Swift
- 模型部署
- 开源模型
draft: false
translated_at: '2026-01-06T01:05:10.592Z'
---

推出 AnyLanguageModel：为苹果平台本地与远程 LLM 提供统一 API
LLM 已成为构建软件的重要工具。但对于苹果开发者而言，集成它们仍然存在不必要的困难。

开发者在构建 AI 应用时通常采用混合方案，组合使用以下方式：
*   使用 Core ML 或 MLX 的本地模型，以满足隐私和离线需求
*   使用 OpenAI 或 Anthropic 等云服务提供商，以获得前沿能力
*   将苹果的基础模型作为系统级备用方案

每种方案都有不同的 API、不同的要求和不同的集成模式。这很复杂，而且问题会迅速累积。当我采访开发者关于构建 AI 应用的经验时，模型集成带来的摩擦立即显现。一位开发者直言不讳：

> 我原以为可以快速用演示版做个测试，或者快速做个简易版本，结果却浪费了大量时间。简直让我抓狂。

实验成本很高，这阻碍了开发者去发现本地开源模型可能非常适合他们的用例。

今天我们宣布推出 AnyLanguageModel，这是一个 Swift 包，可作为苹果基础模型框架的即插即用替代品，并支持多种模型提供商。我们的目标是减少在苹果平台上使用 LLM 的摩擦，并让采用本地运行的开源模型变得更加容易。

**解决方案**
核心思想很简单：
**更换你的导入语句，保持相同的 API。**

```
- import FoundationModels
+ import AnyLanguageModel
```

以下是实际应用中的样子。首先使用苹果内置模型：

```
let model = SystemLanguageModel.default
let session = LanguageModelSession(model: model)
let response = try await session.respond(to: "Explain quantum computing in one sentence")
print(response.content)
```

现在尝试通过 MLX 本地运行的开源模型：

```
let model = MLXLanguageModel(modelId: "mlx-community/Qwen3-4B-4bit")
```

AnyLanguageModel 支持一系列提供商：
*   **苹果基础模型**：与苹果系统模型原生集成（macOS 26+ / iOS 26+）
*   **Core ML**：运行经过转换的模型，并利用神经引擎加速
*   **MLX**：在 Apple Silicon 上高效运行量化模型
*   **llama.cpp**：通过 llama.cpp 后端加载 GGUF 模型
*   **Ollama**：通过 Ollama 的 HTTP API 连接本地服务的模型
*   **OpenAI、Anthropic、Google Gemini**：用于比较和备用的云服务提供商
*   **Hugging Face 推理提供商**：由世界级推理提供商支持的数百个云模型。

重点是你可以从 Hugging Face Hub 下载的本地模型。包含云提供商是为了降低入门门槛并提供迁移路径。先让它跑起来，再让它跑得好。

**为何以基础模型作为基础 API**
在设计 AnyLanguageModel 时，我们面临一个选择：创建一个试图涵盖一切的新抽象层，还是基于现有 API 构建。我们选择了后者，使用苹果的基础模型框架作为模板。

这似乎有违直觉。为什么要受限于苹果的选择？原因如下：
1.  **基础模型框架设计精良**。它利用了宏等 Swift 特性，提供了符合人体工学的开发者体验，并且其围绕会话、工具和生成的抽象很好地映射了 LLM 的实际工作方式。
2.  **它有意保持简洁**。基础模型框架代表了语言模型能力的某种最小公分母。我们并不将此视为弱点，而是将其视为一个稳定的基础（嘿嘿）。每个面向苹果平台的 Swift 开发者都会遇到这个 API，因此直接基于它构建意味着更少的概念开销。
3.  **它让我们脚踏实地**。每一层额外的抽象都会让你离实际要解决的问题更远。抽象很强大，但堆叠太多，它们本身就会成为问题。

结果是，在不同提供商之间切换只需要最少的代码更改，并且核心抽象保持清晰和可预测。

**包特性：只包含你需要的部分**
多后端库的一个挑战是依赖膨胀。如果你只想运行 MLX 模型，就不应该拉取 llama.cpp 及其所有依赖项。

AnyLanguageModel 使用 Swift 6.1 的包特性来解决这个问题。你只需选择所需的后端：

```
dependencies: [
    .package(
        url: "https://github.com/mattt/AnyLanguageModel.git",
        from: "0.4.0",
        traits: ["MLX"] // 仅拉取 MLX 依赖项
    )
]
```

可用的特性包括 `CoreML`、`MLX` 和 `Llama`（用于 llama.cpp / llama.swift）。

**默认情况下，不包含任何重量级依赖项。**
你会获得基础 API 加上云提供商支持，后者仅需要标准的 `URLSession` 网络功能。

对于 Xcode 项目（目前尚不支持直接声明特性），你可以创建一个依赖于 AnyLanguageModel 并包含所需特性的小型内部 Swift 包，然后将该包作为本地依赖项添加。README 文件中有详细说明。

**图像支持（及 API 设计权衡）**
视觉语言模型能力强大且应用广泛。它们可以描述图像、从截图中提取文本、分析图表以及回答关于视觉内容的问题。不幸的是，苹果的基础模型框架目前不支持在提示词中发送图像。

基于现有 API 构建意味着接受其限制。苹果可能会在未来的版本（也许是 iOS 27？）中添加图像支持，但视觉语言模型太有用了，等不及。因此，我们已经扩展了基础模型目前提供的功能。

以下是将图像发送给 Claude 的示例：

```
let model = AnthropicLanguageModel(
    apiKey: ProcessInfo.processInfo.environment["ANTHROPIC_API_KEY"]!,
    model: "claude-sonnet-4-5-20250929"
)
let response = try await session.respond(
    to: "What's in this image?",
    image: .init(url: URL(fileURLWithPath: "/path/to/image.png"))
)
```

我们在这里冒了一个经过计算的风险；我们设计的东西可能与苹果最终的实现相冲突。但这就是弃用警告的用途。有时，你必须为尚不存在的框架编写 API。

**试试看：chat-ui-swift**
要查看 AnyLanguageModel 的实际应用，请查看 chat-ui-swift，这是一个展示该库功能的 SwiftUI 聊天应用程序。

该应用包括：
*   通过基础模型集成苹果智能（macOS 26+）
*   用于访问受限模型的 Hugging Face OAuth 认证
*   流式响应
*   聊天持久化

它旨在作为一个起点：复刻它、扩展它、更换不同的模型。看看各个部分如何组合，并根据你的需求进行调整。

**下一步计划**
AnyLanguageModel 目前处于 1.0 版之前。核心 API 是稳定的，但我们正在积极致力于将基础模型框架的全部功能集带到所有适配器中，即：
*   在所有提供商中支持工具调用
*   用于工具和引导的 MCP 集成
*   用于结构化输出的引导生成
*   本地推理的性能优化

这个库是迈向更大目标的第一步。统一的推理 API 为在苹果平台上构建无缝的智能体工作流提供了所需的脚手架——在这些应用中，模型可以使用工具、访问系统资源并完成复杂任务。更多内容即将揭晓。🤫

**参与进来**
我们非常希望得到你的帮助，让它变得更好：
*   **试用** — 构建点什么，试试看
*   **分享你的经验** — 什么好用？什么令人沮丧？我们想听听你在将 AI 集成到应用时面临的挑战
*   **提交问题** — 功能请求、错误报告、疑问
*   **贡献代码** — 欢迎提交 PR

**链接**
我们期待看到你的作品 🦾

> 本文由AI自动翻译，原文链接：[Introducing AnyLanguageModel: One API for Local and Remote LLMs on Apple Platforms](https://huggingface.co/blog/anylanguagemodel)
> 
> 翻译时间：2026-01-06 01:05
