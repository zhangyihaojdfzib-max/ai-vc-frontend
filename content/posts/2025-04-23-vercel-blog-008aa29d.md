---
title: Vercel Functions 支持请求取消，减少资源浪费
title_original: Node.js Vercel Functions now support request cancellation - Vercel
date: '2025-04-23'
source: Vercel Blog
source_url: https://vercel.com/changelog/node-js-vercel-functions-now-support-request-cancellation
author: ''
summary: Vercel 宣布其 Node.js Functions 现在支持请求取消功能，允许开发者在用户导航离开、关闭标签页或停止 AI 聊天等场景下提前终止函数执行。通过监听
  Request.signal.aborted 或 abort 事件，可以避免不必要的计算、Token 生成和数据传输。文章提供了基础用法和 AI SDK 集成示例，强调该功能需在
  vercel.json 中手动启用，有助于优化资源利用和成本控制。
categories:
- AI基础设施
tags:
- Vercel
- 请求取消
- Node.js
- AI SDK
- 资源优化
draft: false
translated_at: '2026-05-24T05:58:14.557737'
---

现在已在路由级别支持请求取消；你需要在 `vercel.json` 中手动启用。了解更多。

使用 Node.js 的 Vercel Functions 现在可以检测到请求何时被取消，并在完成前停止执行。这包括导航离开、关闭标签页，或在 AI 聊天中点击停止以提前终止计算处理等操作。

这可以减少不必要的计算、Token 生成以及发送用户永远不会看到的数据。

你可以通过 `Request.signal.aborted` 或 `abort` 事件来监听取消：

```
1export const GET = async (req: Request) => {2  const abortController = new AbortController();3
4  req.signal.addEventListener("abort", () => {5    console.log("request aborted");6    abortController.abort();7  });8
9  const res = await fetch("https://my-backend-service.example.com", {10    headers: {11      Authorization: `Bearer ${process.env.AUTH_TOKEN}`,12    },13    signal: abortController.signal,14  });15
16  return new Response(res.body, {17    status: res.status,18    headers: res.headers,19  });20};
```

如果你正在使用 AI SDK，请将 `abortSignal` 转发到你的流中：

```
1import { openai } from '@ai-sdk/openai';2import { streamText } from 'ai';3
4export async function POST(req: Request) {5  const { prompt } = await req.json();6  const result = streamText({7    model: openai('gpt-4-turbo'),8    prompt,9    10    abortSignal: req.signal,11  });12  return result.toTextStreamResponse();13}
```

了解更多关于取消 Function 请求的信息。

---

> 本文由AI自动翻译，原文链接：[Node.js Vercel Functions now support request cancellation - Vercel](https://vercel.com/changelog/node-js-vercel-functions-now-support-request-cancellation)
> 
> 翻译时间：2026-05-24 05:58
