---
title: Replicate推出全新搜索API，助力用户精准查找AI模型
title_original: Introducing our new search API – Replicate blog
date: '2025-09-16'
source: Replicate Blog
source_url: https://replicate.com/blog/new-search-api
author: ''
summary: Replicate在其博客宣布推出全新的搜索API，目前处于测试阶段，已通过TypeScript和Python SDK以及MCP服务器向所有用户开放。该API允许用户通过查询关键词搜索匹配的模型、集合和文档页面，并返回包含模型URL、运行次数、描述等详细信息，以及新增加的元数据对象，其中包含更详细的生成描述和标签等属性。文章提供了使用cURL和jq的示例代码，展示了如何搜索特定模型并提取关键数据。
categories:
- AI产品
tags:
- Replicate
- 搜索API
- AI模型
- 开发者工具
- API测试
draft: false
translated_at: '2026-01-15T04:41:34.422966'
---

-   Replicate
-   博客

# 推出全新的搜索 API

-   zeke

我们新增了一个 `search` API，以帮助您找到最佳模型。该 API 目前处于测试阶段，但已通过我们的 TypeScript 和 Python SDK 以及我们的 MCP 服务器向所有用户开放。

以下是一个使用 cURL 的示例：

```
curl -s \
  -H "Authorization: Bearer $REPLICATE_API_TOKEN" \
  "https://api.replicate.com/v1/search?query=lip+sync"
```

以下是通过我们的 MCP 服务器在 Claude Desktop 中使用搜索 API 的视频演示：

您的浏览器不支持 video 标签。

新的搜索 API 会返回与您的查询匹配的模型、集合和文档页面的结果。

```
{
  query: "lip sync",
  models: [
    {model: { url, run_count, etc }, metadata: { tags, score, etc }},
  ],
  collections: [
    {name, slug, description},
  ],
  pages: [
    {name, href},
    {name, href},
  ],
}
```

对于模型结果，API 会返回一个 `model` 对象，其中包含您通常期望的所有数据，如 `url`、`description` 和 `run_count`，但它还会为每个模型返回一个新的 `metadata` 对象，该对象包含诸如更长、更详细的 `generated_description`、`tags` 等属性。

以下是一个使用 cURL 和 `jq` 来获取 Google 的 🍌Nano Banana🍌 模型特定数据的示例：

```
curl -s \
  "https://api.replicate.com/v1/search?query=nano+banana" \
  | jq ".models[0] | {url: .model.url, description: .model.description, run_count: .model.run_count, tags: .metadata.tags, generated_description: .metadata.generated_description}"
```

响应如下：

```
{
  "url": "https://replicate.com/google/nano-banana",
  "description": "Google's latest image editing model in Gemini 2.5",
  "run_count": 5426257,
  "tags": [
    "text-to-image",
    "image-editing",
    "image-to-image"
  ],
  "generated_description": "Generate and edit images from a text prompt and one or more reference images. Combine multiple inputs (multi-image fusion) to integrate objects into new scenes, transfer styles, and perform targeted edits—object removal, background blur, pose changes, and colorization—using natural language. Maintain character, object, and style consistency across a series for branding and storytelling. Follow complex, multi-step instructions for fast, conversational workflows. Outputs a single image with SynthID watermarking."
}
```

新的搜索 API 已在我们远程和本地的 MCP 服务器中得到支持，因此您可以使用您喜爱的工具，如 Claude Desktop、Claude Code、VS Code、Cursor、OpenAI Codex CLI 和 Google 的 Gemini CLI，来发现和探索最佳模型和集合。

我们的 MCP 服务器会进行复杂的 API 响应过滤，以防止您的 LLM 的上下文窗口因大型响应对象而过载。它会根据每个 API 操作的响应模式动态构建 `jq` 过滤查询，并且只返回响应中最相关的部分。以下是它用于搜索 API 的过滤查询示例：

```
{
  `query`: `lip sync`,
  `jq_filter`: `.models[] | {name: .model.name, owner: .model.owner, description: .model.description, run_count: .model.run_count, tags: .metadata.tags}`
}
```

要开始使用 Replicate 的 MCP 服务器，请阅读我们的 MCP 公告博客文章或访问 mcp.replicate.com。

## TypeScript SDK 支持

新的搜索 API 在我们 TypeScript SDK 的最新 alpha 版本中作为 `replicate.search()` 提供。

要使用它，请从 npm 安装最新的 alpha 版本：

```
npm install replicate@alpha
```

然后像这样使用：

```
import Replicate from "replicate"
const replicate = new Replicate()

const query = "lip sync"
const { models } = await replicate.search({ query })

for (const { model, metadata } of models) {
  console.log(model.url)
  console.log(metadata.tags)
  console.log(metadata.generated_description)
  console.log("\n\n--------------------------------\n\n")
}
```

TypeScript SDK 还包含搜索 API 的类型提示，因此您的编辑器可以指导您编写方法签名和处理响应模式：

![TypeScript SDK 类型提示](/images/posts/548e203ce22a.webp)

新的搜索 API 在我们 Python SDK 的最新预发布版本中作为 `replicate.search()` 提供。

要使用它，请从 PyPI 安装最新的预发布版本：

```
pip install --pre replicate
```

然后，您可以像这样使用它：

```
import replicate

query = "lip sync"
results = replicate.search(query=query)

for result in results.models:
  print(result.model)
  print(result.metadata)
  print("\n---------\n")
```

🐍 查看 Google Colab notebook，获取一个可以直接在浏览器中运行的 Python 示例。

您可以在我们的 HTTP API 参考页面 replicate.com/docs/reference/http#search 上找到新搜索 API 的文档。

如果您更喜欢结构化的参考文档，也可以在 OpenAPI 模式 api.replicate.com/openapi.json 中找到文档。

## 向后兼容性

旧的 `QUERY /v1/models` 搜索端点仍然有效并将继续工作，但我们建议使用新的 `GET /v1/search` 端点以获得更好的搜索结果。

您仍然可以直接通过 HTTP API 或使用我们的 Python 或 TypeScript SDK 来使用旧的搜索端点。

旧的搜索端点现在已在我们的 MCP 服务器中禁用，取而代之的是新的搜索端点。

这个新的搜索 API 仍处于测试阶段，因此我们非常希望听到您的反馈。

如果您遇到任何问题或意外的搜索结果，请告诉我们。


> 本文由AI自动翻译，原文链接：[Introducing our new search API – Replicate blog](https://replicate.com/blog/new-search-api)
> 
> 翻译时间：2026-01-15 04:41
