---
title: Replicate推出远程MCP服务器，让AI工具直接调用其API
title_original: Announcing Replicate's remote MCP server – Replicate blog
date: '2025-08-10'
source: Replicate Blog
source_url: https://replicate.com/blog/remote-mcp-server
author: ''
summary: Replicate宣布推出托管式远程MCP服务器，用户可通过Claude、Cursor等AI工具直接访问其完整的HTTP API。该服务基于Anthropic的模型上下文协议标准，支持远程和本地两种部署方式，并利用Cloudflare
  Workers确保API令牌安全存储。新功能还集成了jq响应过滤机制，可智能筛选大型JSON数据，避免占用过多模型上下文窗口。这大大简化了开发者通过自然语言界面调用AI模型的过程。
categories:
- AI产品
tags:
- MCP
- Replicate
- AI工具集成
- API
- Cloudflare
draft: false
translated_at: '2026-01-15T04:41:20.802973'
---

- Replicate
- 博客

# 宣布推出 Replicate 的远程 MCP 服务器

- zeke
- shridharathi

上个月，我们悄然发布了一个用于 Replicate HTTP API 的本地 MCP 服务器。

今天我们宣布推出一个托管的远程 MCP 服务器，你可以将其用于 Claude Desktop、Claude Code、Cursor 和 VS Code 等应用程序，让你能够通过熟悉的基于聊天的自然语言界面，探索和运行 Replicate 的所有 HTTP API。

要开始使用，请访问 👉mcp.replicate.com👈

您的浏览器不支持视频标签。

MCP 代表模型上下文协议。这是 Anthropic 开发的一个标准，旨在让语言模型能够访问外部工具。这通常被称为“工具使用”或“函数调用”。这使得语言模型变得更加强大，因为它们现在可以访问外部工具和数据源，而不仅仅是其自身的内部知识。

安装服务器后，你可以在 Claude 或 Cursor 中提出如下问题：

以下是一个介绍 MCP 并展示如何在 Claude Desktop 中使用 Replicate MCP 服务器的视频：

## 两种形式：远程与本地

我们的官方 MCP 服务器既可作为托管服务使用，也可作为公共 npm 包在本地运行。

*   远程 MCP 服务器（推荐）：这是最简单的选项，推荐给大多数用户。你只需将托管服务器的 URL 添加到 Claude 或 Cursor 等应用程序中。安装服务器后，你将进入一个基于 Web 的身份验证流程，在那里你可以提供一个 Replicate API 密钥，供服务器代表你使用。要开始使用，请访问 mcp.replicate.com
*   本地 MCP 服务器：你可以在本地机器上运行该服务器。只需要一个较新版本的 Node.js。要在本地安装和运行服务器，请查看文档。

## 使用 jq 进行 JSON 响应过滤

一些 HTTP API 可能返回非常大的 JSON 响应，很容易用过多数据填满模型的上下文窗口。例如，Replicate 的搜索 API 返回分页的模型列表，每个模型都包含输入、输出、描述等大量元数据。这些元数据很有用，但对于大多数大语言模型的上下文窗口来说也可能过于庞大。

为了解决这个问题，我们与 Stainless 团队合作，他们正在构建和维护我们的新 API SDK。他们实现了动态过滤较大响应对象、只保留最相关部分的工具。它使用了流行的 jq 命令行工具的 WebAssembly 实现，来编写针对特定响应对象模式和当前任务的临时过滤表达式。

这种方法允许语言模型决定响应的哪些部分与当前任务最相关，并且只返回这些部分。下面是一个例子，仅从搜索响应中提取名称、所有者、描述和运行次数：

```
{
  "body": "upscaler",
  "jq_filter": ".results[] | {name: .name, owner: .owner, description: .description}"
}
```

观看此视频，了解响应过滤的实际效果：

## 使用 Cloudflare Workers 进行安全身份验证

我们新的托管 MCP 服务器运行在 Cloudflare Workers 上，这使得部署和扩展变得容易。Cloudflare 在如何安全构建和扩展 MCP 服务器方面，提供了出色的工具、教程和内容，处于行业领先地位。

我们使用 Cloudflare 的 Workers OAuth 提供者框架来保护你的 Replicate API 令牌安全。当你将 AI 工具连接到 Replicate 时，你会访问一个网页，在那里输入你的 Replicate API 令牌。该令牌存储在 Cloudflare 的 KV 存储中，其功能类似于一个安全的数字保险库，只有你的 MCP 服务器可以访问。这种方法的好处在于，你的令牌永远不会暴露给 AI 工具本身；相反，MCP 服务器充当一个可信的中介，使用你存储的令牌代表你向 Replicate 发出请求。这种设计意味着即使有人访问了你的 AI 工具配置，你的凭据也是安全的，并且由于一切都在 Cloudflare 的基础设施上运行，你可以获得企业级的安全性，而无需复杂操作。

## 开始使用你的工具吧

前往 mcp.replicate.com 开始使用，并告诉我们你构建了什么！我们很高兴看到你利用这些新功能创造出什么。

如果你有任何问题或反馈，请加入我们的 Discord 或在 Twitter/X 上联系我们。

---

> 本文由AI自动翻译，原文链接：[Announcing Replicate's remote MCP server – Replicate blog](https://replicate.com/blog/remote-mcp-server)
> 
> 翻译时间：2026-01-15 04:41
