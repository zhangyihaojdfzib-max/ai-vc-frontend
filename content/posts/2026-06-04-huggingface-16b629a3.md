---
title: Nemotron 3.5：可定制多模态安全模型
title_original: 'Nemotron 3.5 Content Safety: Customizable Multimodal Safety for Global
  Enterprise AI'
date: '2026-06-04'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/nvidia/nemotron-3-5-content-safety
author: ''
summary: NVIDIA发布Nemotron 3.5内容安全模型，该模型基于Gemma 3 4B构建，统一处理多模态输入（文本、图像）、支持12种语言及140种语言的零样本泛化，并引入自定义策略执行与可审计推理轨迹（THINK模式）。模型可在一次推理调用中完成安全判定，同时发布配套安全数据集，适用于医疗、金融、教育等不同行业的企业AI安全需求。
categories:
- AI产品
tags:
- NVIDIA
- 内容安全
- 多模态
- 企业AI
- 模型微调
draft: false
translated_at: '2026-06-05T06:20:39.223027'
---

# Nemotron 3.5 内容安全：面向全球企业AI的可定制多模态安全

过去两年，NVIDIA的内容安全栈从一个专注于英语文本的分类器发展成为一个专业模型系列——每个模型都将覆盖范围扩展到新的模态、语言和推理模式。2026年3月发布的Nemotron 3内容安全首次在一个4B参数模型中结合了多模态和多语言能力。今天，我们发布Nemotron 3.5内容安全，它完成了这一演进：一个统一的模型，将多模态输入、多语言覆盖、自定义企业策略执行和可审计推理整合到一次推理调用中。

本文将介绍3.5版本的变化、每项新功能背后的设计决策，以及如何将该模型集成到生产安全流程中。

## Nemotron 3.5 内容安全的新特性

### 1. 统一多模态评估

Nemotron 3引入了图像理解能力；Nemotron 3.5深化了多模态集成。该模型将用户提示词、可选图像和可选助手响应作为一个单一的上下文窗口进行处理，并对组合输入产生一致的安全判定。将三者一起评估——而非各自独立评分——弥补了多模态安全场景中一个众所周知的缺陷：仅从文本与图像之间或请求与响应之间的交互中产生的策略违规，现在可以在一次处理中被捕获。

### 2. 全球语言覆盖

Nemotron 3.5保持了其前代产品明确训练的12种语言覆盖——英语、法语、西班牙语、德语、中文、日语、韩语、阿拉伯语、印地语、俄语、葡萄牙语和意大利语——同时继承了Gemma 3基础模型在大约140种语言上的强大零样本泛化能力。这意味着在训练数据稀缺的市场（例如东南亚语言、斯堪的纳维亚语言、资源较少的非洲语言）中部署时，可以受益于基础模型的多语言迁移，而无需单独的微调。

### 3. 自定义策略执行

这是3.5版本相对于Nemotron 3最重要的架构新增功能。生产部署很少在单一通用安全分类体系下运行。医疗健康平台与金融服务聊天机器人、开发者工具IDE或儿童教育应用具有不同的风险特征。Nemotron 3.5接受与输入一起提供的自定义策略规范。该模型在做出判定时会根据该策略进行推理，而非完全依赖内置分类体系。这扩展了最初在Nemotron Content Safety Reasoning 4B中引入的功能，使其适用于完整的多模态、多语言场景。

### 4. 推理轨迹（THINK模式）

Nemotron 3.5中的每个安全判定都可以通过可选的think模式附带可审计的推理轨迹。启用后，模型在输出最终的safe/unsafe标签以及可选的违规类别之前，会输出其逐步推理过程。

```
<think>
用户提示词要求获取关于无需处方获取受控物质的指导。
助手响应提供了具体的获取步骤并引用了一个在线市场。
此交互违反了犯罪计划/自白和受控物质类别。
图像（药房外观）提供了位置背景信息，但不改变判定结果。
</think>

用户安全：不安全
响应安全：不安全
安全类别：犯罪计划/自白，受控物质

```

当延迟是主要约束时，可以禁用THINK模式，以恢复到与Nemotron 3相同的低延迟二元判定。

### 5. 安全数据集

随着Nemotron 3.5的发布，我们同时发布了我们的安全数据集。这是一个重要的里程碑，因为大多数开源安全模型通常不提供训练或评估集。这个问题在多模态领域更为严重，因为图像或视频等素材通常来自具有限制性许可条款的资源。Nemotron 3.5内容安全数据集是多模态、多语言的，并包含用于训练模型的安全推理轨迹。这些推理轨迹通过两步方式生成，使其简洁，类似于Nemotron Content Safety Reasoning 4B模型。

## 模型架构

Nemotron 3.5内容安全基于Google Gemma 3 4B IT（4B参数）构建，提供128K上下文窗口、强大的视觉语言推理能力和广泛的多语言覆盖。NVIDIA使用LoRA适配器对该基础模型进行微调，该适配器安装了针对性的安全分类行为，同时保持模型足够紧凑，可在8GB以上显存的GPU上进行实时部署。

推理接口支持三种输出模式：

模式1 — 低延迟二元判定：

```
用户安全：安全
响应安全：不安全

```

模式2 — 带类别的二元判定：

```
用户安全：安全
响应安全：不安全
安全类别：暴力，犯罪计划/自白

```

模式3 — THINK模式（推理 + 判定）：

```
<think>
[逐步推理轨迹]
</think>

用户安全：不安全
响应安全：不安全
安全类别：[类别]

```

安全分类体系遵循Aegis 2.0框架：13个与MLCommons安全分类体系对齐的核心类别，外加10个细粒度子类别。这种对齐允许与在Aegis分类数据集上基准测试的其他开源和闭源防护系统进行直接比较。

## 推理

推理是内容安全分类的加速器，因为它提供了生产AI系统（尤其是在企业和受监管环境中）所需的上下文、定制化和可问责性。

### 支持自定义和上下文策略执行

推理允许内容安全模型在推理时动态解释和执行以自然语言定义的、特定领域的自定义策略。这是必要的，因为生产部署很少在单一通用安全分类体系下运行。金融服务聊天机器人与儿童教育应用具有不同的风险特征，后者可能对脏话的容忍度更低。此能力支持：

- **类别抑制**：禁用不相关的类别，例如当DevOps工具处理"终止进程"这一短语时，防止触发"暴力"类别。
- **自定义类别注入**：定义特定于组织监管或产品策略的专有风险类别。

### 提供可审计和文档化的理由说明

推理轨迹展示了模型在给出最终安全或不安全判定之前的逐步逻辑。这种文档化的理由说明具有多种用途：

- **合规与审计日志**：受监管行业通常需要对内容审核决策提供文档化的理由说明。
- **人工审核**：审核人员可以审计得出判定的原因，以识别系统性模型错误。
- **策略迭代**：轨迹揭示了模型如何解释边缘情况，使团队能够迭代优化和改进自定义策略语言。

### 延迟

虽然推理可能引入延迟，但Nemotron模型通过将推理链压缩为简洁摘要来限制输出Token并提高效率，从而解决了这一问题。这是通过两步过程完成的，类似于前代模型Nemotron-Content-Safety-Reasoning-4B。第一步，我们使用更大、更强的模型（如Qwen 397B）根据提供的提示词、图像和响应生成思维链推理轨迹。我们还提供了样本的真实标签，以避免任何错误分类进入推理轨迹。第二步，我们使用另一个大型模型（如Qwen 80B）使这些推理轨迹更加简洁。我们特别指示该模型重新表述原始轨迹（来自第一步），使其不超过3个句子。根据我们的实验，大多数生成的推理轨迹都在3个句子以内。

高效推理轨迹优化实现了低延迟的自定义策略执行。此外，推理轨迹提供了宝贵的训练信号，可用于训练专门的审核模型。开发者可以选择双模式运行：在通用任务中禁用推理以实现最低延迟，或在复杂策略中启用推理。

## 训练数据

驱动 Nemotron 3.5 的数据集是 Nemotron 3 所使用的多模态、多语言混合数据的演进版本，并新增了针对推理和自定义策略能力的数据。我们使用了以下数据来源：

- **多语言文本安全数据**，来自 Nemotron Safety Guard Dataset v3，从具有文化细微差别的子集中采样，并在安全类别和安全/不安全划分中按比例分布。
- **人工标注的多模态数据**，由 NVIDIA 以英语收集，并翻译成 12 种语言。关键的是，99% 的训练图像是真实照片——而非合成生成。这直接解决了多模态安全基准测试领域的一个已知弱点，即现有的 VLGuard 和 MM-SafetyBench 等数据集严重依赖 SDXL 生成的图像，这些图像缺乏生产内容的纹理和文化复杂性。尽管由于许可限制，并非所有这些真实图像都能发布，但我们仍然能够发布来自 Wikimedia 的图像子集以及合成生成的数据。
- **安全多模态数据**，来自 Nemotron VLM Dataset v2，涵盖扫描文档、图表、论文和示意图及其相关查询——确保模型不会过度标记良性的专业内容。
- **推理轨迹**，源自较大教师模型（Qwen 397B）的思维链输出，然后使用 Qwen 80B 进行精简，用于教导模型如何进行推理。
- **主题跟随数据**，来自 CantTalkAboutThis 数据集，包含一系列企业部署场景（医疗、金融、银行、教育等）中的策略规范/判定对。
- **合成数据**，约占总体训练数据量的 10%，主要用于多样化越狱模式、生成罕见的策略违规示例以及产生多模态对抗性案例。

## 基准测试

Nemotron 3.5 Content Safety 在多项多语言、多模态和自定义策略安全基准测试中进行了评估，包括 VLGuard、MM-SafetyBench、PolyGuard、RTP-LX、Aya Redteaming、XSafety、MultiJail、Aegis、Dynaguardrail 和 CoSA。这些评估反映了企业安全在生产中的核心挑战：在全球语言、文本和图像输入以及特定领域策略中应用一致的护栏，同时不增加显著延迟。

Nemotron 3 在多模态有害内容测试中取得了 84% 的平均准确率，延迟约为 LlamaGuard-4-12B 的一半，奠定了坚实的基线。Nemotron 3.5 保持了紧凑的 4B 效率，同时增加了自定义策略支持和推理轨迹。

在多语言和多模态安全基准测试中，Nemotron 3.5 在保持紧凑规模的同时，提供了强大的有害内容分类准确率。这一点很重要，因为许多安全模型仍然是英语优先、纯文本或成本过高而无法在生产流程中重复运行。Nemotron 3.5 旨在将多语言覆盖、多模态分类、自定义策略支持和低延迟部署整合到一个模型中。

![figure1](/images/posts/53dffc4a3282.png)

图 1. Nemotron 3.5 Content Safety 在多语言和多模态安全基准测试中提供了强大的有害内容分类准确率，在评估的基准测试集中平均约为 85%。

语言层面的结果突显了多语言安全对全球企业 AI 的重要性。在多语言 Aegis 上，Nemotron 3.5 在 12 种语言中的有害内容分类平均准确率达到 96.5%。在 RTP-LX 上，平均准确率为 88.8%，Aegis 和 RTP-LX 的综合平均值为 92.7%。这种一致性有助于团队在面向客户、员工和合作伙伴的工作流程中应用相同的安全策略，而不是依赖仅限英语的审核或单独的区域安全模型。

图 2. Nemotron 3.5 Content Safety 在多语言 Aegis Cultural + Adapted（提示词分类）（harmful-f1）上，在 12 种语言中的有害内容分类平均准确率达到 97%。

![figure2](/images/posts/ef59cf686718.png)

图 3. Nemotron 3.5 Content Safety 在 RTPLX（提示词分类）（harmful-f1）上，在 12 种语言中的有害内容分类平均准确率达到 89%。

![figure3](/images/posts/02d2a5f25e95.png)

仅靠准确率不足以满足生产环境下的护栏要求。安全模型还必须足够高效，以便在内容被处理、返回或路由到下游之前运行。Nemotron 3.5 Content Safety 紧凑的 4B 设计有助于降低重复安全检查的成本和延迟，使多语言和多模态护栏在实际 AI 应用中变得可行。

在默认（无 THINK）模式下，延迟特性与 Nemotron 3 相同。THINK 模式会增加与推理轨迹长度成比例的推理时间，但这种开销是可预测的，并且可以与同步审核循环分开预算——例如，将 THINK 模式评估作为审计管道的一部分异步运行，而默认模式则处理实时决策。

图 4. 与另一种多模态安全模型相比，Nemotron 3.5 Content Safety 在多模态基准测试中实现了 3 倍的端到端延迟降低。

![figure4](/images/posts/d1cf8d5f079c.png)

与另一种推理安全模型相比，我们的模型在启用推理时生成的 Token 最多减少 50%，从而在成本和延迟方面更加高效。

## 解决基准测试差距

多模态安全研究中一个反复出现的主题是现有评估基础设施的差距。Nemotron 3.5 的开发遇到了更广泛文献中记载的相同差距：

- **纯文本覆盖**：最广泛引用的安全基准测试（WildGuard、XSTest、HarmBench）都是纯文本的。多模态性能无法从文本基准测试结果中推断出来。
- **合成图像质量**：现有的大多数多模态基准测试使用 AI 生成的图像（通常是 SDXL）而非真实照片，低估了真实生产内容的难度。
- **真实图像许可**：图库照片许可证禁止在 AI 数据集中重新分发，这在研究条件和生产条件之间造成了结构性差距。

NVIDIA 的多模态训练数据——包含真实图像和具有文化细微差别的多语言提示词——旨在填补模型训练方面的部分差距。评估方面的基准测试差距对于更广泛的安全研究社区来说仍然是一个开放性问题。

## 快速开始

Nemotron 3.5 Content Safety 可在 Hugging Face 上获取，采用 NVIDIA 开放模型许可证，可用于研究和商业用途，同时提供训练数据集。它支持 transformers、vLLM 和 SGLang，并可作为生产级 NVIDIA NIM 在 build.nvidia.com 上获取，适用于需要预打包、GPU 优化推理微服务的团队。

开发者还可以通过推理平台访问该模型，包括 Baseten、Eigen AI、DeepInfra、OpenRouter 和 Vultr。

对于自定义策略工作流程，NVIDIA 提供了一个兼容 Claude 和 Codex 的技能用于生成自定义策略，以及展示如何使用该模型的指南。自定义策略和推理轨迹有助于团队根据特定领域规则调整安全行为，同时保持决策的可审计性。

---

> 本文由AI自动翻译，原文链接：[Nemotron 3.5 Content Safety: Customizable Multimodal Safety for Global Enterprise AI](https://huggingface.co/blog/nvidia/nemotron-3-5-content-safety)
> 
> 翻译时间：2026-06-05 06:20
