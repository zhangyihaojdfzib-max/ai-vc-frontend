---
title: AI智能体开发的务实指南
title_original: The no-nonsense approach to AI agent development
date: '2025-06-04'
source: Vercel Blog
source_url: https://vercel.com/blog/the-no-nonsense-approach-to-ai-agent-development
author: ''
summary: 本文提出了一种构建AI Agent（智能体）的务实方法，强调智能体应范围狭窄、界限清晰且特定于领域。开发过程分为两步：首先手动模拟智能体，用真实输入和提示词引导LLM完成任务，识别可自动化的重复环节；然后编写代码自动化循环，构建智能体骨架，包括输入收集、状态机建模、确定性计算与LLM调用的结合。文章指出，LLM的非确定性决定了能用普通函数处理的步骤应避免使用模型，并强调缩小任务范围比依赖更强模型更有效。
categories:
- AI研究
tags:
- AI Agent
- 智能体开发
- 自动化
- LLM
- 提示工程
draft: false
translated_at: '2026-05-07T05:32:37.527845'
---

AI Agent（智能体）是接管由手动、多步骤流程组成的任务的软件系统。这些任务通常需要上下文、判断和适应能力，因此难以用简单的基于规则的代码实现自动化。

虽然传统的自动化是可行的，但这通常意味着要硬编码无数个边缘情况。Agent（智能体）提供了一种更灵活的方法。它们利用上下文来决定下一步做什么，减少了繁琐步骤的手动工作，同时为重要决策保留了审查流程。

最高效的AI Agent（智能体）是范围狭窄、界限清晰且特定于领域的。

以下是构建一个AI Agent（智能体）的方法。

## 链接到标题步骤1：手动构建Agent（智能体）原型

在编写任何代码之前，先手动模拟Agent（智能体）。

像人类处理任务一样，一步步地理解任务。使用真实的输入，无论是截图、API响应还是杂乱的CSV数据。手动将这些输入喂给LLM，并使用模拟实际工作流程的提示词来引导其完成整个过程。

在此过程中，亲自执行所有产生的操作。注意哪些部分感觉重复或机械。这些是后期自动化的有力候选对象。

预计早期结果会比较粗糙。当我们在2023年开始构建`v0.devin`时，LLM难以生成可靠的HTML。突破点并非来自更好的模型，而是来自缩小范围并使用带有Tailwind的结构化输入。

如果模型即使经过调整也始终无法取得进展，那么该任务可能不适合Agent（智能体）。但如果它有效，或者接近有效，那么就值得继续。

## 链接到标题步骤2：自动化循环

一旦你的手动模拟表明该任务是可行的，就开始编写代码。这就是构建Agent（智能体）骨架的地方。

首先，自动化它收集输入的方式。使用API、爬虫、截图或任何适合该问题的方法，然后将Agent（智能体）的过程建模为一个循环或一个简单的状态机。收集输入，尽可能执行确定性计算，在需要逻辑或判断时调用模型，评估结果，并决定是继续还是退出。

LLM是非确定性的。如果你能编写一个普通函数来处理某个步骤，那就去做。使用普通代码进行解析、计算或排序。任何不需要推理的事情。

```
1import { streamText, tool } from "ai";2import { anthropic } from "@ai-sdk/anthropic";3import { z } from "zod";4
5async function stockAnalysisAgent(6  symbol: string,7  startDate: string,8  endDate: string9) {10  return streamText({11    model: anthropic("claude-4-sonnet-20250514"),12    system: `You are a professional stock analyst AI powered by Claude Sonnet. You MUST perform a comprehensive analysis using BOTH available tools.13
14ANALYSIS STRUCTURE:15- Technical Analysis: Use price data, P/E ratio, beta, 52-week range, volume patterns16- Fundamental Analysis: Use news sentiment, earnings data, market cap, industry trends17- Investment Recommendation: Clear BUY/SELL/HOLD with specific price targets and reasoning18
19IMPORTANT: You MUST use both tools before providing any analysis. Do not wait or ask - call both tools immediately and concurrently.20
21NOTE: The news search uses Finnhub as the primary source (more reliable for stock news) with NewsAPI as fallback.`,22    prompt: `Analyze ${symbol.toUpperCase()} stock comprehensively. Use BOTH the getStockData and searchNews tools to gather complete market data, then provide detailed technical and fundamental analysis with a clear investment recommendation.`,23    tools: {24      getStockData: tool({25        description:26          "Get real-time stock price, volume, financial metrics, and historical data for a given symbol using Finnhub API",27        parameters: z.object({28          symbol: z.string().describe("Stock symbol (e.g., NVDA, AAPL, TSLA)"),29        }),30        execute: async ({ symbol }) => {31          return getStockData(symbol, startDate, endDate);32        },33      }),34      searchNews: tool({35        description:36          "Search for recent news and analysis about a stock or company using Finnhub (primary) and NewsAPI (fallback)",37        parameters: z.object({38          query: z39            .string()40            .describe(41              'Search query for news (e.g., "NVIDIA", "Apple earnings", "Tesla Model 3")'42            ),43        }),44        execute: async ({ query }) => {45          return searchNews(query, startDate, endDate);46        },47      }),48    },49    maxSteps: 5,50  });51}52

```

股票分析Agent（智能体）示例

不要害怕使用普通的编程。将LLM保留给真正需要判断的部分。

构建AI Agent（智能体）可能看起来像是一件需要新抽象的新事物，但它只是常规编程。使用`if`语句、循环或开关，什么合适就用什么。不要过度思考结构。对于常见模式，请参阅我们的AI SDK Agent（智能体）文档。

当Agent（智能体）能够在很少或无需干预的情况下可靠地产生可接受的结果时，它就“工作”了。

## 链接到标题步骤3：优化可靠性

现在Agent（智能体）已经端到端运行，将重点转移到提高质量上。这意味着要让Agent（智能体）在特定示例和广泛的真实世界输入上都能表现良好。

首先收紧循环。优化你的提示词。使工具调用更精确。移除不必要的重试。尽可能用普通的确定性函数替换模型调用。如果Agent（智能体）产生不完整的结果，使用第二个模型来批评第一个模型的结果并继续工作。

在这个阶段，直觉和动手测试是你最好的工具。不断迭代，直到Agent（智能体）在针对真实示例运行时感觉稳固。一旦核心循环稳定，引入结构化评估来测试跨广泛输入和边缘情况的性能。这有助于及早发现回归问题，并确保随着Agent（智能体）的发展，质量得以保持。

## 链接到标题要点

AI Agent（智能体）扩展了软件的能力，但构建一个Agent（智能体）并不需要重新思考你对编程的看法。最可靠的Agent（智能体）是建立在相同的基础上的：清晰的逻辑、良好的结构和紧密的反馈循环。

在投入任何实现之前，使用真实输入和提示词手动测试任务。如果模型在帮助下几乎可以完成任务，那么它可能值得构建。从那里开始，使用普通代码构建逻辑，仅在必要时依赖模型。一旦Agent（智能体）工作，专注于使其具有弹性。无论是在单个输入上还是在跨真实世界的复杂性上。

总结一下，Agent（智能体）在以下情况下很有用：

-   任务难以用传统代码自动化
-   手动提示模型显示出成功的迹象
-   你将Agent（智能体）的范围限定得很窄，并建立在扎实的软件实践之上
-   你同时通过动手直觉和结构化评估进行优化

任务难以用传统代码自动化

手动提示模型显示出成功的迹象

你将Agent（智能体）的范围限定得很窄，并建立在扎实的软件实践之上

你同时通过动手直觉和结构化评估进行优化

如果构建得当，AI Agent（智能体）感觉就像魔法。但背后没有巫术。只是内置了推理能力的智能系统。

准备好构建了吗？

使用AI SDK开始构建可靠、范围明确的AI Agent（智能体），采用真实的软件模式。

尝试AI SDK

---

> 本文由AI自动翻译，原文链接：[The no-nonsense approach to AI agent development](https://vercel.com/blog/the-no-nonsense-approach-to-ai-agent-development)
> 
> 翻译时间：2026-05-07 05:32
