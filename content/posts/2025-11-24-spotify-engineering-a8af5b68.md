---
title: 背景编程智能体：上下文工程实践与提示词设计
title_original: 'Background Coding Agents: Context Engineering (Part 2) | Spotify
  Engineering'
date: '2025-11-24'
source: Spotify Engineering
source_url: https://engineering.atspotify.com/2025/11/context-engineering-background-coding-agents-part-2/
author: Max Charas; Marc Bruggmann
summary: 本文是Spotify工程团队关于背景编程智能体系列的第二部分，重点探讨如何通过有效的上下文工程指导智能体完成代码迁移任务。文章对比了早期开源智能体与自研智能体循环的局限性，并介绍了转向Claude
  Code后的改进：它支持更自然的任务描述，能动态管理复杂操作，显著提升了生成可靠、可合并PR的能力。文章还总结了提示词设计的经验教训，强调需根据智能体特性调整提示策略，避免过于泛泛或过于具体，并说明了前提条件的重要性。
categories:
- 工程实践
tags:
- 编程智能体
- 上下文工程
- 代码迁移
- Claude Code
- 提示词设计
draft: false
translated_at: '2026-01-04T23:56:03.580Z'
---

**背景编程智能体：上下文工程（第二部分）**

这是我们关于Spotify在背景编程智能体领域的探索之旅以及大规模软件维护未来系列文章的第二部分。另请参阅第一部分和第三部分。

在第一部分中，我们介绍了Spotify如何使用背景编程智能体来扩展其Fleet管理系统。该智能体能够编辑代码、运行构建和测试，并自动创建拉取请求。一旦我们的智能体开始运行，一个新的挑战随之出现：

👉 **我们如何告诉背景编程智能体该做什么？**

在本文中，我们将探讨为编程智能体进行上下文工程的技巧。一个好的迁移提示词是什么样的？智能体应该能够访问哪些工具？我们发现，精心设计的上下文工程对于在现实世界的代码库中生成可靠、可合并的拉取请求至关重要。

**早期的开源智能体**

当我们刚开始时，我们尝试了诸如Goose和Aider等开源智能体。初次使用这些工具时，仅凭一个提示词它们就能完成许多工作——探索磁盘上的代码库、识别需要修改的内容、编辑代码等等，这令人惊叹。

但是，当我们试图将这些智能体扩展到我们的迁移用例时，很快就遇到了问题。很难让它们可靠地生成可合并的PR。事实证明，如果你想将更改应用到数千个代码库中，编写好的提示词——并验证智能体是否做了正确的事情——会变得异常困难。

**构建我们自己的智能体循环**

为了追求更具可预测性的智能体，我们在LLM API之上构建了自己的“智能体循环”。每个任务由三部分组成：

1.  用户提供一个提示词和范围内所有文件的列表。
2.  智能体根据提示词进行几轮文件编辑，每轮都结合构建系统的反馈。
3.  一旦测试通过或达到限制（每会话10轮，总共可重试3次会话），任务即告完成。

这对于小的更改效果很好，例如编辑部署清单、替换配置标志或更改单行代码。但对于除此之外的任何事情，它很快就变得难以控制。

首先，用户很难使用这个智能体。用户必须通过编写git-grep命令来精确选择他们想要加载到上下文窗口中的文件。选择一个过于宽泛的搜索模式（如 `git grep -l '*.java'`）很容易超出LLM的上下文窗口容量。相反，选择一个过于有限的搜索模式则会使智能体因缺乏足够上下文而无法解决问题。

此外，智能体在处理更复杂的多文件更改时也遇到困难。例如，我们经常需要级联更改，比如更新一个文件中的公共方法，然后调整所有调用点。对于这种用例，我们的循环经常用完轮次。当上下文窗口填满时，它也容易迷失方向，几轮之后就忘记了原始任务。

**引入Claude Code**

我们自研的智能体循环面临关键挑战——它需要过于僵化的指令，并且在处理复杂的多步骤编辑时力不从心。我们需要一个能够理解高层次目标、动态管理任务，并为那些只想描述结果而非规定每一步的用户减少摩擦的智能体。同时，我们也希望避免在处理较长或更复杂的操作时遇到上下文窗口限制。

为了解决这个问题，我们转向了Claude Code。它允许我们使用更自然的、面向任务的提示词，并且我们受益于其内置的高效管理待办事项列表和生成子智能体的能力。

截至目前，Claude Code是我们表现最佳的智能体，我们已经将其应用于大约50次迁移，并且大部分由背景智能体创建的、合并到生产环境的PR都出自它手。

> “Spotify团队在这里构建的系统非常出色——不仅在于成果，更在于他们实现的方式。Spotify构建了一个大规模系统，使用Claude Agent SDK在数百个代码库中合并了数千个PR。他们对提示词工程和上下文管理的深刻理解，代表了先进工程组织在思考自主编码方面的前沿水平。我们构建Claude Code和SDK正是为了支持这种创新，看到它如此大规模地驱动他们的背景智能体，令人无比兴奋。”

**关键在于提示词**

这里有一个令人谦卑的事实：编写提示词很难，而且大多数人在这方面经验不足。如果你不深入了解如何向你的智能体发出提示，看似简单的指令可能会导致令人啼笑皆非的错误结果。在某种程度上，为LLM编写提示词就像写一份关于如何制作花生酱果冻三明治的清晰说明。

通过让Spotify员工在没有太多培训或指导的情况下使用我们的背景编程智能体，我们开始看到两种主要的反模式：

1.  **过于泛泛的提示词**：期望智能体能够心灵感应般地猜测意图和期望结果。
2.  **过于具体的提示词**：试图涵盖每一种情况，但一旦智能体遇到意外情况就会崩溃。

但我们也看到一些团队花费了大量时间学习Claude Code以及如何最好地提示它。以下是我们来之不易的一些经验教训：

*   **根据智能体定制提示词**：我们自研的智能体最适合严格的、逐步的提示词。Claude Code则更适合描述最终状态并留有空间让其自行找出实现方法的提示词。
*   **说明前提条件**：智能体非常渴望根据你的提示采取行动，甚至到了犯错的地步。如果在迁移场景中跨代码库重用提示词，这会成为问题：智能体可能会得到一个无法完成的任务——例如，目标代码库的语言级别不支持的操作。在提示词中明确说明何时不应采取行动会很有帮助。
*   **使用示例**：提供一些具体的代码示例会极大地影响结果。
*   **定义期望的最终状态**，最好以测试的形式。“让这段代码更好”不是一个好的提示词。智能体需要一个可验证的目标，以便在过程中迭代解决方案。
*   **一次只做一个更改**：用户可能为了方便，将几个相关的更改合并到一个复杂的提示词中，但这更可能让智能体陷入困境，因为它会耗尽上下文窗口或只能交付部分结果。
*   **向智能体征求对提示词的反馈**：在一个会话结束后，智能体本身出人意料地处于一个很好的位置，可以告诉你提示词中缺少了什么。利用这些反馈来改进未来的提示词。

我们运用这些原则来构建和运行复杂的迁移提示词。为了让您有个概念，以下是我们用于从AutoValue迁移到Java记录的一个示例提示词：

```
（此处应插入示例提示词，但原文未提供具体内容）
```

**合适的工具**

从上面的示例可以看出，我们的提示词可能会变得相当复杂。这是因为我们更倾向于使用较大的静态提示词，这样更容易推理。你可以对提示词进行版本控制、编写测试并评估其性能。这提高了智能体的整体可预测性。

另一种方法是从一个更简单的提示词开始，但连接到模型上下文协议工具，允许智能体在处理问题时动态获取更多上下文。虽然这确实使智能体能够处理更复杂和模糊的任务，但也使其可测试性和可预测性降低。你拥有的工具越多，引入的不可预测性维度就越多。

我们在工具和钩子方面对背景编程智能体进行了非常严格的限制，以便它能专注于根据提示词生成正确的代码更改。这限制了智能体上下文中的信息量，并消除了不可预测故障的来源。

目前，我们为智能体提供了以下访问权限：

*   一个“验证”工具，用于运行格式化器、代码检查器和测试。我们发现，将如何调用我们内部构建系统的逻辑编码在MCP中（而不是依赖AGENTS.md风格的文件）更容易，因为我们的智能体需要在数千个具有完全不同构建配置的现有代码库上运行。

这也使我们能够通过将日志总结为更易于智能体理解的形式，来减少工具输出中的噪声。

一个提供有限且标准化Git访问权限的Git工具。这使我们能够有选择地暴露git子命令（例如，永不推送或更改远程仓库），并标准化其他操作（例如，设置提交者信息和使用标准化的提交信息格式）。

内置的Bash工具，具有严格的命令许可清单。访问少数Bash命令（如ripgrep）非常方便。

值得注意的是，我们目前没有向智能体开放代码搜索或文档工具。相反，我们要求用户预先将相关上下文浓缩到提示词中，方法可以是在将任务移交给智能体之前，将所有相关信息包含在提示词中，或者通过独立的工作流智能体从各种内部和外部来源为编码智能体生成提示词。

我们还建议尽可能通过代码本身引导智能体，例如，在目标代码库中设置测试、代码检查工具或API文档。这将适用于未来所有操作此代码的提示词（及所有智能体）。

**结论**

我们持续探索背景编码智能体的前沿能力。我们在Claude Code方面的工作展现出巨大潜力，并随着我们学会如何指导智能体及构建有效工具而实现了重大进展。将任务分解为合适的模块、编写优质提示词以及更好地将工具与我们的基础设施集成，对于生成整洁的拉取请求至关重要。

但在实践中，我们仍主要依靠直觉行事。我们的提示词通过试错不断演进。我们尚未建立结构化方法来评估哪些提示词或模型表现最佳。即使我们成功合并了拉取请求，又如何确认它真正解决了原始问题？敬请关注我们下一篇关于利用反馈循环实现更可预测结果的博文。

---

> 本文由AI自动翻译，原文链接：[Background Coding Agents: Context Engineering (Part 2) | Spotify Engineering](https://engineering.atspotify.com/2025/11/context-engineering-background-coding-agents-part-2/)
> 
> 翻译时间：2026-01-04 23:56
