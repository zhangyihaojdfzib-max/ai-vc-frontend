---
title: TRL ⚡️ 推出视觉语言模型对齐新方法
title_original: Vision Language Model Alignment in TRL ⚡️
date: '2025-08-07'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/trl-vlm-alignment
author: ''
summary: 本文介绍了TRL库中针对视觉语言模型（VLM）对齐的新方法，包括混合偏好优化（MPO）、组相对策略优化（GRPO）及其变体GSPO，这些方法超越了传统的直接偏好优化（DPO），能从偏好数据中提取更丰富的信号。文章还扩展了强化留一法（RLOO）和在线DPO以支持VLM，并提供了原生监督微调支持、vLLM集成及实用资源，旨在实现更高效、可扩展的多模态对齐。
categories:
- AI研究
tags:
- 视觉语言模型
- 模型对齐
- TRL
- 偏好优化
- 多模态AI
draft: false
translated_at: '2026-02-21T04:18:29.411596'
---

# TRL ⚡️ 中的视觉语言模型对齐

## 引言

视觉语言模型（VLMs）正变得越来越强大，但将其与人类偏好对齐仍然至关重要。在 TRL 中，我们已经展示了如何使用**监督微调（SFT）**和**直接偏好优化（DPO）**对 VLM 进行后训练。这次，我们将更进一步。

**太长不看版**：以下是 TRL 中的新内容：

- **混合偏好优化（MPO）**
- **组相对策略优化（GRPO）**
- **组序列策略优化（GSPO）**（GRPO 的一个变体）

这些方法超越了成对比较的 DPO，能从偏好数据中提取更丰富的信号，并能更好地与现代 VLM 协同扩展。

我们还扩展了现有方法以支持 VLM：

- **强化留一法（RLOO）**
- **在线直接偏好优化（在线 DPO）**

这使得多模态对齐更加高效和可扩展。

最后：

- **对视觉语言模型的原生监督微调支持**
- **训练脚本和演示笔记本**，助您快速上手

- TRL ⚡️ 中的多模态 VLM 对齐
    - 引言
    - 视觉语言模型的对齐
        - 混合偏好优化（MPO）
        - 多模态组相对策略优化（GRPO）
        - 组序列策略优化（GSPO）
        - 对比
        - VLM 的进一步扩展
            - 强化留一法（RLOO）
            - 在线直接偏好优化（在线 DPO）
    - 原生监督微调支持
    - TRL 中的 vLLM 集成
    - 实用资源

- 引言
- 视觉语言模型的对齐
    - 混合偏好优化（MPO）
    - 多模态组相对策略优化（GRPO）
    - 组序列策略优化（GSPO）
    - 对比
    - VLM 的进一步扩展
        - 强化留一法（RLOO）
        - 在线直接偏好优化（在线 DPO）
- 原生监督微调支持
- TRL 中的 vLLM 集成
- 实用资源

- 混合偏好优化（MPO）
- 多模态组相对策略优化（GRPO）
- 组序列策略优化（GSPO）
- 对比
- VLM 的进一步扩展
    - 强化留一法（RLOO）
    - 在线直接偏好优化（在线 DPO）

- 强化留一法（RLOO）
- 在线直接偏好优化（在线 DPO）

## 视觉语言模型的对齐

传统上，您会采用一个基础模型，应用 SFT 使其遵循指令，然后应用 DPO 将其与偏好数据对齐。此前，**我们将这种方法适配到视觉语言模型（VLMs）**，并在 IDEFICS2 上进行了验证，结果显示模型响应有所改善。

DPO 的工作原理是使用对比损失来优化模型响应对之间的偏好：您有一个被选中的答案和一个被拒绝的答案，然后根据您想要和不想要的内容来优化您的偏好。

但在过去一年中，新的多模态对齐方法 GRPO 和 MPO 越来越受欢迎，它们能将 VLM 的性能推向更高水平。在博文末尾，您可以找到一个展示模型响应差异的表格。

### 混合偏好优化（MPO）

使用 SFT 对齐多模态模型以执行推理任务，会因分布偏移而效果不佳。同时，使用 DPO 对齐的模型无法生成连贯的推理过程，并可能产生重复的响应。为了解决这个问题，出现了一种专门为多模态模型设计的新技术，称为**混合偏好优化（MPO）**。该方法本质上是 DPO 的扩展，包含多个损失：来自 DPO 的偏好损失（sigmoid）、来自二元分类器优化（BCO）的质量损失，以及来自 SFT 的生成损失。根据**论文**所述，仅仅切换到这种组合损失就能在 MathVista 上带来 6.2 分的提升！

![MPO](/images/posts/92d5a5849c06.png)

由于这仅涉及修改损失函数，我们已将组合损失支持添加到 TRL 的 `DPOTrainer` 类中。要使用它，您可以按如下方式初始化 `DPOConfig`：

```python
mpo_config = DPOConfig(
    loss_type=["sigmoid", "bco_pair", "sft"], 
    loss_weights=[0.8, 0.2, 1.0], 
)
```

然后初始化 `DPOTrainer`：

```python
mpo_trainer = DPOTrainer(
    model=model_id,
    args=mpo_config,
    processing_class=tokenizer,
    train_dataset=dataset,
)
mpo_trainer.train()
```

就是这样！如果您想进一步探索，可以在此处找到一个完整的笔记本示例。

### 多模态组相对策略优化（GRPO）

**组相对策略优化（GRPO）** 是一种前沿的对齐方法，最初在 DeepSeek Math 论文中提出，后来集成到突破性的大语言模型 DeepSeek R1 中。它是 PPO 的一个补充，其中策略更新是在组（代表对话如何展开的轨迹批次）上进行的。这一特性使其对奖励噪声更具鲁棒性，因为噪声在组内被平均掉了。由于模型学习的是良好响应的更广泛意义，而不是单一的高奖励样本，这种方法也使模型具有高性能。

![GRPO](/images/posts/540691e33357.png)

在 TRL 中，我们现在为视觉语言模型引入了 GRPO 支持。我们不会提供完整的训练脚本示例，因为您可以在笔记本中找到。相反，我们将重点介绍关键组件和概念。

为了使训练脚本有效工作，我们需要验证答案格式是否正确，以及解决方案本身是否接近已完成的部分，因此我们编写了两个奖励函数。为了真正看到后一个奖励的改进，您需要一个相当极致的设置，包括相对较大的模型、大量的生成以及高质量、多样化的数据集。

```python
import re
from math_verify import LatexExtractionConfig, parse, verify

def format_reward(completions, **kwargs):
    """Reward function that checks if the completion has a specific format."""
    pattern = r"^<think>.*?</think>\s*<answer>.*?</answer>$"
    matches = [re.match(pattern, content) for content in completions]
    rewards_list = [1.0 if match else 0.0 for match in matches]
    rewards = [1.0 if match else 0.0 for match in matches]
    print(completions)
    print(rewards)
    return rewards

def accuracy_reward(completions, **kwargs):
    """Reward function that checks if the completion is the same as the ground truth."""
    solutions = kwargs['solution']
    completion_contents = [completion[0]["content"] for completion in completions]
    rewards = []
    for content, solution in zip(completion_contents, solutions):
        gold_parsed = parse(solution, extraction_mode="first_match", extraction_config=[LatexExtractionConfig()])
        answer_parsed = parse(content, extraction_mode="first_match", extraction_config=[LatexExtractionConfig()])
        if len(gold_parsed) != 0:
            try:
                rewards.append(float(verify(answer_parsed, gold_parsed)))
            except Exception:
                rewards.append(0.0)
        else:
            rewards.append(1.0)
    return rewards
```

然后，您可以初始化 GRPOConfig 和 GRPOTrainer，传入我们上面定义的奖励函数，并调用 train() 开始训练。

```python
from trl import GRPOConfig, GRPOTrainer

training_args = GRPOConfig(
    learning_rate=1e-5,
    max_prompt_length=None,
    ... 
)
trainer = GRPOTrainer(
    model=model_id,
    reward_funcs=[format_reward, accuracy_reward],
    args=training_args,
    train_dataset=train_dataset,
)
trainer.train()
```

探索完整的笔记本示例。

### 组序列策略优化（GSPO）

**组序列策略优化（GSPO）** 是 Qwen 最近发布的一种 RL 对齐算法，它克服了 GRPO 的一些限制。它通过在序列级别而不是每个 Token 级别计算重要性采样权重，实现了更稳定的训练。其好处在 MoE 风格的模型中**更为相关**。

最新的 TRL 也引入了对 GSPO 的支持，由于它是 GRPO 损失的一个变体，因此也支持多模态。创建训练器的过程与 GRPO 相同，但需要添加以下额外参数（值摘自论文）。

```python
from trl import GRPOConfig
```

training_args = GRPOConfig(
        ...
    importance_sampling_level="sequence",
    epsilon=3e-4,
    epsilon_high=4e-4,
    beta=0.0,
    loss_type="grpo",
    steps_per_generation=1,
    steps_per_generation=4
)

```

### 对比

下表总结了使用上述讨论的技术微调后的 Qwen2.5VL-3B 模型输出。请注意，我们仅在数据集子集上进行了少量运行，并且模型是在不同数据集上微调的，因此此对比仅供参考。

图片：

![Image](/images/posts/17c778f5cd34.png)

问题：在给定的图中，如果角 APO 是 25.0 度，那么角 AOB 的度数是多少？选项：A: 140° B: 130° C: 120° D: 110°

数据集中的答案：要求角 AOB 的度数，我们可以利用四边形内角和等于 360 度的性质。应用此性质，我们可以将角 AOB 计算为 360 - 角 OAP - 角 OBP - 角 P。由于角 OAP 和角 OBP 都是 90 度，且角 P 已知为 50 度，我们可以将这些值代入方程得到角 AOB = 360 - 90 - 90 - 50 = 130 度。因此，角 AOB 的度数是 130 度。所以，答案是选项 B。答案：B

基础模型输出：要求角 AOB 的度数，我们需要利用圆中同一弧所对角的性质。具体来说，弧在圆心所对的角（角 AOB）是同一弧在圆周上任意一点所对角（角 APO）的两倍。已知角 APO = 25°，我们可以如下计算角 AOB：由于角 AOB 是角 APO 的两倍，我们有：[ 角 AOB = 2 * 角 APO ][ 角 AOB = 2 * 25° ][ 角 AOB = 50° ]然而，根据提供的选项，这个计算似乎不正确。让我们用正确的性质重新评估。正确的性质指出，弧在圆心所对的角是同一弧在圆周上任意一点所对角的两倍。因此，正确的计算应该是：角 AOB = 2 * 角 APO = 2 * 25° = 50°但由于这与给定的选项不符，让我们考虑另一种方法。如果角 APO 是 25°，并且考虑到圆内接四边形的性质或圆内接四边形对角和为 180° 的事实，我们可能需要考虑由弧和弦形成的补角。让我们假设正确的解释涉及找到给定角 APO 的补角，这将给出角 AOB 的度数。角 AOB = 180° - 角 APO = 180° - 25° = 155°这仍然与选项不符。根据提供的选项，似乎可能存在误解或需要不同的解释。然而，基于标准的几何原理，正确答案应该是：角 AOB = 2 * 角 APO = 2 * 25° = 50° 如果不是这种情况，请提供更多上下文或澄清问题陈述。

MPO 输出：要求角 AOB 的度数，我们需要利用圆中同一弧所对角的性质。具体来说，弧在圆心所对的角（角 AOB）是同一弧在圆周上任意一点所对角（角 APO）的两倍。已知角 APO = 25°，我们可以如下计算角 AOB：由于角 AOB 是角 APO 的两倍，我们有：[ 角 AOB = 2 * 角 APO ][ 角 AOB = 2 * 25° ][ 角 AOB = 50° ]然而，根据提供的选项，这个计算似乎不正确。让我们用正确的性质重新评估。正确的性质指出，弧在圆心所对的角是同一弧在圆周上任意一点所对角的两倍。因此，正确的计算应该是：角 AOB = 2 * 角 APO = 2 * 25° = 50°但由于这与给定的选项不符，让我们考虑另一种方法。如果我们看三角形 OAP，角 AOP 是 90°，因为它是直角（因为它内接于半圆）。现在，角 APO 已知为 25°，所以角 AOA 将是 65°（因为它是角 APO 的补角）。因此，角 AOB 将是这个的一半，即 32.5°。然而，这也不符合选项。让我们考虑问题可能涉及不同的解释或不同的几何配置的可能性。根据选项，让我们假设问题陈述或选项本身可能存在错误。如果我们要从给定的选项中选择，基于此类问题的标准几何原理，似乎没有一个选项正确匹配。

GRPO 输出：要求角 AOB 的度数，我们需要理解角 AOB 是一个内接角，它截取与圆心角 AOP 相同的弧。根据圆周角定理，内接角的度数是截取同一弧的圆心角度数的一半。已知：- 角 APO = 25.0 度由于角 AOB 是一个内接角，截取与圆心角 AOP 相同的弧，我们可以使用圆周角定理：角 AOB 的度数是角 AOP 度数的一半。因此，角 AOB 的度数是 130 度。

GSPO 输出：要求角 AOB 的度数，我们需要理解角 AOB 是一个内接角，它截取与圆心角 AOP 相同的弧。根据圆周角定理，内接角的度数是截取同一弧的圆心角度数的一半。已知：- 角 APO = 25.0 度由于角 AOB 是一个内接角，截取与圆心角 AOP 相同的弧，我们可以使用圆周角定理来求角 AOB 的度数：角 AOB 的度数是角 AOP 度数的一半。因此，答案是 B: 130°。

### 针对 VLM 的进一步扩展

除了 MPO、GRPO 和 GSPO，TRL 现在支持用于视觉语言模型（VLM）的**强化学习留一法**（RLOO）和**在线直接偏好优化**（Online DPO），从而能够在多模态数据集上进行对齐。

#### 强化学习留一法（RLOO）

RLOO 现在支持常见的 VLM。您可以在 `rloo_vlm.py` 脚本中找到完整的训练示例。

以下是设置 `RLOOTrainer` 的方法：

```python
trainer = RLOOTrainer(
    model=model_name,
    args=training_args,
    reward_funcs=[think_format_reward, accuracy_reward],
    train_dataset=train_dataset,
    eval_dataset=eval_dataset,
)

trainer.train()

```

要从示例脚本直接启动训练：

```bash
CUDA_VISIBLE_DEVICES=1,2 python3 examples/scripts/rloo_vlm.py --model_name_or_path Qwen/Qwen2.5-VL-3B-Instruct

```

#### 在线直接偏好优化（Online DPO）

Online DPO 也支持 VLM。请参阅 `online_dpo_vlm.py` 脚本获取简单示例。

运行示例脚本（vLLM 集成将在后面讨论）：

```bash
CUDA_VISIBLE_DEVICES=1,2 python3 examples/scripts/online_dpo_vlm.py --model_name_or_path Qwen/Qwen2.5-VL-3B-Instruct --use_vllm --vllm_mode server

```

这些脚本已准备好用于 VLM 训练；完整的参数调优记录在 TRL 文档中：Online DPO trainerRLOO Trainer。

## 原生监督微调支持

此前，`SFTTrainer` 对视觉语言模型的支持是部分的。这主要是由于 transformers API 中 VLM 实现的许多差异。随着 transformers API 的标准化，我们已经完全支持视觉语言模型。您可以简单地使用 VLM 初始化 `SFTTrainer`。

```python
from trl import SFTConfig, SFTTrainer
from datasets import load_dataset

trainer = SFTTrainer(
    model="Qwen/Qwen2.5-VL-3B-Instruct",
    args=SFTConfig(max_length=None), 
    train_dataset=load_dataset("trl-lib/llava-instruct-mix", split="train"),
)
trainer.train()

```

要训练一个VLM，您需要提供一个包含额外`images`列的数据集，该列存放待处理的图像。您可以查阅[Dataset Formats — Vision Datasets](https://huggingface.co/docs/datasets/main/en/image_dataset)了解更多关于数据集格式的信息。一个很好的示例是[LLaVA Instruct Mix](https://huggingface.co/datasets/liuhaotian/LLaVA-Instruct-150K)。

我们还提供了一个开箱即用的`sft_vlm.py`脚本，适用于基于Transformer的视觉语言模型。

## TRL中的vLLM集成

vLLM已集成到TRL中，以支持需要在训练期间生成样本的在线对齐方法。运行以下示例脚本即可启用vLLM：

```bash
CUDA_VISIBLE_DEVICES=1,2 python3 examples/scripts/grpo_vlm.py --model_name_or_path Qwen/Qwen2.5-VL-3B-Instruct --use_vllm --vllm_mode colocate
```

主要有两种模式：`colocate`和`server`。`colocate`模式在与训练循环相同的进程中运行vLLM，在训练和生成之间共享同一GPU，并在`GRPOTrainer`内部创建一个vLLM LLM实例。而`server`模式要求您在单独的进程中独立运行vLLM服务，并通过服务器接口进行调用。您可以使用以下命令启动此服务器：

```bash
trl vllm-serve --model Qwen/Qwen2.5-VL-3B-Instruct --tensor-parallel-size 1 
```

然后，您可以按如下方式运行脚本。

```bash
CUDA_VISIBLE_DEVICES=1,2 python3 examples/scripts/grpo_vlm.py --model_name_or_path Qwen/Qwen2.5-VL-3B-Instruct --use_vllm --vllm_mode server
```

还有一个提示：我们已在TRL中添加了对使用基于Transformer后端的vLLM的支持。您可以在运行`colocate`模式的脚本或启动模型服务时，通过传递`--vllm_model_impl transformers`标志来启用此功能。

您可以在[此处](https://huggingface.co/docs/trl/main/en/vllm_integration)阅读更多关于TRL中vLLM集成的信息。

## 实用资源

下面，您可以找到一系列详细探索VLM对齐的资源。敬请查阅！

- [视觉语言模型（更好、更快、更强）](https://huggingface.co/blog/vlms)
- [通过混合偏好优化增强多模态大语言模型的推理能力](https://arxiv.org/abs/2406.07594)（MPO论文）
- [DeepSeekMath：突破开源语言模型数学推理的极限](https://arxiv.org/abs/2402.03300)（GRPO论文）
- [Open-R1仓库](https://github.com/allenai/open-r1)和[Open-R1奖励函数](https://huggingface.co/collections/allenai/open-r1-66e8c4c8c3b8c5a4e0a0b5e5)
- [TRL文档](https://huggingface.co/docs/trl)和[TRL仓库](https://github.com/huggingface/trl)
- [MPO VLM配方](https://github.com/huggingface/trl/tree/main/recipes/mpo_vlm)
- [GRPO VLM配方](https://github.com/huggingface/trl/tree/main/recipes/grpo_vlm)
- [更多多模态对齐配方](https://github.com/huggingface/trl/tree/main/recipes)
- [TRL多模态训练脚本](https://github.com/huggingface/trl/tree/main/examples/scripts)
- [trl文档中的vLLM集成](https://huggingface.co/docs/trl/main/en/vllm_integration)
- [vLLM中的Transformer后端集成](https://docs.vllm.ai/en/latest/models/transformers.html)

---

> 本文由AI自动翻译，原文链接：[Vision Language Model Alignment in TRL ⚡️](https://huggingface.co/blog/trl-vlm-alignment)
> 
> 翻译时间：2026-02-21 04:18
