---
title: OVHcloud成为Hugging Face推理服务提供商
title_original: OVHcloud on Hugging Face Inference Providers 🔥
date: '2025-11-24'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/OVHcloud/inference-providers-ovhcloud
author: null
summary: OVHcloud正式成为Hugging Face Hub的推理服务提供商，为用户提供在欧洲数据中心运行的无服务器AI推理服务。该服务支持GPT-OSS、Qwen3、DeepSeek
  R1等热门开源模型，具有低延迟、按Token付费、数据主权保障等特点，并已集成到Hugging Face的Python和JavaScript SDK中，支持用户通过自定义API密钥或HF路由两种方式调用。
categories:
- AI基础设施
tags:
- Hugging Face
- OVHcloud
- AI推理
- 无服务器计算
- 开源模型
draft: false
translated_at: '2026-01-06T01:03:48.409Z'
---

**OVHcloud 成为 Hugging Face 推理服务提供商 🔥**
我们非常高兴地宣布，OVHcloud 现已成为 Hugging Face Hub 上支持的推理服务提供商！OVHcloud 加入了我们不断壮大的生态系统，直接在 Hub 的模型页面上增强了无服务器推理的广度和能力。推理服务提供商也已无缝集成到我们的客户端 SDK（包括 JS 和 Python 版本）中，使得使用您偏爱的提供商来调用各种模型变得超级简单。

此次发布使得从 Hugging Face 直接访问流行的开放权重模型（如 gpt-oss、Qwen3、DeepSeek R1 和 Llama）变得前所未有的容易。您可以在 Hub 上浏览 OVHcloud 的组织页面：https://huggingface.co/ovhcloud，并在 https://huggingface.co/models?inference_provider=ovhcloud&sort=trending 尝试受支持的热门模型。

OVHcloud AI Endpoints 是一项完全托管的无服务器服务，通过简单的 API 调用即可访问来自领先研究实验室的前沿 AI 模型。该服务提供具有竞争力的按 Token 付费定价，起价为每百万 Token 0.04 欧元。

该服务运行于位于欧洲数据中心的安全基础设施上，确保欧洲用户的数据主权和低延迟。该平台支持高级功能，包括结构化输出、函数调用以及面向文本和图像处理的多模态能力。

专为生产环境打造，OVHcloud 的推理基础设施可实现首个 Token 低于 200 毫秒的响应时间，非常适合交互式应用和智能体工作流。该服务同时支持文本生成和嵌入/向量模型。您可以在 https://www.ovhcloud.com/en/public-cloud/ai-endpoints/catalog/ 了解更多关于 OVHcloud 平台和基础设施的信息。

在其专用文档页面阅读更多关于如何使用 OVHcloud 作为推理服务提供商的信息。
在此处查看支持的模型列表。

**工作原理**
**在网站界面中**
在您的用户账户设置中，您可以：
- 为您已注册的提供商设置您自己的 API 密钥。如果未设置自定义密钥，您的请求将通过 HF 路由。
- 按偏好排序提供商。这适用于模型页面中的小部件和代码片段。

如前所述，调用推理服务提供商时有两种模式：
- 自定义密钥（调用直接发送至推理服务提供商，使用您在该提供商处的自有 API 密钥）
- 由 HF 路由（在这种情况下，您不需要提供商的 Token，费用将直接计入您的 HF 账户，而非提供商的账户）

模型页面会展示第三方推理服务提供商（与当前模型兼容的提供商，按用户偏好排序）。

**从客户端 SDK**
**从 Python，使用 huggingface_hub**
以下示例展示了如何使用 OVHcloud 作为推理服务提供商来调用 OpenAI 的 gpt-oss-120b。您可以使用 Hugging Face Token 通过 Hugging Face 自动路由，或者如果您有的话，使用您自己的 OVHcloud AI Endpoints API 密钥。

注意：这需要使用较新版本的 huggingface_hub（>= 1.1.5）。

```python
import os
from huggingface_hub import InferenceClient

client = InferenceClient(
    api_key=os.environ["HF_TOKEN"],
)

completion = client.chat.completions.create(
    model="openai/gpt-oss-120b:ovhcloud",
    messages=[
        {
            "role": "user",
            "content": "What is the capital of France?"
        }
    ],
)

print(completion.choices[0].message)
```

**从 JS，使用 @huggingface/inference**
```javascript
import { InferenceClient } from "@huggingface/inference";

const client = new InferenceClient(process.env.HF_TOKEN);

const chatCompletion = await client.chatCompletion({
    model: "openai/gpt-oss-120b:ovhcloud",
    messages: [
        {
            role: "user",
            content: "What is the capital of France?",
        },
    ],
});

console.log(chatCompletion.choices[0].message);
```

**计费**
计费方式如下：
对于直接请求，即当您使用推理服务提供商的密钥时，您将由相应的提供商计费。例如，如果您使用 OVHcloud API 密钥，费用将计入您的 OVHcloud 账户。
对于路由请求，即当您通过 Hugging Face Hub 进行身份验证时，您只需支付标准的提供商 API 费率。我们不收取额外加价；我们只是直接转嫁提供商的成本。（未来，我们可能与我们的提供商合作伙伴建立收入分成协议。）

重要提示 ‼️ PRO 用户每月可获得价值 2 美元的推理额度。您可以在各提供商间使用这些额度。🔥
订阅 Hugging Face PRO 计划以获取推理额度、ZeroGPU、Spaces 开发者模式、20 倍更高的限制等权益。

我们也为已登录的免费用户提供少量配额的免费推理，但如果可以，请升级到 PRO！

**反馈与后续步骤**
我们非常希望得到您的反馈！请在此处分享您的想法和/或评论：https://huggingface.co/spaces/huggingface/HuggingDiscussions/discussions/49


> 本文由AI自动翻译，原文链接：[OVHcloud on Hugging Face Inference Providers 🔥](https://huggingface.co/blog/OVHcloud/inference-providers-ovhcloud)
> 
> 翻译时间：2026-01-06 01:03
