---
title: Python projects now support routing rules - Vercel
title_original: Python projects now support routing rules - Vercel
date: '2026-08-26'
source: Vercel Blog
source_url: https://vercel.com/changelog/python-projects-now-support-routing-rules
author: ''
summary: '[翻译失败，原文如下]


  Python projects can now userouting rulesto set response headers or rewrite requests
  to internal paths, including apps built with FastAPI,...'
categories:
- 未分类
tags: []
draft: false
translated_at: '2026-09-01T07:32:13.353137'
---

[翻译失败，原文如下]

Python projects can now userouting rulesto set response headers or rewrite requests to internal paths, including apps built with FastAPI, Django, and Flask.

TheVercel CDNevaluates rules before requests reach your application, so changes apply without a new deployment.

For example, this FastAPI app serves a/newroute:

```
1from fastapi import FastAPI2
3app = FastAPI()4
5@app.get("/new")6def new_route() -> dict[str, str]:7    return {"message": "New route!"}
```

Defines the /new route the rewrite below targets

To send requests for/oldto that route, create a rewrite from the CDN tab in your project dashboard or with theVercel CLI:

```
vercel routes add --ai "Rewrite /old to /new" --yes
vercel routes list --diff
vercel routes publish
```

Creating and publishing a rewrite with the Vercel CLI

Published rules take effect immediately across all regions, and you can roll back to a previous version from the History tab.

You can also manage rules from Python with thevercelSDK, which wraps theVercel REST API.add_routestages the rule, andupdate_route_versionpublishes it:

```
1from vercel.client import Vercel2from vercel.project_routes import RewriteRoute3
4vercel = Vercel(access_token="...")5
6added = vercel.project_routes.add_route(7    project_id="prj_123",8    route=RewriteRoute(name="Rewrite /old to /new", source="/old", destination="/new"),9)10vercel.project_routes.update_route_version(11    project_id="prj_123", version_id=added.version.id, action="promote"12)
```

Stages and publishes the same rewrite from Python

Rewrites can also be defined invercel.json:

```
1{2  "$schema": "https://openapi.vercel.sh/vercel.json",3  "rewrites": [4    {5      "source": "/old",6      "destination": "/new"7    }8  ]9}
```

Rewrites /old to /new at the deployment level

Project-level rules live outside your deployment and are evaluated before its routes, so a published rule takes precedence over avercel.jsonrewrite for the same path. Each surface fits a different workflow:

How you define it

Takes effect

Best for

CDN tab in the dashboard

On publish

Editing and testing rules visually

Vercel CLI

One-off changes from the terminal

vercelPython SDK or REST API

Automation and CI/CD

vercel.json

On each deployment

Rewrites that ship with the application code

Update Vercel CLI to the latest version and read therouting documentationto get started.

Kevin Corbett

---

> 本文由AI自动翻译，原文链接：[Python projects now support routing rules - Vercel](https://vercel.com/changelog/python-projects-now-support-routing-rules)
> 
> 翻译时间：2026-09-01 07:32
