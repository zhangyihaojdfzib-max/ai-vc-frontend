---
title: 后台编码智能体：通过强验证循环确保可预测性
title_original: 'Background Coding Agents: Predictable Results Through Strong Feedback
  Loops (Part 3) | Spotify Engineering'
date: '2025-12-09'
source: Spotify Engineering
source_url: https://engineering.atspotify.com/2025/12/feedback-loops-background-coding-agents-part-3/
author: Max Charas; Marc Bruggmann
summary: 本文是Spotify关于后台编码智能体系列文章的第三部分，探讨如何在没有直接人工监督下，确保智能体驱动的代码变更可靠且正确。文章分析了三种主要故障模式，并提出了通过设计“验证循环”来提升可预测性的解决方案。该方案结合了确定性验证器（如构建和测试验证）与LLM裁判，以约束智能体行为、防止其超出提示词范围，从而减少错误PR的生成，保障自动化代码更改的质量与工程师信任。
categories:
- 工程实践
tags:
- AI工程
- 自动化编码
- 软件维护
- 验证循环
- 持续集成
draft: false
---

**后台编码智能体：通过强反馈循环实现可预测的结果（第三部分）**

这是我们关于 Spotify 在后台编码智能体领域的探索之旅以及大规模软件维护未来系列文章的第三部分。另请参阅第一部分和第二部分。

在第二部分中，我们探讨了如何让我们的舰队管理系统能够使用智能体自动重写软件。我们还探讨了如何编写良好的提示词，使智能体能够在无需人工输入的情况下发挥最佳作用。在这篇博文中，我们试图回答一个新问题：

👉 **一个在没有直接人工监督下运行的智能体，需要什么样的环境才能尽可能频繁地产生正确且可靠的结果？**

**故障模式**

从高层次来看，当我们在数千个不同的软件组件上运行智能体驱动的代码变更时，我们主要担心三种故障模式。

1.  **后台智能体未能生成 PR。** 这只是个小麻烦。我们通常可以容忍此处较低的失败率。最坏的情况是我们必须手动执行更改。
2.  **后台智能体生成了一个在持续集成（CI）中失败的 PR。** 这对工程师来说是一个令人沮丧的错误。智能体似乎无法完全完成任务，这迫使工程师决定是否要尝试修复半损坏的代码。
3.  **后台智能体生成了一个通过 CI 但在功能上不正确的 PR。** 这是最严重的错误，因为它会削弱我们对自动化的信任。在对数千个组件执行更改时，这些更改在代码审查中很难被发现。如果这些 PR 被合并，它们可能会破坏生产环境的功能。

第二和第三种故障模式的发生可能有几个原因：目标组件几乎没有测试覆盖率；智能体“发挥创意”，决定更改提示词范围之外的内容；或者智能体根本不知道如何正确运行构建和测试。第二和第三种故障模式也可能大量消耗工程师的时间；审查毫无意义的 PR 成本高昂。

为了缓解这个问题，我们必须从一开始就为可预测性进行设计。

**为可预测性而设计：验证循环**

我们通过实施强大的验证循环来应对这一挑战，这些循环引导智能体走向期望的结果。验证循环允许智能体及其底层 LLM 在提交更改之前，逐步确认其走在正确的轨道上。

这个验证循环的一个关键设计原则是，智能体不知道验证做什么以及如何做，它只知道它可以（在某些情况下必须）调用验证来检查其更改。

验证循环由一个或多个独立的验证器组成。单个验证器不会直接暴露给智能体；相反，它会根据软件组件的内容自动激活。例如，如果在代码库根目录中找到 `pom.xml` 文件，则会激活 Maven 验证器。

上面是一个简单的 Maven 验证器示例，下面是向智能体暴露验证器的模型上下文协议（MCP）工具定义。请注意 MCP 层面的抽象。智能体不知道底层的 Maven 验证器。

这些验证器提供了两个关键好处。如前所述，它们允许智能体获得增量反馈，引导其找到正确的解决方案。它们还抽象掉了许多原本会消耗智能体宝贵上下文窗口的噪音和决策。例如，智能体不需要理解调用不同构建系统的细节或解析测试的复杂输出；验证器会处理这些任务。这种类型的输出解析非常有影响力，但实现起来有点繁琐。例如，我们的许多验证器使用正则表达式在失败时仅提取最相关的错误消息，否则返回非常简短的成功消息。

这个验证循环可以作为工具调用来触发，但我们的智能体在尝试打开 PR 之前也会运行所有相关的验证器。对于 Claude Code，我们使用停止钩子来实现这一点。如果其中一个验证器失败，则不会打开 PR，并向用户显示错误消息。

这些调用格式化、构建和测试的验证器确保我们的智能体生成语法正确、能够构建并通过测试的代码。

**在验证循环中使用 LLM**

除了这些确定性的验证器之外，我们还增加了另一层保护：使用 LLM 作为裁判。我们发现这是必要的，因为一些智能体有点过于“雄心勃勃”，试图解决严格来说不在其提示词范围内的问题，比如重构代码或禁用不稳定的测试。

裁判很简单。它使用提议更改的差异和原始提示词，并将它们发送给 LLM 进行评估。裁判包含在标准验证循环中，并在所有其他验证器完成后运行。

上面，您可以找到我们裁判的系统提示词。

我们尚未对我们的裁判进行评估。然而，我们从内部指标得知，在数千次智能体会话中，裁判否决了大约四分之一。当这种情况发生时，智能体有一半的时间能够进行纠正。根据经验观察，我们发现最常见的触发原因是智能体超出了提示词中概述的指令范围。

**保持智能体专注**

根据设计，我们的后台编码智能体旨在做一件事：接收提示词并尽其所能执行代码更改。智能体本身的访问权限非常有限。它可以查看相关的代码库，使用工具编辑文件，并将验证器作为工具执行。

许多复杂的任务都在智能体本身之外处理。推送代码、在 Slack 上与用户交互，甚至提示词的编写，都由外围基础设施管理。这是有意为之的，我们相信智能体灵活性的降低使其更具可预测性。这对安全性也有积极的次要影响。智能体在权限有限、二进制文件很少、几乎无法访问周围系统的容器中运行。它是高度沙盒化的。

通过验证器和裁判的引导，我们发现我们的智能体能够以高度的可靠性解决日益复杂的任务。没有这些反馈循环，智能体通常会产生根本无法工作的代码。

**未来展望**

虽然我们为取得的进展感到自豪，但我们在后台编码智能体方面的工作还远未结束。我们将继续投资这一领域，并确定了未来探索的几个关键方向。

首先，我们计划扩展验证器基础设施，以支持更广泛的硬件和操作系统。我们当前的验证器仅在 Linux x86 上运行，虽然这很好地服务于我们的后端和 Web 基础设施，但我们的许多系统有特定需求。例如，我们的 iOS 应用程序需要 macOS 主机才能成功运行验证器，而一些后端系统依赖于 ARM64 架构。构建这种能力对于更广泛的采用至关重要。

其次，我们的目标是让后台智能体与我们现有的 CI/CD 流水线更深度地集成，特别是使其能够对 GitHub 拉取请求中的 CI 检查采取行动。我们将其设想为对我们验证器快速反馈“内循环”的补充性“外循环”，增加另一层验证。

最后，我们认识到需要更结构化的评估。实施稳健的评估将使我们能够系统地评估系统提示词的更改、试验新的智能体架构，并对不同的 LLM 提供商进行基准测试，最终带来更可靠、更高效的智能体。

这是一个快速发展的领域，我们很高兴能应对这些挑战。如果您的组织在使用 LLM 进行大规模代码转换方面有经验，我们非常乐意与您联系并向您学习。

---

> 本文由AI自动翻译，原文链接：[Background Coding Agents: Predictable Results Through Strong Feedback Loops (Part 3) | Spotify Engineering](https://engineering.atspotify.com/2025/12/feedback-loops-background-coding-agents-part-3/)
> 
> 翻译时间：2026-01-04 23:55
