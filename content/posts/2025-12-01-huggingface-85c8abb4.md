---
title: Transformers v5发布：以简洁模型定义驱动AI生态
title_original: 'Transformers v5: Simple model definitions powering the AI ecosystem'
date: '2025-12-01'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/transformers-v5
author: null
summary: 文章介绍了Hugging Face Transformers库从v4到v5的五年发展历程，安装量从每日2万次激增至300万次，模型架构从40种扩展到400多种。v5版本聚焦于简洁性、训练、推理和生产四大方向，通过模块化设计、代码精简、统一注意力接口和简化分词处理等举措，旨在成为模型定义的“单一可信源”，并逐步将PyTorch作为核心后端，以更清晰的代码支持AI生态系统的持续创新与协作。
categories:
- AI基础设施
tags:
- Transformers
- Hugging Face
- 模型架构
- 开源AI
- 机器学习库
draft: false
translated_at: '2026-01-06T01:01:58.217Z'
---

Transformers v5：驱动AI生态系统的简洁模型定义
Transformers的v4.0.0rc-1版本，即第4版的初始候选版本，于2020年11月19日发布。五年后的今天，我们发布v5.0.0rc-0。
如今，随着v5的发布，Transformers每天通过pip的安装量已超过300万次——而在v4时期仅为每天2万次🤯。总计下来，其安装量现已突破12亿次！
生态系统已从v4时期的40种模型架构扩展到如今的400多种，社区在Hub上贡献了超过75万个与Transformers兼容的模型检查点，而在v4时期这一数字大约仅为1,000个。
这一增长得益于该领域的发展以及如今AI已成为主流。作为生态系统中的领先模型定义库，我们需要持续演进和调整该库以保持其相关性。自我革新是AI领域保持长久生命力的关键。
我们很幸运能与许多基于transformers构建的库和应用进行合作，排名不分先后：llama.cpp、MLX、onnxruntime、Jan、LMStudio、vLLM、SGLang、Unsloth、LlamaFactory、dLLM、MaxText、TensorRT、Argmax，以及许多其他伙伴。
对于v5，我们希望专注于几个重要方面：简洁性、训练、推理和生产。我们将在本文中详细介绍在这些方面所做的工作。

**简洁性**
团队的首要关注点是简洁性。在开发transformers时，我们将代码视为产品。我们希望我们的模型集成是清晰的，这样生态系统可以依赖我们的模型定义，并理解底层真正发生了什么、模型之间有何不同，以及每个新模型的关键特性。简洁性带来了更广泛的标准化、通用性和更广泛的支持。

**模型新增**
Transformers是数十万个项目的支柱，Unsloth也包括在内。我们基于Transformers构建，以帮助人们高效地微调和训练模型，无论是BERT、文本转语音（TTS）还是其他模型；即使模型尚未得到其他库的支持，也能为强化学习（RL）运行快速推理。我们对Transformers v5感到兴奋，并且非常高兴能与Hugging Face团队合作！
-- Unsloth的Michael Han

Transformers的核心仍然是一个模型架构工具包。我们的目标是涵盖所有最新的架构，并成为模型定义的“单一可信源”。五年来，我们每周新增1到3个新模型，如下图所示：
我们一直在努力改进这个模型新增流程。

**模块化方法**
在过去的一年里，我们大力推行模块化设计，这标志着向前迈出了重要一步。这使得维护更简单、集成更快，并促进了社区内更好的协作。
我们在《维护不可维护之物》的博客文章中进行了更深入的概述。简而言之，我们的目标是实现更简单的模型贡献流程，以及更低的维护负担。我们可以强调的一个指标是，当使用模块化方法时，贡献（和审查）的代码行数显著减少：
虽然我们尊重“一个模型，一个文件”的理念，但我们继续引入一些抽象，使通用辅助工具的管理更简单。这方面的一个主要例子是引入了AttentionInterface，它为注意力方法提供了一个集中化的抽象。eager方法将保留在建模文件中；而其他方法，如FA1/2/3、FlexAttention或SDPA，则移至接口中。
在过去的几年里，对新模型架构日益增加的零日支持以及注意力处理的标准化，有助于简化我们对现代LLM训练后阶段的支持。
-- Axolotl的Wing Lian

**模型转换工具**
我们正在构建工具，以帮助我们识别新模型与哪个现有模型架构相似。此功能使用机器学习来查找独立建模文件之间的代码相似性。更进一步，我们的目标是通过为要集成到我们transformers格式的模型开启一个草稿PR来自动化转换过程。这个过程减少了手动工作量并确保了一致性。

**代码精简**
**精简建模与分词/处理文件**
我们对建模和分词文件进行了重大重构。得益于上述模块化方法以及跨模型的标准化，建模文件得到了极大改进。标准化有助于抽象出大部分不构成模型的工具，使得建模代码仅包含模型前向/后向传递的相关部分。
与此同时，我们正在简化分词和处理文件：未来，我们将只专注于tokenizers后端，移除“快速”和“慢速”分词器的概念。
我们将使用tokenizers作为我们的主要分词后端，就像我们对基于PyTorch的模型所做的那样。我们将为基于Sentencepiece或MistralCommon的分词器提供替代方案，这些将不是默认选项但会得到支持。图像处理器现在将只存在其快速变体，这依赖于torchvision后端。
最后，我们将逐步停止对Flax/TensorFlow的支持，转而专注于将PyTorch作为唯一后端；然而，我们也在与Jax生态系统中的合作伙伴合作，以确保我们的模型与该生态系统之间的兼容性。
随着v5版本的发布，transformers将全面投入PyTorch。Transformers作为整个领域建模的单一可信源和基础；我们一直与团队合作，以确保在整个技术栈上都有良好的性能。
我们很高兴能在未来继续在训练、推理和部署方面推进这一点。
-- PyTorch基金会执行董事、Linux基金会AI总经理 Matt White

**训练**
训练仍然是团队在v5版本中的重点：虽然之前我们主要关注微调而非大规模预训练/全量训练，但最近我们也做了大量工作来改进对后者的支持。

**大规模预训练**
支持预训练意味着重新设计我们模型的初始化，确保它们能在不同并行范式下大规模工作，并提供对前向和后向传递的优化内核支持。
展望未来，我们很高兴能与torchtitan、megatron、nanotron以及任何其他有兴趣与我们合作的预训练工具扩展兼容性。

**微调与训练后处理**
我们继续与Python生态系统中的所有微调工具紧密合作。我们的目标是继续提供与PyTorch生态系统中的Unsloth、Axolotl、LlamaFactory、TRL等兼容的模型实现；同时，我们也在与JAX生态系统中的MaxText等工具合作，以确保它们的框架与transformers之间具有良好的互操作性。
现在，所有微调和训练后处理工具都可以依赖transformers获取模型定义；通过OpenEnv或Prime Environment Hub进一步支持Agentic用例。

**推理**
我们在v5版本中非常重视推理，进行了几项范式变更：引入专用内核、更简洁的默认设置、新的API、对优化推理引擎的支持。
与训练类似，我们一直在努力打包内核，以便在您的硬件和软件允许的情况下自动使用它们。如果您以前没听说过内核，我们建议您查看这篇文档。
除了这项工作，我们还推出了两个专门用于推理的新API：
- 我们支持连续批处理和分页注意力机制。这已在内部使用了一段时间，我们正在努力完善细节并编写使用指南。
- 我们引入了`transformers serve`作为新的transformers专用服务系统，它部署了一个与OpenAI API兼容的服务器。
我们认为这对于评估等用例来说是重要的一步，在这些用例中需要同时处理大量的推理请求。

我们并不追求像专用推理引擎（vLLM、SGLang、TensorRT LLM）那样进行专门的优化。相反，我们的目标是与此类引擎实现完美的相互兼容，具体细节将在下一节详述。

vLLM中的Transformers后端极大地促进了更多架构（如BERT和其他编码器）能够为更多用户所用。我们一直与Transformers团队合作，以确保众多模型能够跨模态提供，并实现最佳性能。这只是我们合作的开始：我们很高兴看到Transformers团队将把此作为版本5的重点发展方向。
-- vLLM的Simon Mo、Harry Mellor

标准化是加速AI创新的关键。Transformers v5使SGLang团队能够减少在模型重新实现上的时间，而将更多时间投入到内核优化上。我们期待共同构建一个更高效、更统一的AI生态系统！
-- SGLang的Chenyang Zhao

**生产与本地部署**
最近，我们一直与最流行的推理引擎紧密合作，推动它们使用transformers作为后端。其带来的附加价值是显著的：一旦一个模型被添加到transformers中，它就能在这些推理引擎中使用，同时还能利用每个引擎提供的优势：推理优化、专用内核、动态批处理等。

我们也一直与ONNXRuntime、llama.cpp和MLX保持非常密切的合作，以确保transformers与这些建模库的实现之间具有良好的互操作性。例如，得益于社区的巨大努力，现在可以非常轻松地在transformers中加载GGUF文件以进行进一步的微调。反之，transformers模型也可以轻松转换为GGUF文件，供llama.cpp使用。

Transformers框架是AI模型参考实现的首选之地。该框架在推动现代AI贯穿整个技术栈方面发挥着至关重要的作用。项目背后的团队和社区真正理解并秉承着开源开发与合作的精神。
-- ggml-org的Georgi Gerganov

对于MLX也是如此，transformers的safetensors文件可以直接与MLX的模型兼容。

很难过分强调Transformers（以及datasets、tokenizers等）对开源乃至整个AI生态系统的重要性。我数不清自己有多少次将Transformers作为事实上的标准参考来使用。
-- MLX的Awni Hannun

最后，我们正在突破本地推理的边界，并与executorch团队携手合作，以使transformers模型能够在设备端运行。我们正通过optimum将覆盖范围扩展到多模态模型（视觉、音频）。

**量化**
量化正迅速成为最先进模型开发的标准。许多SOTA模型现在以低精度格式（如8位和4位）发布（例如，gpt-oss、Kimi-K2、Deepseek-r1），硬件也日益针对低精度工作负载进行优化，社区也在积极分享高质量的量化检查点。在v5中，我们将量化作为Transformers支持的核心重点，确保与所有主要功能的完全兼容，并提供一个可靠的训练和推理框架。

我们引入了对模型权重加载方式的重大改变；借此，我们将量化提升为一等公民。

我们与Transformers团队的合作卓有成效，他们积极主动的代码审查、反馈和技术专长令人印象深刻。他们的支持对于集成TorchAO、扩展量化功能以及改进文档以促进在V5中的广泛采用至关重要。
-- TorchAO的Jerry Zhang

我们很高兴v5将量化提升为一等公民。这为bitsandbytes更好地支持TP和MoEs等关键功能奠定了基础，同时也使得集成新的量化方法变得更加容易。
-- bitsandbytes的Matthew Douglas & Titus von Koeller

**结语**
版本5发布的总体主题是“互操作性”。所有的重构、性能改进和标准化都围绕这一主题展开。v5与日益壮大的生态系统实现了良好且端到端的协同：使用Unsloth/Axolotl/LlamaFactory/MaxText训练模型，用vLLM/SGLang部署它，并将其导出到llama.cpp/executorch/MLX以在本地运行！

版本5无疑是我们社区众多成员过去五年努力的成果。我们也将其视为一个承诺，以及我们未来发展方向的一座灯塔。

我们借此机会清理了工具包并明确了重点；现在我们拥有一个干净的基石，可以在此基础上进行构建。得益于社区和团队的诸多改进，性能、可用性和可读性的提升将更容易实现。

现在v5.0.0的第一个候选版本已经发布，我们热切期待您的反馈。请查看我们的发布说明以了解所有技术细节，我们将在GitHub issues中等待您的反馈！

---

> 本文由AI自动翻译，原文链接：[Transformers v5: Simple model definitions powering the AI ecosystem](https://huggingface.co/blog/transformers-v5)
> 
> 翻译时间：2026-01-06 01:01
