---
title: AI智能体持续学习的三个层面：模型、框架与上下文
title_original: Continual learning for AI agents
date: '2026-04-05'
source: LangChain Blog
source_url: https://blog.langchain.com/continual-learning-for-ai-agents/
author: ''
summary: 本文提出AI智能体的持续学习应超越传统的模型权重更新，从三个层面展开：模型层（权重更新，面临灾难性遗忘挑战）、框架层（驱动智能体的代码与指令优化）和上下文层（外部配置与记忆更新）。文章通过Claude
  Code和OpenClaw等实例说明各层差异，强调执行轨迹是驱动各层学习的关键，并指出学习可在智能体、用户和组织等多层面混合进行。
categories:
- AI研究
tags:
- 持续学习
- AI智能体
- 机器学习框架
- 灾难性遗忘
- 执行轨迹
draft: false
translated_at: '2026-04-06T04:44:36.639175'
---

关于AI持续学习的大多数讨论都聚焦于一点：更新模型权重。但对于AI智能体而言，学习可以发生在三个不同的层面：模型层、框架层和上下文层。理解其中的差异，将改变你对如何构建能持续改进的系统的思考方式。

智能体系统的三个主要层面是：

- **模型**：模型权重本身。
- **框架**：围绕模型、驱动智能体所有实例的框架。这指的是驱动智能体的代码，以及始终作为框架一部分的任何指令或工具。
- **上下文**：存在于框架之外的附加上下文（指令、技能），可用于配置框架。

![](/images/posts/6b397b5529c9.png)

**示例一**：将其映射到像Claude Code这样的编码智能体：
- 模型：claude-sonnet等
- 框架：Claude Code
- 用户上下文：CLAUDE.md、/skills、mcp.json

**示例二**：将其映射到OpenClaw：
- 模型：多种
- 框架：Pi + 其他一些脚手架
- 智能体上下文：SOUL.md、来自clawhub的技能

当我们谈论持续学习时，大多数人会立刻想到模型。但实际上，一个AI系统可以在这三个层面都进行学习。

## 模型层的持续学习

当大多数人谈论持续学习时，他们最常指的就是这个：更新模型权重。

更新权重的技术包括SFT、RL（例如GRPO）等。

这里的一个核心挑战是**灾难性遗忘**——当模型根据新数据或任务进行更新时，它往往会在之前掌握的知识上表现退化。这是一个开放的研究问题。

当人们为特定的智能体系统训练模型时（例如，你可以将OpenAI的Codex模型视为为其Codex智能体而训练），他们很大程度上是为整个智能体系统进行训练的。理论上，你可以在更细粒度上做这件事（例如，可以为每个用户设置一个LORA），但在实践中，这主要在智能体层面进行。

## 框架层的持续学习

如前所述，框架指的是驱动智能体的代码，以及始终作为框架一部分的任何指令或工具。

随着**框架**变得越来越流行，已经有多篇论文讨论了如何优化框架。

最近的一篇是**《Meta-Harness：模型框架的端到端优化》**。

核心思想是智能体在循环中运行。首先让它在一系列任务上运行，然后进行评估。接着将所有日志存储到文件系统中。之后运行一个编码智能体来查看这些执行轨迹，并提出对框架代码的修改建议。

![](/images/posts/f005de16718a.png)

与模型的持续学习类似，这通常在智能体层面进行。理论上，你可以在更细粒度上做这件事（例如，为每个用户学习不同的代码框架）。

## 上下文层的持续学习

“上下文”位于框架之外，可用于配置框架。上下文包括指令、技能，甚至工具等。这也常被称为**记忆**。

同类型的上下文也存在于框架内部（例如，框架可能有基础系统提示词、技能）。区别在于它是框架的一部分还是配置的一部分。

学习上下文可以在几个不同的层面进行。

可以在智能体层面学习上下文——智能体拥有持久的“记忆”，并随时间更新其自身配置。一个很好的例子是OpenClaw，它有自己的SOUL.md文件，会随时间更新。

更常见的是在租户层面（用户、组织、团队等）学习上下文。在这种情况下，每个租户都有自己的上下文，并随时间更新。例子包括Hex的Context Studio、Decagon的Duet、Sierra的Explorer。

你也可以混合搭配！因此，一个智能体可以同时拥有智能体层面的上下文更新、用户层面的上下文更新以及组织层面的上下文更新。这些更新可以通过两种方式完成：

- **事后离线作业**。类似于框架更新——运行一系列最近的执行轨迹以提取见解并更新上下文。这就是OpenClaw所称的“梦想”。
- **在智能体运行的热路径中**。智能体可以决定（或用户可以提示它）在核心任务执行过程中更新其记忆。

![](/images/posts/0fae731ad905.png)

这里需要考虑的另一个维度是记忆更新的明确程度。是用户提示智能体记住，还是智能体基于框架本身的核心指令进行记忆？

## 对比

![](/images/posts/f2b7b9a98c10.jpg)

## 执行轨迹是核心

所有这些流程都由**执行轨迹**驱动——即智能体所做事情的完整执行路径。LangSmith是我们的平台（除其他功能外），它帮助收集执行轨迹。

然后，你可以以多种不同的方式使用这些轨迹。

如果你想更新模型，可以收集轨迹，然后与像Prime Intellect这样的合作伙伴合作来训练你自己的模型。

如果你想改进框架，可以使用LangSmith CLI和LangSmith Skills，让编码智能体能够访问这些轨迹。我们正是通过这种模式在终端基准测试上改进了Deep Agents（我们开源的、模型无关的通用基础框架）。

如果你想随时间学习上下文（无论是在智能体、用户还是组织层面）——那么你的智能体框架需要支持这一点。Deep Agents——我们选择的框架——以生产就绪的方式支持这一点。请参阅其文档，了解如何实现用户级记忆、后台学习等的示例。

### 加入我们的通讯

来自LangChain团队和社区的更新

正在处理您的申请...

成功！请检查您的收件箱并点击链接确认订阅。

抱歉，出错了。请重试。

---

> 本文由AI自动翻译，原文链接：[Continual learning for AI agents](https://blog.langchain.com/continual-learning-for-ai-agents/)
> 
> 翻译时间：2026-04-06 04:44
