---
title: Swift Transformers 发布1.0版本，聚焦MLX与智能体未来
title_original: Swift Transformers Reaches 1.0 – and Looks to the Future
date: '2025-09-26'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/swift-transformers
author: ''
summary: Swift Transformers 库正式发布1.0稳定版本，旨在为Apple开发者提供在本地设备上集成大型语言模型的工具支持。该版本将Tokenizers和Hub模块提升为顶级模块，并与Swift
  Jinja库深度整合以优化聊天模板性能。项目未来将重点投入MLX框架和智能体用例的开发，同时已获mlx-swift-examples、WhisperKit等知名项目采用，为Apple生态的本地AI应用奠定基础。
categories:
- AI基础设施
tags:
- Swift
- 本地AI推理
- Apple生态
- 开源库
- MLX
draft: false
translated_at: '2026-02-04T04:15:09.960307'
---

# Swift Transformers 发布 1.0 版本——展望未来

两年前（！）我们发布了 `swift-transformers`，目标是支持 Apple 开发者，帮助他们将本地 LLM 集成到自己的应用中。自那时起，很多事情都发生了变化（当时 MLX 和聊天模板还不存在！），我们也了解了社区实际上是如何使用这个库的。

我们希望加倍投入那些能为社区带来最大效益的用例，并为未来奠定基础。剧透预警：在此版本发布后，我们将重点关注 MLX 和智能体用例 🚀

## 什么是 `swift-transformers`

`swift-transformers` 是一个 Swift 库，旨在**减少**希望在 Apple Silicon 平台（包括 iPhone）上使用本地模型的开发者的**摩擦**。它包含了 Core ML 或 MLX 单独未提供、但进行本地推理所必需的缺失部分。具体来说，它提供了以下组件：

*   **分词器**。为语言模型准备输入出人意料地复杂。我们在 `tokenizers` Python 和 Rust 库方面积累了丰富的经验，这些库是 AI 生态系统的基石。我们希望将同样高性能、符合人体工程学的体验带到 Swift 中。Swift 版本的 `Tokenizers` 应该能为您处理一切，包括聊天模板和智能体用例！
*   **Hub**。这是 **Hugging Face Hub** 的接口，所有开源模型都可以在这里找到。它允许您从 Hub 下载模型并在本地缓存，支持后台可恢复下载、模型更新、离线模式。它包含了 Python 和 JavaScript 库提供的功能子集，专注于 Apple 开发者最需要的任务（例如，不支持上传）。
*   **模型**和**生成**。这些是针对转换为 Core ML 格式的 LLM 的封装器。转换它们超出了本库的范围（但我们有一些指南）。一旦转换完成，这些模块可以轻松地使用它们进行推理。

![来自 mlx-swift-examples 的测试应用，显示 SmolVLM2 正在解释视频中的动作。](/images/posts/00f1dbd85f71.jpg)

## 社区如何使用它

大多数时候，人们使用 `Tokenizers` 或 `Hub` 模块，并且经常两者都用。依赖 `swift-transformers` 的一些著名项目包括：

*   **mlx-swift-examples**，由 Apple 发布。实际上，它不仅仅是一个示例集合，更是一个库的列表，您可以使用这些库通过 MLX 运行各种类型的模型，包括 LLM 和 VLM（视觉语言模型）。它有点像我们的 `Models` 和 `Generation` 库，但针对的是 MLX 而非 Core ML——并且它支持更多模型类型，如嵌入器或 Stable Diffusion。
*   **WhisperKit**，由 `argmax` 发布。开源 ASR（语音识别）框架，针对 Apple Silicon 进行了深度优化。它依赖于我们的 `Hub` 和 `Tokenizers` 模块。
*   **FastVLM**，由 Apple 发布，以及许多其他应用演示，例如我们自己的 **SmolVLM2 原生应用**。

## v1.0 带来了哪些变化

1.0 版本标志着包的稳定性。开发者们正在基于 swift-transformers 构建应用，这第一个主要版本认可了这些用例，并使版本号与这一现实保持一致。它也为与社区合作迭代、构建下一组功能奠定了基础。以下是我们偏爱的部分更新：

*   `Tokenizers` 和 `Hub` 现在成为**一等、顶级模块**。在 1.0 之前，您必须依赖并导入整个包，而现在您可以只选择 `Tokenizers`，例如。
*   说到 Jinja，我们非常自豪地宣布，我们与 **John Mai** (X) 合作，创建了**他优秀的 Swift Jinja 库的下一个版本**。
    John 的工作对社区至关重要：他独自承担了提供一个坚实的聊天模板库的任务，该库能够随着模板变得越来越复杂而发展。新版本的速度快了几个数量级（不开玩笑），并且**作为 `swift-jinja` 存在于此**。
*   为了进一步减少下游用户的负担，我们**移除了示例 CLI 目标和 `swift-argument-parser` 依赖**，这反过来又避免了已使用它的项目出现版本冲突。
*   感谢 Apple 的贡献，我们**采用了现代 Core ML API**，支持有状态模型（便于 KV 缓存）和富有表现力的 `MLTensor` API——这移除了数千行自定义张量操作和数学代码。
*   移除了大量**额外的冗余代码**并**减少了 API 表面积**，以降低认知负荷并加快迭代速度。
*   **测试**变得**更好、更快、更强**。
*   为**公共 API** 添加了**文档注释**。
*   完全支持 **Swift 6**。

1.0 版本带来了破坏性的 API 变更。但是，如果您是 `Tokenizers` 或 `Hub` 的用户，我们预计不会出现重大问题。如果您使用该库的 Core ML 组件，请**联系我们**，以便我们在过渡期间为您提供支持。我们将准备一份迁移指南并添加到文档中。

## 使用示例

以下是如何使用 `Tokenizers` 为 LLM 格式化工具调用输入：

```swift
import Tokenizers

let tokenizer = try await AutoTokenizer.from(pretrained: "mlx-community/Qwen2.5-7B-Instruct-4bit")

let weatherTool = [
    "type": "function",
    "function": [
        "name": "get_current_weather",
        "description": "Get the current weather in a given location",
        "parameters": [
            "type": "object",
            "properties": ["location": ["type": "string", "description": "City and state"]],
            "required": ["location"]
        ]
    ]
]

let tokens = try tokenizer.applyChatTemplate(
    messages: [["role": "user", "content": "What's the weather in Paris?"]],
    tools: [weatherTool]
)

```

更多示例，请查看 README 中的**此部分**和 **Examples 文件夹**。

## 未来展望

老实说，我们不知道。但我们确实知道，我们对探索 MLX 非常感兴趣，因为这通常是开发者在原生应用中开始接触 ML 时当前首选的方法，我们希望能帮助实现尽可能无缝的体验。我们正在考虑如何更好地与 `mlx-swift-examples` 集成以支持 LLM 和 VLM，可能通过开发者经常遇到的预处理和后处理操作。

我们也对智能体用例，特别是 MCP 感到非常兴奋。我们认为，将系统资源暴露给本地工作流将会是 🚀

如果您想跟随我们一起踏上这段旅程，或者想分享您的想法，请通过我们的社交媒体或**代码仓库**联系我们。

## 没有你们，我们无法做到这一切 🫵

我们衷心感谢所有库的贡献者和用户，感谢你们的帮助和反馈。我们爱你们所有人，并迫不及待地想继续与你们合作，共同塑造设备端生成的未来！ ❤️

---

> 本文由AI自动翻译，原文链接：[Swift Transformers Reaches 1.0 – and Looks to the Future](https://huggingface.co/blog/swift-transformers)
> 
> 翻译时间：2026-02-04 04:15
