---
title: 开放平台与统一管道：为何dbt在Databricks上加速发展
title_original: 'Open Platform, Unified Pipelines: Why dbt on Databricks is Accelerating'
date: '2026-04-16'
source: Databricks Blog
source_url: https://www.databricks.com/blog/open-platform-unified-pipelines-why-dbt-databricks-accelerating
author: ''
summary: 本文阐述了将dbt工作流整合到Databricks平台上的优势。文章指出，dbt的强大功能高度依赖于其运行的数据平台。Databricks通过提供开放基础、无缝编排、内置治理和强大性价比四大支柱，解决了传统数据技术栈中数据重复、权限不一致和可观测性碎片化等问题。在Databricks上运行dbt，用户可以在湖仓一体架构上获得开放性、治理、性能和操作简便性，实现零供应商锁定、端到端管道编排以及默认的治理集成，从而加速数据转换工作流。
categories:
- AI基础设施
tags:
- dbt
- Databricks
- 数据工程
- 数据治理
- 湖仓一体
draft: false
translated_at: '2026-04-17T04:54:46.385710'
---

dbt为数据转换工作流带来了结构化。团队使用它将原始数据转化为经过治理的数据集，为下游消费场景提供支持，例如BI仪表板、AI/ML模型和跨部门报告。

但现实是：dbt的强大程度取决于它所运行的数据平台。

大多数数据技术栈迫使你在多个系统间拼凑存储、计算、治理、编排和监控功能。结果如何？数据重复、权限不一致、可观测性碎片化，性能调优变成了兼职工作。这就是为什么越来越多的团队正在将他们的dbt工作流整合到Databricks上。

要有效运行dbt，一个平台需要具备四个要素：

1.  **开放基础**：确保你的dbt工作流不会被锁定在专有技术栈中
2.  **无缝编排**：在一个地方端到端地运行dbt管道
3.  **内置治理**：作为默认dbt工作流的一部分
4.  **强大的性价比**：让dbt从第一天起就能快速运行，无需手动调优

Databricks在一个平台中原生集成了所有这四个支柱。当你在Databricks上运行dbt时，你将在湖仓一体架构上获得dbt开发体验，该架构从第一天起就为开放性、治理、性能和操作简便性而设计。让我们看看这些在实践中是如何运作的：

![在Databricks湖仓一体上运行dbt不再碎片化](/images/posts/a55f602ac02a.png)

## 在开放基础上运行dbt，实现零供应商锁定

供应商锁定是组织数据战略最重大的战略风险之一。dbt采用开放的适配器框架构建，这意味着你的转换逻辑不会被锁定在任何单一平台上。dbt在设计上是开放的，而Databricks提供了一个开放的平台来运行它。许多现代数据技术栈围绕专有存储层构建，这带来了短期便利，但引入了长期摩擦。随着时间的推移，这会导致为服务不同消费者而重复的数据和导出管道、限制互操作性的存储格式，以及随着平台需求演变而不断增加的切换成本。

Databricks是一个开放的湖仓一体平台：一个统一的平台，你的数据以开放表格式存储，并通过开放接口访问，确保存储和治理不绑定到单一查询引擎。在Databricks上，dbt模型成为开放格式（Delta Lake和Apache Iceberg）的表，确保你转换后的数据无需导出或维护并行副本，就能在整个数据生态中被访问。这种开放性对dbt工作流尤其重要。你精心建模的中间层和核心层表成为可复用的数据产品，下游用户可以通过任何查询引擎消费，而不仅仅是通过运行dbt的平台。

这种开放性超越了存储。Unity Catalog围绕开放的目录和访问标准构建，支持外部引擎进行受治理的读写。Databricks SQL遵循ANSI标准，确保你的查询在不同平台间保持可移植性，减少针对特定供应商的重写。这意味着你的dbt工作流运行在一个为可移植性而非锁定而设计的技术栈上。

## 使用Lakeflow Jobs端到端编排dbt管道

编排是操作复杂性累积的地方。将dbt与外部编排器搭配Databricks使用，意味着需要操作两个系统、在两个地方调试，以及它们之间脆弱的交接。

Lakeflow Jobs通过将**dbt作为统一管道中的一等任务类型**来消除这种复杂性。团队无需维护单独的编排层，而是在单一工作流中，让dbt与上游数据摄取和下游操作一起运行。例如，你可以使用Auto Loader摄取原始数据，用dbt模型转换数据，然后触发仪表板刷新或ML模型重新训练，所有这些都在一个具有统一重试逻辑和依赖管理的管道中完成。在Databricks上运行dbt还支持通过**流式表**直接进行数据摄取。对于dbt Platform用户，**dbt Platform任务**（测试版）使Lakeflow能够触发和管理在dbt Platform中运行的dbt工作流。

![dbt编排](/images/posts/96d128fd08f8.png)

当dbt通过Lakeflow编排时，故障、重试和上下文信息在一个地方可见。你无需在单独的dbt编排器和Databricks日志之间切换，而是可以直接在同一个作业运行视图中看到故障、受影响的下游任务以及受影响的仪表板。

## 让治理成为默认dbt工作流的一部分

随着dbt工作流的扩展，治理成为瓶颈。团队需要关于表内容、所有权和访问权限的清晰答案。在传统技术栈中，这些上下文信息分散在独立的目录工具、权限系统以及不连接端到端的不完整血缘视图中。

Databricks通过**Unity Catalog**解决了这个问题，它为你整个湖仓一体统一了访问控制、数据发现和血缘关系——不仅限于dbt内部，还涵盖数据摄取、BI、ML/AI及其他领域。使用Unity Catalog，你无需在dbt每次重建表时重新运行授权语句。权限在模式级别进行管理，并在表重建过程中持续存在。**行级过滤器**、**列掩码**和**基于属性的访问控制**等细粒度控制，在dbt、BI工具和笔记本中保持一致应用。

例如，当你使用**dbt的persist_docs功能**将dbt文档持久化到Unity Catalog时，在dbt中编写的列描述和上下文信息，在数据被查询和消费的地方变得可发现。Unity Catalog提供列级**数据血缘**，追踪数据从原始摄取、经过dbt转换到下游使用的流程。当源模式发生变化时，你可以立即看到哪些dbt模型和下游资产会受到影响。当数据管道跨越互不连接的系统时，这种级别的可见性是无法实现的。

成本治理与数据治理同样重要。通过**查询标签**，你可以将业务上下文附加到dbt运行中，并通过**系统表**按团队、项目或环境跟踪支出。团队终于可以用真实数据而非估算来回答“我们的营销分析dbt管道成本是多少？”这个问题。此外，**DBSQL细粒度成本监控**（私有预览版）也提供了跨所有dbt工作负载的聚合成本监控。

## 从第一天起就以强大的性价比运行dbt

为性能优化数据仓库通常需要持续的手动工作。团队常常不得不在开发速度和性能维护之间做出权衡。

Databricks通过将高性能执行引擎与原生支持dbt的特性相结合，**在不增加手动开销的情况下提供速度提升**，从而抽象了这种复杂性。

#### 内置性能

*   **Photon引擎**通过向量化执行加速SQL工作负载，与云数据仓库相比，提供高达**12倍的性价比提升**。无服务器SQL仓库默认包含Photon，因此团队无需额外成本即可获得加速性能。
*   **预测性优化**利用AI监控表并自动化维护，实现高达**20倍的查询加速**。这减少了对dbt工程师历来依赖的手动OPTIMIZE后置钩子的需求。

#### 通过dbt配置解锁的性能特性

*   dbt与**Liquid Clustering**的集成，用灵活的方法取代了僵化的分区策略，该方法随着数据量的增长动态调整，无需手动调优即可实现高达**10倍的速度提升**。
*   dbt中的**物化视图**，由开源Spark声明式管道提供支持，自动处理增量处理。Databricks管理确定需要更新内容的复杂性，并且只处理新的或修改过的记录，而不是重新计算整个数据集。与低效的定时批量刷新相比，这降低了计算成本。

凭借这些特性，用户无需耗费大量时间进行调优，便能构建出随数据集增长仍保持高性能的流水线。仅在2025年，Databricks SQL就在ETL工作负载（涉及写入的查询）上实现了**10%的性能提升**，且无需任何额外配置。

## 立即开始使用

Databricks为dbt工作流集开放存储、统一治理、卓越性价比与一体化运维于一体。现已加入2900多家在Databricks上运行dbt的客户行列。请按照[快速入门指南](https://docs.databricks.com/en/dbt/get-started.html)立即开启体验。

---

> 本文由AI自动翻译，原文链接：[Open Platform, Unified Pipelines: Why dbt on Databricks is Accelerating](https://www.databricks.com/blog/open-platform-unified-pipelines-why-dbt-databricks-accelerating)
> 
> 翻译时间：2026-04-17 04:54
