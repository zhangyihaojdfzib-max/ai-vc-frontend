---
title: Fluid计算：为AI工作负载演进无服务器架构
title_original: 'Fluid compute: Evolving serverless for AI workloads'
date: '2025-05-30'
source: Vercel Blog
source_url: https://vercel.com/blog/fluid-compute-evolving-serverless-for-ai-workloads
author: ''
summary: 文章指出传统无服务器计算为快速、无状态的网络事务设计，与LLM交互所需的持续计算和连续执行模式不匹配。为此，作者提出Fluid计算模型，通过优先重用现有资源、在函数内部动态扩展而非生成新实例，减少冷启动和资源闲置。该模型使单个实例能同时处理多个AI推理请求，降低开销并提升效率，同时策略性部署资源以优化性能。Fluid计算旨在为AI工作负载提供更流畅、成本高效的执行环境。
categories:
- AI基础设施
tags:
- 无服务器计算
- AI工作负载
- Fluid计算
- LLM交互
- 资源优化
draft: false
translated_at: '2026-05-07T05:32:48.047049'
---

人工智能的快速发展正在重塑科技行业和应用开发。传统的无服务器计算是为快速、无状态的网络应用事务而设计的。而LLM（大语言模型）交互则需要不同的持续计算和连续执行模式。

这种设计上的不匹配，为针对AI工作负载量身定制的新型计算模型创造了机会。

### Link to headingLLM（大语言模型）交互：一个序列，而非单个请求

与LLM（大语言模型）交互不仅仅是发送请求和接收响应。与大多数请求在毫秒内处理完成的传统网络应用不同，LLM工作负载涉及较长的执行时间和空闲期。

每次交互都遵循一个多步骤流程：

- 无服务器函数被调用：函数初始化并准备处理LLM请求
- 函数向LLM API发送请求：在此阶段，函数正在积极消耗计算资源
- 函数进入空闲状态等待响应：这可能需要几秒到几分钟，但函数仍在运行，尽管没有执行任何有用工作，却仍在累积计费时间
- LLM完成处理并将响应返回给函数：等待中的函数（如果仍处于活动状态）接收响应
- 响应被流式传输回客户端：如果函数在等待期间超时，则必须启动一个新实例来处理响应
- 后续请求重复该循环：每次新的交互通常会生成一个新的函数实例，即使现有实例可能仍然可用但未被充分利用

无服务器函数被调用：函数初始化并准备处理LLM请求

函数向LLM API发送请求：在此阶段，函数正在积极消耗计算资源

函数进入空闲状态等待响应：这可能需要几秒到几分钟，但函数仍在运行，尽管没有执行任何有用工作，却仍在累积计费时间

LLM完成处理并将响应返回给函数：等待中的函数（如果仍处于活动状态）接收响应

响应被流式传输回客户端：如果函数在等待期间超时，则必须启动一个新实例来处理响应

后续请求重复该循环：每次新的交互通常会生成一个新的函数实例，即使现有实例可能仍然可用但未被充分利用

这个序列揭示了LLM交互与传统无服务器计算在架构假设上的差异：

- 事务时序：传统无服务器计算是为毫秒级响应设计的，而非持续数秒的LLM处理时间
- 执行模式：函数是为单个、孤立的请求构建的，而非连续的LLM对话
- 资源生命周期：传统模型假设快速完成，而非持续交互
- 扩展模型：水平扩展针对短暂的突发活动进行了优化，而非持续的AI工作负载

事务时序：传统无服务器计算是为毫秒级响应设计的，而非持续数秒的LLM处理时间

执行模式：函数是为单个、孤立的请求构建的，而非连续的LLM对话

资源生命周期：传统模型假设快速完成，而非持续交互

扩展模型：水平扩展针对短暂的突发活动进行了优化，而非持续的AI工作负载

传统的无服务器计算在其预期的用例（快速、无状态的网络事务）中表现出色。而LLM交互则代表了一种不同的计算范式，需要专门构建的基础设施。

## Link to heading面向AI工作负载的Fluid计算

Fluid是我们新的计算模型，通过在扩展新资源之前优先利用现有资源来减少总计算消耗。它不是为每个请求启动一个新函数，而是智能地重用可用容量，确保计算资源不会闲置。这意味着：

- 在生成新资源之前优先使用现有资源：Fluid首先在单个实例内部进行扩展（多对一），避免了冷启动和增加成本的额外资源生成
- 扩展发生在函数内部，而不仅仅是在实例之间：计算资源被动态共享，而不是生成不必要的新函数
- AI对话保持流畅且成本高效：用户获得不间断的响应，同时开发者减少基础设施的低效

在生成新资源之前优先使用现有资源：Fluid首先在单个实例内部进行扩展（多对一），避免了冷启动和增加成本的额外资源生成

扩展发生在函数内部，而不仅仅是在实例之间：计算资源被动态共享，而不是生成不必要的新函数

AI对话保持流畅且成本高效：用户获得不间断的响应，同时开发者减少基础设施的低效

单个实例现在可以同时处理多个AI推理请求，从而减少开销并实现更高效的扩展。通过动态地将计算资源重新分配到需要的地方，Fluid确保您的AI工作负载以峰值性能运行，并且每个资源都积极参与处理。

![Fluid计算相比传统无服务器计算实现了更高效的计算资源使用](/images/posts/27b7112fa512.jpg)

![Fluid计算相比传统无服务器计算实现了更高效的计算资源使用](/images/posts/b9ece2ae3d76.jpg)

![Fluid计算相比传统无服务器计算实现了更高效的计算资源使用](/images/posts/122b5e1693d2.jpg)

![Fluid计算相比传统无服务器计算实现了更高效的计算资源使用](/images/posts/6203e63f2097.jpg)

此外，Fluid策略性地将资源部署在靠近您数据的区域，以优化性能和一致性，同时实现资源密集型AI工作负载的高效执行。

借助Fluid计算，LLM工作负载不再需要在可扩展性和效率之间做出选择；两者可以兼得。AI应用保持安全且响应迅速，成本保持可预测，并且每次函数调用都积极参与处理，而不仅仅是等待。Fluid计算确保每一秒的函数时间都得到充分利用，而不是为闲置的计算资源付费。

了解Fluid计算如何在Vercel上工作

Fluid计算通过在LLM调用期间重用空闲计算资源来提升无服务器性能，保持函数活跃并动态扩展。

了解更多

## Link to heading默认安全

除了优化之外，Fluid计算还建立在安全和可靠的基础之上，以满足现代AI工作负载的需求。

- 通过Vercel防火墙实现边缘安全：请求在到达Vercel Functions路由器之前，会被路由到最近的Vercel存在点（PoP）并由Vercel防火墙进行检查。这种第7层防护可阻止大多数应用层威胁，包括DoS攻击，并过滤可疑流量，确保只有合法请求到达您的应用
- 安全的实例架构：Fluid计算实例从不直接暴露于互联网。相反，它们通过安全的持久TCP隧道连接到Vercel Functions路由器。所有通信，包括函数调用和响应，都流经这个受控通道，确保了强大的隔离性和精确的工作负载管理
- 增强的可靠性和可用性：Fluid在一个区域内跨多个可用区自动进行故障转移。对于企业客户，Fluid还具备多区域故障转移能力。这些安全措施有助于确保正常运行时间，并使您的应用在面对局部中断时保持弹性

通过Vercel防火墙实现边缘安全：请求在到达Vercel Functions路由器之前，会被路由到最近的Vercel存在点（PoP）并由Vercel防火墙进行检查。这种第7层防护可阻止大多数应用层威胁，包括DoS攻击，并过滤可疑流量，确保只有合法请求到达您的应用

安全实例架构：流体计算实例从不直接暴露于互联网。相反，它们通过安全、持久的TCP隧道连接到Vercel Functions路由器。所有通信（包括函数调用和响应）都通过这一受控通道进行，确保强隔离性和精确的工作负载管理。

增强的可靠性和可用性：Fluid会在区域内跨多个可用区自动进行故障转移。对于企业客户，Fluid还具备跨区域故障转移能力。这些保障措施有助于确保正常运行时间，并使您的应用在面对局部故障时保持弹性。

这种分层安全模型确保Fluid在提供性能和效率的同时，不会牺牲敏感AI工作负载所需的保护。

## 链接到标题开始使用AI构建

AI工作负载不仅需要规模，还需要效率和安全性。Fluid计算确保资源充分利用，最终降低成本，同时提供安全、高性能的执行。

这正是AI团队一直在寻找的基础设施：快速、自适应，并专为支持实时推理和后台任务而构建。Fluid计算现已作为新项目的默认选项，并可为现有项目启用。

立即使用Fluid计算部署AI驱动应用

通过Fluid计算优化您的AI应用，最大化您的计算能力。

开始使用

---

> 本文由AI自动翻译，原文链接：[Fluid compute: Evolving serverless for AI workloads](https://vercel.com/blog/fluid-compute-evolving-serverless-for-ai-workloads)
> 
> 翻译时间：2026-05-07 05:32
