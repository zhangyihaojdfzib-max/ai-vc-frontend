---
title: Nemotron ColEmbed V2：多模态检索新标杆，ViDoRe V3榜首模型
title_original: 'Nemotron ColEmbed V2: Raising the Bar for Multimodal Retrieval with
  ViDoRe V3’s Top Model'
date: '2026-02-04'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/nvidia/nemotron-colembed-v2
author: ''
summary: NVIDIA推出Nemotron ColEmbed V2系列多模态嵌入模型，包含3B、4B和8B三种规模，采用延迟交互式架构，在ViDoRe V3视觉文档检索基准测试中取得领先性能。该模型扩展了ColBERT的多向量交互机制至多模态场景，支持文本、图像和结构化视觉元素的细粒度语义匹配，适用于高精度多媒体检索、跨模态搜索及多模态RAG系统，为企业级视觉文档检索树立了新的准确性标准。
categories:
- AI研究
tags:
- 多模态检索
- 视觉文档检索
- 嵌入模型
- NVIDIA
- 延迟交互
draft: false
translated_at: '2026-02-05T04:14:21.810256'
---

# Nemotron ColEmbed V2：凭借ViDoRe V3榜首模型，为多模态检索树立新标杆

现代搜索系统越来越多地被设计用于处理可能包含文本、表格、图表、图形和其他视觉组件的异构文档图像。在此背景下，准确检索这些不同模态中的相关信息成为一个核心挑战。基于基础视觉-语言模型构建的多模态嵌入模型，将不同类型的内容映射到一个共享的表示空间中，从而实现对文本、图像和结构化视觉元素的统一检索。尽管将整个查询和候选文档编码为单个向量是一种常见做法——我们最近发布的、优先考虑效率和低存储的商业就绪型模型 Llama-Nemotron-Embed-VL-1B 就是例证——但关于多向量、延迟交互式嵌入架构的研究方向正日益增多，这些架构能在查询和文档之间提供细粒度的多向量交互。通过实现更丰富的 Token 表示，这些模型能更好地捕捉更详细的语义关系，并在各种（多模态）基准测试中展现出更高的准确性。

NVIDIA 推出了 Nemotron ColEmbed V2 系列，这是一组延迟交互式嵌入模型，提供 3B、4B 和 8B 三种规模，专为高精度多模态检索而设计。这些模型采用统一的方法进行文本-图像检索，并在 ViDoRe V1、V2 和 V3 基准测试中实现了最先进的性能。

## Nemotron ColEmbed V2 亮点（摘要）

`nemotron-colembed-vl-8b-v2`、`nemotron-colembed-vl-4b-v2` 和 `llama-nemotron-colembed-vl-3b-v2` 是最先进的延迟交互嵌入模型，截至 2026 年 2 月 3 日，在 ViDoRe V3 基准测试（一个针对企业用例的视觉文档检索综合评估基准）中，分别排名第 1、第 3 和第 6 位——是各自权重类别中排名最高的模型。

![late_interaction](/images/posts/fe087884f2b8.png)

由 ColBERT 为多向量嵌入匹配引入的延迟交互机制，在我们的工作中被扩展到了多模态场景，使得查询和文档 Token（无论是文本还是视觉）之间能够进行细粒度的交互。如图所示，每个查询 Token 嵌入通过 `MaxSim` 操作符与所有文档 Token 嵌入进行交互，该操作符为每个查询 Token 选择最大相似度，然后对这些最大值求和以产生最终的相关性分数。这种方法需要存储整个文档语料库（无论是文本还是视觉）的 Token 嵌入，从而增加了存储需求。在推理过程中，计算查询 Token 嵌入，并使用相同的 MaxSim 操作与存储的文档嵌入进行匹配。

Nemotron ColEmbed V2 系列模型面向那些探索视觉文档检索应用、且对准确性要求极高的研究人员。这使其有别于我们上个月发布的、专为需要最小存储和高吞吐量的商业环境而设计的 1B 单向量模型。它在多模态 RAG 系统中非常有用，在这些系统中，文本查询可用于检索文档图像，例如页面、文本、图表、表格或信息图。这些模型为输入查询和文档输出多向量嵌入。潜在应用包括多媒体搜索引擎、跨模态检索系统以及具有丰富输入理解能力的对话式 AI。

作为一个新的基准，ViDoRe V3 旨在为多模态企业文档检索设定行业标准。它解决了生产 RAG 系统中的一个关键挑战：从复杂、视觉丰富的文档中准确提取信息。凭借其强大的多模态文档检索能力，`nemotron-colembed-vl-8b-v2` 模型在 ViDoRe V3 排行榜上排名第一，为准确性树立了新标准。

视觉文档检索基准（页面检索）—— ViDoRe V3 公共和私有任务的平均 NDCG@10。

## 模型架构

`llama-nemotron-colembed-vl-3b-v2` 是一个基于 Transformer 的多模态嵌入模型，构建在基于 `google/siglip2-giant-opt-patch16-384` 和 `meta-llama/Llama-3.2-3B` 的 VLM 之上。`nemotron-colembed-vl-8b-v2` 和 `nemotron-colembed-vl-4b-v2` 多模态编码器模型分别基于 `Qwen3-VL-8B-Instruct` 和 `Qwen3-VL-4B-Instruct` 构建。

### 架构修改：

- 我们的模型使用双向自注意力机制，而不是原始 LLM 解码器模型中的单向因果自注意力机制。这使得模型能够从整个输入序列中学习丰富的表示。
- ColBERT 风格的延迟交互机制——对于每个输入 Token，每个模型输出一个 n 维的浮点值嵌入向量，其中 n 由模型的隐藏层大小决定。

### 训练方法

`nemotron-colembed-vl-8b-v2`、`nemotron-colembed-vl-4b-v2` 和 `llama-nemotron-colembed-vl-3b-v2` 模型均使用双编码器架构独立训练。这涉及使用嵌入模型独立编码一对句子（例如，一个查询和一个文档）。通过对比学习，用于最大化查询与包含答案的文档之间的延迟交互相似度，同时最小化查询与采样的、对回答问题无用的负样本文档之间的相似度。

`llama-nemotron-colembed-vl-3b-v2` 模型采用两阶段流程进行训练：首先使用 1250 万文本问答对进行微调，随后使用文本-图像对进行微调。`nemotron-colembed-vl-8b-v2` 和 `nemotron-colembed-vl-4b-v2` 模型仅使用文本-图像对（第二阶段）进行微调。

我们的训练数据集包含纯文本对和文本-图像对，并且我们应用了 NV-Retriever 论文中提出的正样本感知难负样本挖掘方法来提高检索性能。

✨ 相较于 V1 的主要改进：

⚗️ 先进的模型合并：利用训练后模型合并技术，融合多个微调检查点的优势。这在不增加任何额外推理延迟的情况下，提供了集成模型的准确性稳定性。

🌍 增强的合成数据：我们通过多样化的多语言合成数据显著丰富了训练混合数据，改善了跨语言和复杂文档类型的语义对齐。

![modelperfs_vidorev3](/images/posts/32580ec98830.png)

## 开始使用 Nemotron ColEmbed V2

Nemotron ColEmbed V2 模型标志着高精度文本-图像检索向前迈出了重要一步，在 ViDoRe V1、V2 和 V3 基准测试中提供了最先进的结果。3B、4B 和 8B 模型变体的可用性，进一步为多模态检索应用的未来研究和高级实验奠定了坚实的基础。

通过下载模型开始使用 Nemotron ColEmbed V2 模型：`nemotron-colembed-vl-8b-v2`、`nemotron-colembed-vl-4b-v2` 和 `llama-nemotron-colembed-vl-3b-v2`，这些模型可在 Hugging Face 上获取。在 产品页面 上了解更多关于 NVIDIA NeMo Retriever 系列 Nemotron RAG 模型的信息，或从 NVIDIA NGC 获取微服务容器。这是在您自己的应用和工作流程中探索最先进检索技术的绝佳机会。

试用 NVIDIA Enterprise RAG Blueprint，它使用了 Nemotron RAG 模型，这些模型由我们赢得 ViDoRe V3 背后的相同技术驱动。

---

> 本文由AI自动翻译，原文链接：[Nemotron ColEmbed V2: Raising the Bar for Multimodal Retrieval with ViDoRe V3’s Top Model](https://huggingface.co/blog/nvidia/nemotron-colembed-v2)
> 
> 翻译时间：2026-02-05 04:14
