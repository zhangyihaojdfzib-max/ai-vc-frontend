---
title: 顶点着色网格转纹理网格教程
title_original: Converting Vertex-Colored Meshes to Textured Meshes
date: '2024-09-30'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/vertex-colored-to-textured-mesh
author: ''
summary: 本文介绍了将顶点着色网格转换为UV映射纹理网格的方法，包括快速版和详细版。快速版使用InstantTexture库一键转换；详细版则逐步讲解依赖安装、UV贴图生成、重心插值填充纹理等核心步骤。该技术适用于将生成式3D模型（如InstantMesh输出）转换为更通用的纹理网格格式，便于后续应用。
categories:
- 技术趋势
tags:
- 3D网格转换
- UV贴图
- 纹理烘焙
- 顶点着色
- InstantTexture
draft: false
translated_at: '2026-06-25T06:06:55.683708'
---

# 将顶点着色网格转换为纹理网格

将顶点着色网格转换为UV映射的纹理网格。

## 简介

顶点颜色是一种直接将颜色信息添加到网格顶点的简单方法。这通常是像InstantMesh这样的生成式3D模型生成网格的方式。然而，大多数应用更偏好UV映射的纹理网格。

本教程将介绍一个快速解决方案，用于将顶点着色网格转换为UV映射的纹理网格。内容包括**快速版**以便快速获得结果，以及**详细版**进行深入讲解。

## 快速版

安装InstantTexture库以便轻松转换。这是我们编写的一个小型库，实现了下文**详细版**中描述的步骤。

```bash
pip install git+https://github.com/dylanebert/InstantTexture

```

### 使用方法

以下代码将一个顶点着色的`.obj`网格转换为UV映射的纹理`.glb`网格，并保存为`output.glb`。

```python
from instant_texture import Converter

input_mesh_path = "https://raw.githubusercontent.com/dylanebert/InstantTexture/refs/heads/main/examples/chair.obj"

converter = Converter()
converter.convert(input_mesh_path)

```

让我们可视化输出网格。

```python
import trimesh

mesh = trimesh.load("output.glb")
mesh.show()

```

就是这样！

如需详细讲解，请继续阅读。

## 详细版

安装以下依赖项：

- `numpy`：用于数值运算
- `trimesh`：用于加载和保存网格数据
- `xatlas`：用于生成UV贴图
- `Pillow`：用于图像处理
- `opencv-python`：用于图像处理
- `httpx`：用于下载输入网格

```bash
pip install numpy trimesh xatlas opencv-python pillow httpx

```

导入依赖项。

```python
import cv2
import numpy as np
import trimesh
import xatlas
from PIL import Image, ImageFilter

```

加载顶点着色的输入网格。该文件应为位于`input_mesh_path`的`.obj`文件。

如果是本地文件，请使用`trimesh.load()`代替`trimesh.load_remote()`。

```python
mesh = trimesh.load_remote(input_mesh_path)
mesh.show()

```

访问网格的顶点颜色。

如果失败，请确保网格是包含顶点颜色的有效`.obj`文件。

```python
vertex_colors = mesh.visual.vertex_colors

```

使用xatlas生成UV贴图。

这是整个过程中最耗时的部分。

```python
vmapping, indices, uvs = xatlas.parametrize(mesh.vertices, mesh.faces)

```

将顶点和顶点颜色重新映射到UV贴图。

```python
vertices = mesh.vertices[vmapping]
vertex_colors = vertex_colors[vmapping]

mesh.vertices = vertices
mesh.faces = indices

```

定义所需的纹理尺寸。

构建一个按`upscale_factor`放大倍率的纹理缓冲区，以创建更高质量的纹理。

```python
texture_size = 1024

upscale_factor = 2
buffer_size = texture_size * upscale_factor

texture_buffer = np.zeros((buffer_size, buffer_size, 4), dtype=np.uint8)

```

使用重心插值填充UV映射网格的纹理。

1. 重心插值：计算由顶点v0、v1和v2定义的三角形内点p的插值颜色，对应颜色为c0、c1和c2。
2. 点内三角形测试：判断点p是否位于由顶点v0、v1和v2定义的三角形内。
3. 纹理填充循环：遍历网格的每个面。获取当前面的UV坐标（uv0、uv1、uv2）和颜色（c0、c1、c2）。将UV坐标转换为缓冲区坐标。确定三角形在纹理缓冲区上的边界框。对于边界框内的每个像素，使用点内三角形测试判断该像素是否位于三角形内。如果在内部，使用重心插值计算插值颜色。将颜色赋值给纹理缓冲区中对应的像素。

- 遍历网格的每个面。
- 获取当前面的UV坐标（uv0、uv1、uv2）和颜色（c0、c1、c2）。
- 将UV坐标转换为缓冲区坐标。
- 确定三角形在纹理缓冲区上的边界框。
- 对于边界框内的每个像素，使用点内三角形测试判断该像素是否位于三角形内。
- 如果在内部，使用重心插值计算插值颜色。
- 将颜色赋值给纹理缓冲区中对应的像素。

```python

def barycentric_interpolate(v0, v1, v2, c0, c1, c2, p):
    v0v1 = v1 - v0
    v0v2 = v2 - v0
    v0p = p - v0
    d00 = np.dot(v0v1, v0v1)
    d01 = np.dot(v0v1, v0v2)
    d11 = np.dot(v0v2, v0v2)
    d20 = np.dot(v0p, v0v1)
    d21 = np.dot(v0p, v0v2)
    denom = d00 * d11 - d01 * d01
    if abs(denom) < 1e-8:
        return (c0 + c1 + c2) / 3
    v = (d11 * d20 - d01 * d21) / denom
    w = (d00 * d21 - d01 * d20) / denom
    u = 1.0 - v - w
    u = np.clip(u, 0, 1)
    v = np.clip(v, 0, 1)
    w = np.clip(w, 0, 1)
    interpolate_color = u * c0 + v * c1 + w * c2
    return np.clip(interpolate_color, 0, 255)



def is_point_in_triangle(p, v0, v1, v2):
    def sign(p1, p2, p3):
        return (p1[0] - p3[0]) * (p2[1] - p3[1]) - (p2[0] - p3[0]) * (p1[1] - p3[1])

    d1 = sign(p, v0, v1)
    d2 = sign(p, v1, v2)
    d3 = sign(p, v2, v0)

    has_neg = (d1 < 0) or (d2 < 0) or (d3 < 0)
    has_pos = (d1 > 0) or (d2 > 0) or (d3 > 0)

    return not (has_neg and has_pos)


for face in mesh.faces:
    uv0, uv1, uv2 = uvs[face]
    c0, c1, c2 = vertex_colors[face]

    uv0 = (uv0 * (buffer_size - 1)).astype(int)
    uv1 = (uv1 * (buffer_size - 1)).astype(int)
    uv2 = (uv2 * (buffer_size - 1)).astype(int)

    min_x = max(int(np.floor(min(uv0[0], uv1[0], uv2[0]))), 0)
    max_x = min(int(np.ceil(max(uv0[0], uv1[0], uv2[0]))), buffer_size - 1)
    min_y = max(int(np.floor(min(uv0[1], uv1[1], uv2[1]))), 0)
    max_y = min(int(np.ceil(max(uv0[1], uv1[1], uv2[1]))), buffer_size - 1)

    for y in range(min_y, max_y + 1):
        for x in range(min_x, max_x + 1):
            p = np.array([x + 0.5, y + 0.5])
            if is_point_in_triangle(p, uv0, uv1, uv2):
                color = barycentric_interpolate(uv0, uv1, uv2, c0, c1, c2, p)
                texture_buffer[y, x] = np.clip(color, 0, 255).astype(
                    np.uint8
                )

```

让我们可视化当前纹理的效果。

```python
from IPython.display import display

image_texture = Image.fromarray(texture_buffer)
display(image_texture)

```

![带有空洞的纹理](/images/posts/9d0204238aab.png)

如我们所见，纹理有很多空洞。

为了修正这个问题，我们将结合4种技术：

1. 修复：使用周围像素的平均颜色填充空洞。
2. 中值滤波：通过用周围像素的中值颜色替换每个像素来去除噪声。
3. 高斯模糊：平滑纹理以去除任何残留噪声。
4. 降采样：使用LANCZOS重采样将尺寸缩小到`texture_size`。

```python

image_bgra = texture_buffer.copy()
mask = (image_bgra[:, :, 3] == 0).astype(np.uint8) * 255
image_bgr = cv2.cvtColor(image_bgra, cv2.COLOR_BGRA2BGR)
inpainted_bgr = cv2.inpaint(
    image_bgr, mask, inpaintRadius=3, flags=cv2.INPAINT_TELEA
)
inpainted_bgra = cv2.cvtColor(inpainted_bgr, cv2.COLOR_BGR2BGRA)
texture_buffer = inpainted_bgra[::-1]
image_texture = Image.fromarray(texture_buffer)


image_texture = image_texture.filter(ImageFilter.MedianFilter(size=3))


image_texture = image_texture.filter(ImageFilter.GaussianBlur(radius=1))


image_texture = image_texture.resize((texture_size, texture_size), Image.LANCZOS)


display(image_texture)

```

![无空洞的纹理](/images/posts/ae151eb04b7e.png)

如我们所见，纹理现在更加平滑且没有空洞。

这可以通过更高级的技术或手动纹理编辑进一步改进。

最后，我们可以使用生成的UV坐标和纹理构建一个新的网格。

```python
material = trimesh.visual.material.PBRMaterial(
    baseColorFactor=[1.0, 1.0, 1.0, 1.0],
    baseColorTexture=image_texture,
    metallicFactor=0.0,
    roughnessFactor=1.0,
)

```

```python
visuals = trimesh.visual.TextureVisuals(uv=uvs, material=material)
mesh.visual = visuals
mesh.show()
```

![最终网格](/images/posts/55d4101545c9.png)

瞧！网格已完成UV映射和纹理贴图。

在本地运行时导出，请调用`mesh.export("output.glb")`。

## 局限性

如您所见，网格仍存在许多微小瑕疵。

UV映射和纹理的质量也远未达到生产级网格的标准。

不过，如果您需要快速将顶点着色网格转换为UV映射网格，这种方法或许对您有用。

## 结论

本教程介绍了如何将顶点着色网格转换为UV映射并带有纹理的网格。

如有任何疑问或反馈，欢迎在GitHub或Space上提交Issue。

感谢阅读！

---

> 本文由AI自动翻译，原文链接：[Converting Vertex-Colored Meshes to Textured Meshes](https://huggingface.co/blog/vertex-colored-to-textured-mesh)
> 
> 翻译时间：2026-06-25 06:06
