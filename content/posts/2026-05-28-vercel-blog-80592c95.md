---
title: Amazon OpenSearch Serverless上线Vercel Marketplace
title_original: Amazon OpenSearch Serverless is now available in the Vercel Marketplace
  - Vercel
date: '2026-05-28'
source: Vercel Blog
source_url: https://vercel.com/changelog/amazon-opensearch-serverless-is-now-available-in-the-vercel-marketplace
author: ''
summary: Amazon OpenSearch Serverless现已集成至Vercel Marketplace，用户可直接在Vercel仪表板内创建和配置OpenSearch集合，环境变量自动注入项目，实现统一管理。该集成由下一代OpenSearch
  Serverless API支持，具备自动扩展速度提升20倍、可缩减至零、按用量付费等特性，专为AI Agent工作负载设计。文章还展示了通过Vercel OIDC认证的客户端代码示例，以及一个RAG检索增强生成路由的实现，帮助开发者快速构建智能搜索应用。
categories:
- AI基础设施
tags:
- Amazon OpenSearch
- Vercel
- Serverless
- RAG
- AI基础设施
draft: false
translated_at: '2026-05-29T06:13:19.529353'
---

Amazon OpenSearch Serverless 现已在 Vercel Marketplace 上线，Vercel 仪表板内置了引导式设置和自动项目配置功能。

## 为 Agent（智能体）基础设施而构建

通过将 Amazon OpenSearch Serverless 引入 Vercel Marketplace，团队可以受益于统一的工作流程：

- **仪表板内配置**：直接从 Vercel 仪表板创建 OpenSearch 集合
- **自动配置**：环境变量在配置时自动注入到您的项目中
- **统一管理**：在 Marketplace 资源视图中，与应用程序基础设施的其他部分一起查看和管理搜索资源，并可通过快速链接跳转到 AWS 进行更深层次的配置

仪表板内配置：直接从 Vercel 仪表板创建 OpenSearch 集合

自动配置：环境变量在配置时自动注入到您的项目中

统一管理：在 Marketplace 资源视图中，与应用程序基础设施的其他部分一起查看和管理搜索资源，并可通过快速链接跳转到 AWS 进行更深层次的配置

```
1import { createOpenSearch } from '@vercel/aws';2
3const os = createOpenSearch();
```

一个通过 Vercel OIDC 认证的 OpenSearch 客户端，无需静态 AWS 密钥。

```
1import { opensearch } from "@/lib/opensearch";2import { embed, generateText } from "ai";3
4export async function POST(req: Request) {5  const { question } = await req.json();6
7  8  const { embedding } = await embed({9    model: "openai/text-embedding-3-small",10    value: question,11  });12
13  14  const { body } = await opensearch.search({15    index: "docs",16    body: {17      size: 5,18      query: { knn: { embedding: { vector: embedding, k: 5 } } },19    },20  });21
22  const context = body.hits.hits23    .map((h: any) => h._source.content)24    .join("\n\n");25
26  27  const { text } = await generateText({28    model: "anthropic/claude-sonnet-4-5",29    prompt: `Answer using only this context:\n\n${context}\n\nQuestion: ${question}`,30  });31
32  return Response.json({ answer: text });33}
```

一个 RAG（检索增强生成）路由，它对问题进行嵌入（向量化），从 OpenSearch 中检索前 5 个最相似的文档，并基于这些文档给出答案。

此集成由下一代 Amazon OpenSearch Serverless API 提供支持，Vercel 在开发过程中作为关键设计合作伙伴与 AWS 合作。

- 在单个集合中统一支持向量、词汇、混合和 Agent（智能体）搜索
- 自动扩展速度提升 20 倍，专为 Agent（智能体）工作负载的突发性、不可预测的负载模式而构建
- 可缩减至零，无闲置成本
- 按实际用量付费而非峰值容量，最高可节省 60% 的成本

在单个集合中统一支持向量、词汇、混合和 Agent（智能体）搜索

自动扩展速度提升 20 倍，专为 Agent（智能体）工作负载的突发性、不可预测的负载模式而构建

可缩减至零，无闲置成本

按实际用量付费而非峰值容量，最高可节省 60% 的成本

## 开始使用

为帮助您快速上手，您可以直接通过 Vercel 创建新的 AWS 账户，并获得 **100 美元额度** 来试用 Amazon OpenSearch Serverless。如果您已有有效的 AWS 账户，您可以立即关联它，并直接从 Vercel 仪表板管理您的计划、查看使用详情。

准备好开始构建了吗？从 Vercel Marketplace 安装 Amazon OpenSearch，或部署一个入门模板来查看集成的实际效果。

---

> 本文由AI自动翻译，原文链接：[Amazon OpenSearch Serverless is now available in the Vercel Marketplace - Vercel](https://vercel.com/changelog/amazon-opensearch-serverless-is-now-available-in-the-vercel-marketplace)
> 
> 翻译时间：2026-05-29 06:13
