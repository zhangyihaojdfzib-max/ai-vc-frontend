---
title: 利用预编译技术大幅提升ZeroGPU Space性能
title_original: Make your ZeroGPU Spaces go brrr with ahead-of-time compilation
date: '2025-09-02'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/zerogpu-aoti
author: ''
summary: 本文介绍了如何通过PyTorch预编译技术优化Hugging Face ZeroGPU Space的性能。ZeroGPU采用按需分配GPU资源的方式，避免了闲置浪费，但初始运行时存在编译开销。文章详细阐述了通过预编译模型、FP8量化、动态形状优化等方法，在Flux、Wan等模型上实现1.3至1.8倍的加速效果，使演示应用响应更迅速。同时提供了具体实施步骤、注意事项以及实际演示案例，帮助用户充分利用Nvidia
  H200硬件潜力。
categories:
- AI基础设施
tags:
- ZeroGPU
- 预编译
- PyTorch
- GPU优化
- Hugging Face
draft: false
translated_at: '2026-02-14T04:13:12.501865'
---

# 通过预编译让你的 ZeroGPU Space 飞速运行

ZeroGPU 让任何人都能在 Hugging Face Space 中轻松启动强大的 Nvidia H200 硬件，而无需为闲置流量锁定 GPU。
它高效、灵活，非常适合演示，但有时并不能充分利用 GPU 和 CUDA 堆栈的全部潜力。
生成图像或视频可能需要大量时间。在这种情况下，能够榨取更多性能、充分利用 H200 硬件就显得尤为重要。

这就是 PyTorch 预编译的用武之地。与即时编译不同（这与 ZeroGPU 的短生命周期进程不太兼容），预编译让你可以一次性优化模型，然后即时重新加载。

结果就是：演示响应更迅速，体验更流畅，在 Flux、Wan 和 LTX 等模型上可获得 **1.3 倍至 1.8 倍** 的加速 🔥

在这篇文章中，我们将展示如何在 ZeroGPU Space 中设置预编译。我们将探索 FP8 量化、动态形状等高级技巧，并分享你可以立即试用的工作演示。如果你等不及了，我们邀请你查看 `zerogpu-aoti` 组织下一些由 ZeroGPU 驱动的演示。

**Pro 用户** 和 **Team / Enterprise** 组织成员可以创建 ZeroGPU Space，而任何人都可以自由使用它们（Pro、Team 和 Enterprise 用户可获得 **8 倍** 的 ZeroGPU 配额）。

-   ZeroGPU 是什么
-   PyTorch 编译
-   在 ZeroGPU 上进行预编译
-   注意事项
    -   量化
    -   动态形状
    -   多编译 / 共享权重
    -   FlashAttention-3
    -   区域编译
    -   使用 Hub 上的编译图
-   AoT 编译的 ZeroGPU Space 演示
-   结论
-   资源

## ZeroGPU 是什么

**Space** 是由 Hugging Face 提供支持的一个平台，允许机器学习从业者轻松发布演示应用。

Space 上典型的演示应用如下所示：

```python
import gradio as gr
from diffusers import DiffusionPipeline

pipe = DiffusionPipeline.from_pretrained(...).to('cuda')

def generate(prompt):
    return pipe(prompt).images

gr.Interface(generate, "text", "gallery").launch()

```

这工作得很好，但最终会在 Space 的整个生命周期内为其保留一个 GPU——即使它没有任何用户活动。

当执行这行代码的 `.to('cuda')` 时：

```python
pipe = DiffusionPipeline.from_pretrained(...).to('cuda')

```

PyTorch 会初始化 NVIDIA 驱动程序，这会将进程永久设置在 CUDA 上。考虑到应用流量并非完全平稳，而是极其稀疏和突发的，这种方式在资源利用上效率不高。

ZeroGPU 采用了一种即时 GPU 初始化的方法。它不会在主进程上设置 CUDA，而是自动 fork 进程，在 CUDA 上设置它，运行 GPU 任务，最后在需要释放 GPU 时终止该 fork。

这意味着：

-   当应用没有收到流量时，它不使用任何 GPU
-   当它实际执行任务时，它将使用一个 GPU
-   它可以根据需要同时使用多个 GPU 来执行任务

得益于 Python 的 `spaces` 包，要获得此行为，唯一需要的代码更改如下：

```diff
  import gradio as gr
+ import spaces
  from diffusers import DiffusionPipeline

  pipe = DiffusionPipeline.from_pretrained(...).to('cuda')

+ @spaces.GPU
  def generate(prompt):
      return pipe(prompt).images

  gr.Interface(generate, "text", "gallery").launch()

```

通过导入 `spaces` 并添加 `@spaces.GPU` 装饰器，我们：

-   拦截 PyTorch API 调用以推迟 CUDA 操作
-   使被装饰的函数在稍后调用时在 fork 中运行
-   （调用内部 API 以使正确的设备对 fork 可见，但这不在本文的讨论范围内）

ZeroGPU 目前分配的是 H200 的一个 **MIG 切片**（`3g.71gb` 配置）。包括完整切片（`7g.141gb` 配置）在内的其他 MIG 尺寸将于 2025 年底推出。

## PyTorch 编译

像 PyTorch 和 JAX 这样的现代机器学习框架具有**编译**的概念，可用于优化模型延迟或推理时间。在幕后，编译应用一系列（通常是硬件相关的）优化步骤，例如算子融合、常量折叠等。

PyTorch（从 2.0 版本开始）目前有两个主要的编译接口：

-   即时编译，使用 `torch.compile`
-   预编译，使用 `torch.export` + `AOTInductor`

`torch.compile` 在标准环境中效果很好：它在模型首次运行时进行编译，并在后续调用中重用优化后的版本。

然而，在 ZeroGPU 上，考虑到进程几乎是为每个 GPU 任务全新启动的，这意味着 `torch.compile` 无法有效地重用编译结果，因此被迫依赖其**文件系统缓存**来恢复已编译的模型。根据被编译的模型，这个过程需要几十秒到几分钟，这对于 Space 中的实际 GPU 任务来说太长了。

这就是**预编译**大放异彩的地方。

通过预编译，我们可以一次性导出编译好的模型，保存它，然后在任何进程中即时重新加载，这正是 ZeroGPU 所需要的。这有助于我们减少框架开销，并消除即时编译中通常出现的冷启动时间。

但是我们如何在 ZeroGPU 上进行预编译呢？让我们深入探讨。

## 在 ZeroGPU 上进行预编译

让我们回到 ZeroGPU 的基础示例，并拆解启用预编译所需的内容。为了本次演示，我们将使用 `black-forest-labs/FLUX.1-dev` 模型：

```python
import gradio as gr
import spaces
import torch
from diffusers import DiffusionPipeline

MODEL_ID = 'black-forest-labs/FLUX.1-dev'

pipe = DiffusionPipeline.from_pretrained(MODEL_ID, torch_dtype=torch.bfloat16)
pipe.to('cuda')

@spaces.GPU
def generate(prompt):
    return pipe(prompt).images

gr.Interface(generate, "text", "gallery").launch()

```

在下面的讨论中，我们只编译 `pipe` 的 **transformer** 组件，因为在生成模型中，transformer（或更一般地说，去噪器）是计算量最大的组件。

使用 PyTorch 预编译模型涉及多个步骤：

### 1. 获取示例输入

回想一下，我们要**提前**编译模型。因此，我们需要为模型推导出示例输入。请注意，这些输入类型与我们期望在实际运行中看到的相同。为了捕获这些输入，我们将利用 `spaces` 包中的 `spaces.aoti_capture` 辅助函数：

```python
with spaces.aoti_capture(pipe.transformer) as call:
    pipe("arbitrary example prompt")

```

当用作上下文管理器时，`aoti_capture` 会拦截对任何可调用对象（在我们的例子中是 `pipe.transformer`）的调用，阻止其执行，捕获原本要传递给它的输入参数，并将其值存储在 `call.args` 和 `call.kwargs` 中。

### 2. 导出模型

现在我们有了 transformer 组件的示例 args 和 kwargs，我们可以使用 `torch.export.export` 工具将其导出为 PyTorch `ExportedProgram`：

```python
exported_transformer = torch.export.export(
    pipe.transformer,
    args=call.args,
    kwargs=call.kwargs,
)

```

导出的 PyTorch 程序是一个计算图，它表示张量计算以及原始模型的参数值。

### 3. 编译导出的模型

一旦模型被导出，编译它就相当简单了。

PyTorch 中传统的预编译通常需要将模型保存到磁盘上，以便稍后重新加载。在我们的例子中，我们将利用 `spaces` 包中的一个辅助函数：`spaces.aoti_compile`。它是 `torch._inductor.aot_compile` 的一个小包装器，根据需要管理模型的保存和延迟加载。它的用法如下：

```python
compiled_transformer = spaces.aoti_compile(exported_transformer)

```

现在，这个 `compiled_transformer` 就是一个已预编译好的二进制文件，可以用于推理了。

### 4. 在流程中使用编译后的模型

现在我们需要将编译好的Transformer绑定到原始流水线，即`pipeline`。

一种简单且近乎可行的方法是直接修补流水线，例如`pipe.transformer = compiled_transformer`。不幸的是，这种方法行不通，因为它会删除重要的属性，如`dtype`、`config`等。仅修补`forward`方法效果也不好，因为这样我们会在内存中保留原始模型参数，常常导致运行时出现OOM错误。

`spaces`包也为此提供了一个实用工具——`spaces.aoti_apply`：

```python
spaces.aoti_apply(compiled_transformer, pipe.transformer)

```

瞧！它会负责用我们编译好的模型修补`pipe.transformer.forward`，同时清理掉内存中的旧模型参数。

### 5. 整合所有步骤

要执行前三个步骤（拦截输入示例、导出模型、使用PyTorch inductor编译），我们需要一个真实的GPU。在`@spaces.GPU`函数之外获得的CUDA模拟是不够的，因为编译确实依赖于硬件，例如，它依赖于微基准测试运行来调整生成的代码。这就是为什么我们需要将所有内容包装在一个`@spaces.GPU`函数中，然后将编译好的模型返回到我们应用的根目录。从我们原始的演示代码开始，得到如下修改：

```diff
  import gradio as gr
  import spaces
  import torch
  from diffusers import DiffusionPipeline
  
  MODEL_ID = 'black-forest-labs/FLUX.1-dev'
  
  pipe = DiffusionPipeline.from_pretrained(MODEL_ID, torch_dtype=torch.bfloat16)
  pipe.to('cuda')
  
+ @spaces.GPU(duration=1500) # 启动期间允许的最大持续时间
+ def compile_transformer():
+     with spaces.aoti_capture(pipe.transformer) as call:
+         pipe("arbitrary example prompt")
+ 
+     exported = torch.export.export(
+         pipe.transformer,
+         args=call.args,
+         kwargs=call.kwargs,
+     )
+     return spaces.aoti_compile(exported)
+ 
+ compiled_transformer = compile_transformer()
+ spaces.aoti_apply(compiled_transformer, pipe.transformer)
  
  @spaces.GPU
  def generate(prompt):
      return pipe(prompt).images
  
  gr.Interface(generate, "text", "gallery").launch()

```

仅仅增加了十几行代码，我们就成功地让我们的演示快了很多（在FLUX.1-dev的情况下快了1.7倍）。

如果你想了解更多关于AoT编译的信息，可以阅读PyTorch的AOTInductor教程

## 注意事项

既然我们已经展示了在ZeroGPU约束下可以实现的加速，接下来我们将讨论在使用此设置时遇到的一些注意事项。

### 量化

AoT可以与量化结合使用，以提供更大的加速。
对于图像和视频生成，FP8训练后动态量化方案提供了良好的速度-质量权衡。
然而，FP8要求CUDA计算能力至少为9.0才能工作。
幸运的是，对于ZeroGPU，由于它们基于H200，我们已经可以利用FP8量化方案。

要在我们的AoT编译工作流中启用FP8量化，我们可以像这样利用`torchao`提供的API：

```diff
+ from torchao.quantization import quantize_, Float8DynamicActivationFloat8WeightConfig

+ # 在导出步骤之前量化transformer。
+ quantize_(pipe.transformer, Float8DynamicActivationFloat8WeightConfig())

exported_transformer = torch.export.export(
    pipe.transformer,
    args=call.args,
    kwargs=call.kwargs,
)

```

（你可以在此处找到关于TorchAO的更多细节。）

然后我们可以按照上面概述的步骤继续。使用量化提供了额外的1.2倍加速。

### 动态形状

图像和视频可以有不同形状和尺寸。因此，在执行AoT编译时，考虑形状动态性也很重要。`torch.export.export`提供的原语使其易于配置，以指定哪些输入应相应处理为动态形状，如下所示。

对于Flux.1-Dev transformer，不同图像分辨率的变化会影响其`forward`的两个参数：

- `hidden_states`：噪声输入潜在表示，transformer需要对其进行去噪。它是一个3D张量，代表`batch_size`、`flattened_latent_dim`、`embed_dim`。当批次大小固定时，`flattened_latent_dim`会随着图像分辨率的任何改变而变化。
- `img_ids`：编码的像素坐标的2D数组，形状为`height * width, 3`。在这种情况下，我们希望使`height * width`动态。

我们首先定义希望（潜在）图像分辨率变化的范围。
为了推导这些值范围，我们检查了流水线中`hidden_states`相对于不同图像分辨率的形状。确切的值取决于模型，需要手动检查和一些直觉。对于Flux.1-Dev，我们最终得到：

```python
transformer_hidden_dim = torch.export.Dim('hidden', min=4096, max=8212)

```

然后我们定义一个参数名称映射，以及我们期望其输入值中哪些维度是动态的：

```python
transformer_dynamic_shapes = {
    "hidden_states": {1: transformer_hidden_dim}, 
    "img_ids": {0: transformer_hidden_dim},
}

```

然后我们需要让我们的动态形状对象复制示例输入的结构。不需要动态形状的输入必须设置为`None`。这可以很容易地使用PyTorch的`tree_map`工具完成：

```python
from torch.utils._pytree import tree_map

dynamic_shapes = tree_map(lambda v: None, call.kwargs)
dynamic_shapes |= transformer_dynamic_shapes

```

现在，在执行导出步骤时，我们只需将`transformer_dynamic_shapes`提供给`torch.export.export`：

```python
exported_transformer = torch.export.export(
    pipe.transformer,
    args=call.args,
    kwargs=call.kwargs,
    dynamic_shapes=dynamic_shapes,
)

```

查看此Space，它展示了如何在导出步骤中同时使用量化和动态形状。

### 多编译 / 共享权重

当动态性非常重要时，动态形状有时是不够的。

例如，对于Wan系列视频生成模型，如果你希望编译后的模型能生成不同分辨率，就会出现这种情况。
在这种情况下可以做一件事：为每个分辨率编译一个模型，同时保持模型参数共享，并在运行时分派正确的模型

这是这种方法的一个最小示例：zerogpu-aoti-multi.py。你也可以在Wan 2.2 Space中看到此范式的完整工作实现。

### FlashAttention-3

由于ZeroGPU硬件和CUDA驱动程序与Flash-Attention 3（FA3）完全兼容，我们可以在ZeroGPU Space中使用它来进一步加速。FA3适用于提前编译。所以，这对我们的情况来说是理想的。

从源代码编译和构建FA3可能需要几分钟，并且这个过程依赖于硬件。作为用户，我们不想浪费宝贵的ZeroGPU计算时间。这就是Hugging Face的`kernels`库发挥作用的地方。它提供了对与给定硬件兼容的预构建内核的访问。例如，当我们尝试运行：

```python
from kernels import get_kernel

vllm_flash_attn3 = get_kernel("kernels-community/vllm-flash-attn3")

```

它会尝试从`kernels-community/vllm-flash-attn3`仓库加载一个与当前设置兼容的内核。否则，由于兼容性问题，它会出错。幸运的是，这对我们来说，在ZeroGPU Space上可以无缝工作。这意味着我们可以利用`kernels`库，在ZeroGPU上发挥FA3的强大功能。

这是一个用于Qwen-Image模型的FA3注意力处理器的完整工作示例。

### 区域编译

到目前为止，我们一直在编译整个模型。根据模型的不同，全模型编译可能导致相当长的冷启动时间。长的冷启动时间会使开发体验不愉快。

我们也可以选择编译模型内的特定区域，这能在保留全模型编译几乎所有优势的同时，显著降低冷启动时间。当模型包含重复的计算模块时，区域编译就变得极具前景。例如，标准语言模型通常包含多个结构相同的Transformer模块。

在我们的示例中，我们可以预先编译Flux transformer中的重复模块，并将编译后的计算图传播到其余重复模块中。Flux Transformer包含两类重复模块：FluxTransformerBlock和FluxSingleTransformerBlock。

您可以通过此Space查看完整示例。

💡 对于Flux.1-Dev模型，采用区域编译可将编译时间从**6分钟**缩短至仅**30秒**，同时实现完全相同的加速效果。

### 使用来自Hub的预编译计算图

当模型（甚至单个模型模块）被预先编译后，我们可以将编译好的计算图模块序列化为工件供后续复用。在Spaces的ZeroGPU驱动演示场景中，这将通过跳过编译时间大幅缩短演示启动时间。

为保持存储轻量化，我们可以仅保存编译后的模型计算图，而无需在工件中包含任何模型参数。

请查看此合集，其中展示了获取编译模型计算图、将其推送至Hub，并用于构建演示的完整工作流程。

## 采用AoT编译的ZeroGPU Spaces演示

### 加速效果对比

- 未使用AoTI的FLUX.1-dev
- 使用AoTI和FA3的FLUX.1-dev（**1.75倍加速**）

### 特色AoTI Spaces

- FLUX.1 Kontext
- QwenImage Edit
- Wan 2.2

- 区域编译方案
- 结合AOT的区域编译
- Diffusers原生集成
- 更多性能数据

## 结论

Hugging Face Spaces中的ZeroGPU是一项强大功能，通过提供高性能计算资源赋能AI开发者。本文展示了用户如何利用PyTorch的提前编译技术来加速基于ZeroGPU的应用程序。

我们以Flux.1-Dev模型展示了加速效果，但这些技术并不局限于该模型。因此，我们鼓励您尝试这些技术，并在此社区讨论中向我们提供反馈。

## 资源

- 访问我们在Hub上的ZeroGPU-AOTI组织，查看运用本文技术的演示合集
- 浏览spaces.aoti_* APIs源代码以深入了解接口
- 查看Hub上的Kernels Community组织
- 从此处了解更多关于区域编译的信息
- 升级至Hugging Face Pro版以创建您自己的ZeroGPU Spaces（每日可获得25分钟H200使用时长）

致谢：感谢ChunTe Lee为本文制作了精美的封面图。感谢Pedro和Vaibhav对本文提出的反馈意见。感谢PyTorch团队的Angela Yi在AOT技术方面给予的指导。

---

> 本文由AI自动翻译，原文链接：[Make your ZeroGPU Spaces go brrr with ahead-of-time compilation](https://huggingface.co/blog/zerogpu-aoti)
> 
> 翻译时间：2026-02-14 04:13
