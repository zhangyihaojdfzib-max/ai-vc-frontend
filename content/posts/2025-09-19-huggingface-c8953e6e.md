---
title: Scaleway成为Hugging Face推理服务新提供商
title_original: Scaleway on Hugging Face Inference Providers 🔥
date: '2025-09-19'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/inference-providers-scaleway
author: ''
summary: Hugging Face宣布Scaleway正式成为其推理服务提供商，用户可通过Hub界面或客户端SDK直接调用Scaleway托管的流行开源模型，如GPT-OSS、Qwen3等。该服务基于欧洲数据中心，提供低延迟、按Token付费的生成式API，支持结构化输出和多模态处理，并允许用户使用自有API密钥或通过Hugging
  Face账户路由请求。
categories:
- AI基础设施
tags:
- Hugging Face
- 推理服务
- Scaleway
- 开源模型
- 无服务器计算
draft: false
translated_at: '2026-02-05T04:14:55.343481'
---

# Scaleway 成为 Hugging Face 推理服务提供商 🔥

我们非常高兴地宣布，**Scaleway** 现已成为 Hugging Face Hub 上支持的推理服务提供商！
Scaleway 加入了我们不断壮大的生态系统，直接在 Hub 的模型页面上增强了无服务器推理的广度和能力。推理服务提供商也已无缝集成到我们的客户端 SDK（包括 JS 和 Python 版本）中，让您能够轻松地通过首选提供商使用各种模型。

此次发布使得从 Hugging Face 直接访问流行的开放权重模型（如 **gpt-oss**、**Qwen3**、**DeepSeek R1** 和 **Gemma 3**）变得前所未有的简单。您可以在 Hub 上浏览 Scaleway 的组织页面：https://huggingface.co/scaleway，并在此处试用受支持的热门模型：https://huggingface.co/models?inference_provider=scaleway&sort=trending。

**Scaleway 生成式 API** 是一项完全托管的无服务器服务，通过简单的 API 调用即可访问来自领先研究实验室的前沿 AI 模型。该服务提供具有竞争力的按 Token 付费定价，起价为每百万 Token 0.20 欧元。

该服务运行在位于欧洲数据中心（法国巴黎）的安全基础设施上，确保了欧洲用户的数据主权和低延迟。该平台支持高级功能，包括结构化输出、函数调用以及面向文本和图像处理的多模态能力。

Scaleway 的推理基础设施专为生产环境打造，首个 Token 的响应时间低于 200 毫秒，非常适合交互式应用和智能体工作流。该服务同时支持文本生成和嵌入模型。您可以在 https://www.scaleway.com/en/generative-apis/ 了解更多关于 Scaleway 平台和基础设施的信息。

在其专门的文档页面中阅读更多关于如何使用 Scaleway 作为推理服务提供商的信息。

查看支持的模型列表。

## 工作原理

### 在网站用户界面中

1.  在您的用户账户设置中，您可以：
    *   为您已注册的提供商设置您自己的 API 密钥。如果未设置自定义密钥，您的请求将通过 HF 路由。
    *   按偏好排序提供商。这适用于模型页面中的小部件和代码片段。

![推理服务提供商](/images/posts/ffb2ed6fa1d1.png)

2.  如前所述，调用推理服务提供商时有两种模式：
    *   自定义密钥（使用您自己的对应推理服务提供商的 API 密钥，直接调用该提供商）
    *   由 HF 路由（在这种情况下，您不需要提供商的 Token，费用将直接计入您的 HF 账户，而非提供商的账户）

![推理服务提供商](/images/posts/d2717fd3c654.png)

3.  模型页面会展示第三方推理服务提供商（与当前模型兼容的提供商，按用户偏好排序）

### 从客户端 SDK

#### 使用 Python，通过 huggingface_hub

以下示例展示了如何使用 Scaleway 作为推理服务提供商来调用 OpenAI 的 gpt-oss-120b 模型。您可以使用 Hugging Face Token 通过 Hugging Face 自动路由，或者如果您有自己的 Scaleway API 密钥，也可以使用它。

注意：这需要使用较新版本的 `huggingface_hub`（>= 0.34.6）。

```python
import os
from huggingface_hub import InferenceClient

client = InferenceClient(
    provider="scaleway",
    api_key=os.environ["HF_TOKEN"],
)

messages = [
    {
        "role": "user",
        "content": "Write a poem in the style of Shakespeare"
    }
]

completion = client.chat.completions.create(
    model="openai/gpt-oss-120b",
    messages=messages,
)

print(completion.choices[0].message)

```

#### 使用 JS，通过 @huggingface/inference

```js
import { InferenceClient } from "@huggingface/inference";

const client = new InferenceClient(process.env.HF_TOKEN);

const chatCompletion = await client.chatCompletion({
  model: "openai/gpt-oss-120b",
  messages: [
    {
      role: "user",
      content: "Write a poem in the style of Shakespeare",
    },
  ],
  provider: "scaleway",
});

console.log(chatCompletion.choices[0].message);

```

## 计费

计费方式如下：

对于直接请求，即当您使用推理服务提供商的密钥时，您将由相应的提供商计费。例如，如果您使用 Scaleway API 密钥，费用将计入您的 Scaleway 账户。

对于路由请求，即当您通过 Hugging Face Hub 进行身份验证时，您只需支付标准的提供商 API 费率。我们不收取额外加价；我们只是直接传递提供商的成本。（未来，我们可能会与我们的提供商合作伙伴建立收入分成协议。）

重要提示‼️ PRO 用户每月可获得价值 2 美元的推理额度。您可以在所有提供商中使用这些额度。🔥

订阅 **Hugging Face PRO 计划** 以获得推理额度、ZeroGPU、Spaces 开发者模式、20 倍更高的限制等权益。

我们也为已登录的免费用户提供少量配额的免费推理，但如果可以，请升级到 PRO！

## 反馈与后续步骤

我们非常希望得到您的反馈！请在此处分享您的想法和/或评论：https://huggingface.co/spaces/huggingface/HuggingDiscussions/discussions/49

---

> 本文由AI自动翻译，原文链接：[Scaleway on Hugging Face Inference Providers 🔥](https://huggingface.co/blog/inference-providers-scaleway)
> 
> 翻译时间：2026-02-05 04:14
