---
title: 用Python和Gradio构建AI购物助手MCP服务器
title_original: 'Implementing MCP Servers in Python: An AI Shopping Assistant with
  Gradio'
date: '2025-07-31'
source: Hugging Face Blog
source_url: https://huggingface.co/blog/gradio-vton-mcp
author: ''
summary: 本文介绍了如何使用Python和Gradio框架构建MCP服务器，实现一个AI驱动的购物助手。该助手能够结合LLM的通用推理能力与Hugging
  Face上的专门模型（如IDM-VTON虚拟试穿模型），让用户通过自然语言指令浏览服装并进行虚拟试穿。文章重点讲解了Gradio如何自动将Python函数转换为LLM工具、处理文件上传和进度通知，并展示了如何集成VS
  Code的AI聊天功能作为交互界面，为开发者提供了快速赋予LLM实际应用能力的实践方案。
categories:
- AI产品
tags:
- MCP服务器
- Gradio
- Python开发
- 虚拟试穿
- AI助手
draft: false
translated_at: '2026-02-24T04:35:17.890546'
---

# 使用 Python 实现 MCP 服务器：一个基于 Gradio 的 AI 购物助手

Python 开发者们，想赋予你们的 LLM 超能力吗？Gradio 是实现这一目标的最快方式！借助 Gradio 的模型上下文协议（MCP）集成，你的 LLM 可以直接接入 Hugging Face Hub 上托管的数千个 AI 模型和空间。通过将 LLM 的通用推理能力与 Hugging Face 上模型的专门能力相结合，你的 LLM 将不再局限于回答文本问题，而是能够真正解决你日常生活中的实际问题。

对于 Python 开发者而言，Gradio 让实现强大的 MCP 服务器变得轻而易举，它提供了以下特性：

- **自动将 Python 函数转换为 LLM 工具**：你的 Gradio 应用中的每个 API 端点都会自动转换为一个 MCP 工具，并配有相应的名称、描述和输入模式。函数的文档字符串用于生成工具及其参数的描述。
- **实时进度通知**：Gradio 将进度通知流式传输给你的 MCP 客户端，让你能够实时监控状态，而无需自己实现此功能。
- **自动文件上传**，包括支持公共 URL 和处理各种文件类型。

想象一下：你讨厌购物，因为它太花时间，而且你害怕自己试穿衣服。如果 LLM 能为你处理这一切呢？在这篇文章中，我们将创建一个由 LLM 驱动的 AI 助手，它可以浏览在线服装店，找到特定的服装，然后使用虚拟试穿模型向你展示这些衣服穿在你身上的效果。请看下面的演示：

## 目标：你的个人 AI 造型师

为了让我们的 AI 购物助手成为现实，我们将结合三个关键组件：

1.  **IDM-VTON 扩散模型**：这个 AI 模型负责虚拟试穿功能。它可以编辑现有照片，使其看起来像一个人穿着不同的服装。我们将使用 Hugging Face 上的 IDM-VTON 空间，可在此处访问。
2.  **Gradio**：Gradio 是一个开源的 Python 库，可以轻松构建由 AI 驱动的 Web 应用程序，并且对我们的项目至关重要的一点是，它可以创建 MCP 服务器。Gradio 将充当桥梁，允许我们的 LLM 调用 IDM-VTON 模型和其他工具。
3.  **Visual Studio Code 的 AI 聊天功能**：我们将使用 VS Code 内置的 AI 聊天功能（支持添加任意的 MCP 服务器）来与我们的 AI 购物助手交互。这将为发出命令和查看虚拟试穿结果提供一个用户友好的界面。

**IDM-VTON 扩散模型**：这个 AI 模型负责虚拟试穿功能。它可以编辑现有照片，使其看起来像一个人穿着不同的服装。我们将使用 Hugging Face 上的 IDM-VTON 空间，可在此处访问。

**Gradio**：Gradio 是一个开源的 Python 库，可以轻松构建由 AI 驱动的 Web 应用程序，并且对我们的项目至关重要的一点是，它可以创建 MCP 服务器。Gradio 将充当桥梁，允许我们的 LLM 调用 IDM-VTON 模型和其他工具。

**Visual Studio Code 的 AI 聊天功能**：我们将使用 VS Code 内置的 AI 聊天功能（支持添加任意的 MCP 服务器）来与我们的 AI 购物助手交互。这将为发出命令和查看虚拟试穿结果提供一个用户友好的界面。

## 构建 Gradio MCP 服务器

我们 AI 购物助手的核心是 Gradio MCP 服务器。该服务器将公开一个主要工具：

1.  **vton_generation**：此函数将接收一个人体模特图像和一个服装图像作为输入，并使用 IDM-VTON 模型生成一张人物穿着该服装的新图像。

以下是我们 Gradio MCP 服务器的 Python 代码：

```python
from gradio_client import Client, handle_file
import gradio as gr
import re


client = Client("freddyaboulton/IDM-VTON",
                hf_token="<Your-token>")


def vton_generation(human_model_img: str, garment: str):
    """Use the IDM-VTON model to generate a new image of a person wearing a garment."""
    """
    Args:
        human_model_img: The human model that is modelling the garment.
        garment: The garment to wear.
    """
    output = client.predict(
        dict={"background": handle_file(human_model_img), "layers":[], "composite":None},
        garm_img=handle_file(garment),
        garment_des="",
        is_checked=True,
        is_checked_crop=False,
        denoise_steps=30,
        seed=42,
        api_name="/tryon"
    )

    return output[0]

vton_mcp = gr.Interface(
    vton_generation,
    inputs=[
        gr.Image(type="filepath", label="Human Model Image URL"),
        gr.Image(type="filepath", label="Garment Image URL or File")
    ],
    outputs=gr.Image(type="filepath", label="Generated Image")
)

if __name__ == "__main__":
    vton_mcp.launch(mcp_server=True)

```

通过在 `launch()` 方法中设置 `mcp_server=True`，Gradio 会自动将我们的 Python 函数转换为 LLM 能够理解和使用的 MCP 工具。我们函数的文档字符串用于生成工具及其参数的描述。

原始的 IDM-VTON 空间是用 Gradio 4.x 实现的，该版本早于自动 MCP 功能。因此，在这个演示中，我们将构建一个通过 Gradio API 客户端查询原始空间的 Gradio 界面。

最后，使用 python 运行此脚本。

## 配置 VS Code

为了将我们的 Gradio MCP 服务器连接到 VS Code 的 AI 聊天，我们需要编辑 `mcp.json` 文件。此配置告诉 AI 聊天在哪里找到我们的 MCP 服务器以及如何与之交互。

你可以在命令面板中输入 `MCP` 并选择 `MCP: Open User Configuration` 来找到此文件。打开后，请确保存在以下服务器：

```json
{
  "servers": {
  "vton": {
    "url": "http://127.0.0.1:7860/gradio_api/mcp/"
  },
  "playwright": {
    "command": "npx",
    "args": [
      "-y",
      "@playwright/mcp@latest"
    ]
   }
  }
}

```

Playwright MCP 服务器将允许我们的 AI 助手浏览网页。

请确保 `vton` 服务器的 URL 与上一节中打印到控制台的 URL 匹配。要运行 playwright MCP 服务器，你需要安装 `node`。

## 整合所有组件

现在我们可以开始与我们的 AI 购物助手交互了。在 VS Code 中打开一个新聊天，你可以向助手提出类似这样的请求："浏览优衣库网站寻找蓝色 T 恤，并使用我在 [你的图片 URL] 的照片，向我展示我穿上其中三件的样子。"

示例请观看上面的视频！

## 结论

Gradio、MCP 和像 IDM-VTON 这样强大的 AI 模型的结合，为创建智能且有用的 AI 助手开辟了令人兴奋的可能性。通过遵循本博文中概述的步骤，你可以构建自己的助手来解决你最关心的问题！

---

> 本文由AI自动翻译，原文链接：[Implementing MCP Servers in Python: An AI Shopping Assistant with Gradio](https://huggingface.co/blog/gradio-vton-mcp)
> 
> 翻译时间：2026-02-24 04:35
