---
title: Vercel Sandbox：安全运行不可信代码的沙箱服务
title_original: Run untrusted code with Vercel Sandbox - Vercel – Vercel
date: '2025-06-25'
source: Vercel Blog
source_url: https://vercel.com/changelog/run-untrusted-code-with-vercel-sandbox
author: ''
summary: Vercel推出Sandbox服务，基于Fluid计算模型，在临时隔离的微虚拟机中安全运行不可信代码（如AI Agent生成的代码）。该SDK可独立于Vercel平台使用，支持最长45分钟执行时间，并按活跃CPU时间计费，现已进入Beta阶段。文章展示了通过SDK创建沙箱、写入文件并执行Node.js脚本的完整流程，为AI生成代码的安全执行提供了基础设施方案。
categories:
- AI基础设施
tags:
- Vercel Sandbox
- 安全沙箱
- AI Agent
- 不可信代码执行
- Fluid计算
draft: false
translated_at: '2026-05-02T05:00:09.974564'
---

Vercel Sandbox 是一种由 Fluid 计算提供支持的安全云资源。它旨在隔离的临时环境中运行不可信代码，例如由 AI Agent（智能体）生成的代码。

Sandbox 是一个独立的 SDK，可以从任何环境（包括非 Vercel 平台）执行。Sandbox 工作负载通过新的 Sandbox SDK 在临时的、隔离的微虚拟机中运行，支持最长 45 分钟的执行时间。

```
1import { Sandbox } from "@vercel/sandbox";2import { generateText } from 'ai';3
4const result = await generateText({5    model: "anthropic/claude-4-sonnet-20250514",6    prompt: `Write a Node.js script that prints a Haiku poem to stdout.`,7    system: `8      You are a developer that responds with the content of a single Node.js script.9      You must include only the code without any markdown, nothing else.10      Just include Javascript code and no characters before or after the code.11    `,12  });13
14  const sandbox = await Sandbox.create();15
16  await sandbox.writeFiles([17    { path: "script.js", stream: Buffer.from(result.text) },18  ]);19
20  await sandbox.runCommand({21    cmd: "node",22    args: ["script.js"],23    stdout: process.stdout,24    stderr: process.stderr,25  });
```

一个使用 Vercel Sandbox 运行生成代码的示例。

Sandbox 采用 Fluid 计算模型，并根据 Fluid 新的活跃 CPU 时间计费，这意味着您仅在主动使用 CPU 时为计算付费。请查看 Sandbox 定价页面，了解 Hobby 和 Pro 团队的包含配额及定价。

现已进入 Beta 阶段，所有计划的客户均可使用。了解更多关于 Vercel Sandbox 的信息。

---

> 本文由AI自动翻译，原文链接：[Run untrusted code with Vercel Sandbox - Vercel – Vercel](https://vercel.com/changelog/run-untrusted-code-with-vercel-sandbox)
> 
> 翻译时间：2026-05-02 05:00
