---
title: Chat SDK集成AI工具，简化智能体构建
title_original: Chat SDK now includes AI SDK tools - Vercel
date: '2026-05-20'
source: Vercel Blog
source_url: https://vercel.com/changelog/chat-sdk-now-includes-ai-sdk-tools
author: ''
summary: Vercel的Chat SDK新增`chat/ai`子路径，内置AI SDK工具集。开发者可通过`createChatTools(chat)`一键将Chat
  SDK的读写操作接入AI Agent。支持Messenger、reader、moderator三种预设，灵活控制工具范围与审批流程。同时，`toAiMessages`等类型已迁移至新路径，旧导出被标记为弃用。该更新旨在简化聊天机器人的开发与部署流程。
categories:
- AI基础设施
tags:
- Chat SDK
- AI SDK
- Vercel
- 智能体
- 开发工具
draft: false
translated_at: '2026-05-21T06:22:53.757470'
---

Chat SDK 现在通过新的 `chat/ai` 子路径内置了一套 AI SDK 工具集。只需调用一次 `createChatTools(chat)`，即可将 Chat SDK 的读写操作接入你的 Agent（智能体）。

```
import { Chat } from "chat";
import { createChatTools } from "chat/ai";
import { ToolLoopAgent } from "ai";

const chat = new Chat({ userName: "mybot" });
const agent = new ToolLoopAgent({
  model: "openai/gpt-5.4",
  instructions: 'You are a helpful assistant.',
  tools: createChatTools(chat, { preset: "messenger" }),
});
```

使用 Messenger 预设创建一个聊天 Agent（智能体）

- 默认需审批：写入工具通过 `requireApproval` 选项进行控制。
- 预设：`reader`、`messenger` 和 `moderator` 限定工具集的范围。
- 懒加载：仅构建预设允许的工具。

默认需审批：写入工具通过 `requireApproval` 选项进行控制。

预设：`reader`、`messenger` 和 `moderator` 限定工具集的范围。

懒加载：仅构建预设允许的工具。

`toAiMessages` 及其支持类型已移至 `chat/ai`。之前的 `chat` 重新导出已被标记为 `@deprecated`。

阅读文档以开始使用，或尝试我们的模板之一。

Chat SDK 完整指南

了解 Chat SDK 的端到端工作原理：从核心概念到构建你的第一个机器人，再到将其部署到 Slack、Teams 等平台。

阅读指南

---

> 本文由AI自动翻译，原文链接：[Chat SDK now includes AI SDK tools - Vercel](https://vercel.com/changelog/chat-sdk-now-includes-ai-sdk-tools)
> 
> 翻译时间：2026-05-21 06:22
